# EvidenceForge Implementation Plan

**Status:** Phase 8.5 (Dual src/dst HostContext) COMPLETE; Pre-MVP quality fixes ongoing
**Started:** 2026-03-11
**Last Updated:** 2026-05-21

See [CHANGELOG.md](CHANGELOG.md) for detailed development history of completed phases.

---

## Phase 1: Core Generation — COMPLETE

Proved the concept with basic functionality: Pydantic scenario models, StateManager, 2 log formats (Windows Event Security, Zeek conn.log), hour-by-hour generation engine, ground truth documentation. 93 tests.

## Phase 2: Scalability — COMPLETE

Scaled to real-world datasets: parallel threaded emitters, 7 log formats (added eCAR, syslog, bash history, Snort, web access), persona-based temporal distribution, network visibility modeling (SPAN/TAP sensors), multi-OS support (Windows + Linux). 526 tests, 100-user 8-hour scenarios in ~14 seconds.

## Phase 3: MVP Release — COMPLETE

Shipped skill-based architecture: 4 Claude Code Skills (/eforge scenario, generate, validate, evaluate), 15 pre-built personas, eforge install-skills CLI command, scenario reference documentation. Skills handle interactive/creative work; CLI stays deterministic.

## Phase 4: Data Quality Evaluation — COMPLETE

Added `eforge eval` command: 5 scoring dimensions (Record Fidelity, Cross-Source Consistency, Noise Realism, Temporal Realism, Signal Integrity) with 23 sub-scores, 17 log parsers, acceptance criteria pass/fail layer. All deterministic/statistical, no LLM required.

## Phase 5: Data Realism Improvements — COMPLETE

Fixed generator-level tells across 5 sub-phases: SID generation, conn_state diversity, process template pools, failed logon events, process termination, eCAR object diversity, protocol diversity (UDP/ICMP/NTP/SSH), system traffic generation (Kerberos/LDAP/DB), work-hour ramp/clustering, human burstiness patterns. Eval score target >= 85.

## Phase 7: Canonical Event Model — COMPLETE

Replaced manual per-emitter field coordination with SecurityEvent intermediate representation. Two-phase build + dispatch architecture: ActivityGenerator builds SecurityEvent with composable contexts (Host, Auth, Process, Network, DNS, File, Registry, IDS), EventDispatcher routes to StateManager + matching emitters. Migrated logon, logoff, process, connection, DNS, failed logon, and process termination event types. 950+ tests.

---

## Pre-MVP: Consolidated Quality Fixes — IN PROGRESS

- [x] Harden Kerberos transport profile weighting against oversized overlay values that can crash weighted protocol selection (schema now caps weights at 1,000,000; runtime picker clamps and safely falls back on overflow).
- [x] Update the global `eforge-assess` Codex skill so iterative loops default to `scenarios/iteration-test`, prefer family-level realism improvements first, and treat automated eval as a regression guardrail rather than an optimization target. Added a progressive-disclosure family iteration reference, refreshed skill metadata, and verified with the lightweight skill validator only.
- [x] Prepare the `dev` -> `main` PR for the loop-driven realism improvement batch — applied the required v0.9.0 minor version/changelog bump, refreshed `uv.lock`, and passed Ruff plus full normal `uv run pytest --no-cov` before opening the PR.
- [x] Run a blind expert panel on the current loop-188 iteration-test output and summarize reviewer synthetic-confidence scores — panel average synthetic-confidence was 61.0 (Threat Hunter 68, Detection 68, Network 36, Host/EDR 72), with reports and `scores.json` saved under `scenarios/iteration-test/blind-test/loop-188/`.
- [x] Run ten additional family-level iteration-test assessment/fix loops using `eforge-assess`, starting from loop 188 reviewer findings. Completed loops 189-198 plus one extra verification/fix loop 199 for the eCAR endpoint timing family.
  - Loop 189 family contract: Linux scheduler/source timing. Owner: `BaselineMixin` scheduled-task generation plus the ambient Linux PAM/logind initiator helper. Invariant: baseline `CRON` syslog and `pam_unix(cron:session)` evidence must originate from configured cron schedules at minute-boundary launch times with only source logging subsecond texture; random ambient logind noise must not masquerade as cron. Entry paths: cron schedules, ambient Linux session noise, activity-generator CRON fallback, and extra syslog filtering. Consumers: syslog, eCAR process trees, rendered-output probes, and timing/eval parsers. Sibling risk: systemd/anacron timers keep their own source-native timing model and are out of scope unless probes show cron leakage.
  - Loop 189 eval guardrail follow-up: regeneration exposed Linux shell-derived eCAR process/flow telemetry after the visible collection window, so the bash/process scheduler and final eCAR source-timing renderer now treat scenario end time as a shared source-visible boundary before emitting bash, process, flow, and termination evidence.
  - Loop 189 completed: cron probes now show 66 CRON rows, 0 non-minute CRON rows, and 0 `pam_unix(cron:session)` rows; automated eval recovered to 95.844 across 75,592 records with no flags. Blind panel average synthetic-confidence increased from loop 188's 61.0 to 73.0 (Threat Hunter 72, Detection 68, Network 74, Host/EDR 78), consistent with deeper concrete issue surfacing rather than latest-fix regression. Next highest-leverage family is Zeek TCP packet accounting, followed by host activity-pool recurrence and SOA RDATA rendering.
  - User reminder: print the rolling score summary for this ten-loop round at the end of every completed loop before continuing to the next fix.
  - Loop 190 family contract: Zeek TCP packet accounting. Owner: canonical network metric construction plus Zeek connection sensor observation rendering. Invariant: TCP `orig_pkts`/`resp_pkts` and `orig_ip_bytes`/`resp_ip_bytes` must include source-native control packets, ACK-only traffic, teardown behavior, MSS/header variation, and sensor texture; established flows must not collapse to `ceil(payload_bytes / 1460)` or `payload_bytes + packets * fixed_header`. Entry paths: baseline web/proxy traffic, storyline connections, SMB/file transfers, process-network correlations, explicit proxy legs, scan/red-herring traffic, and multi-sensor fanout. Consumers: Zeek conn/files/http/ssl correlation, network-forensics probes, timing/eval parsers, and blind review. Sibling risk: SMB session reuse and HTTP app messiness remain separate families unless touched by shared connection metric changes.
  - Loop 190 completed: committed `ace69242` to add TCP MSS/header/control-packet texture in canonical network accounting plus HTTP/SSL enrichment and SSH session paths. Full verification passed (`uv run pytest -v --no-cov`: 3560 passed, 15 skipped; `ruff check .`; `ruff format --check .`). Regenerated output scored 96.456 across 75,323 records with no eval flags. Packet probes show 5,413 established TCP rows with 0 originator and 0 responder payload-bearing sides exactly matching `ceil(payload / 1460)`. Blind panel average synthetic-confidence is 77.5 (Threat Hunter 82, Detection 76, Network 74, Host/EDR 78); packet-accounting exactness mostly shifted to broader capture/proxy/session texture, while Host/EDR found a concrete high-impact RDP LogonID reuse contradiction.
  - Loop 191 family contract: session-owned lifecycle and process lineage. Owner: `StateManager` session selection/immutability plus `WorldPlanner`/`ActivityGenerator` lock-unlock and Linux shell bootstrap paths. Invariant: a LogonID/session allocated for RDP/network/SSH must not be reused for incompatible local interactive unlocks or mutated across session kinds; visible Linux logins that own subsequent eCAR/bash child commands must create durable source-native shell/transport parent process state before child command emission. Entry paths: RDP bootstrap, workstation lock/unlock, interactive/session reuse, SSH session generation, bash command process telemetry, eCAR FLOW/process rendering. Consumers: Windows Security 4624/4634/4800/4801, Sysmon/eCAR process trees, bash history, syslog SSH/PAM/logind, and blind host review. Sibling risk: broad command-pool personality and Windows device-path diversity are out of scope unless touched by session ownership fixes.
  - Loop 191 completed: committed `6e4136c5`, `b1c4ebe0`, `02b7ac1b`, `174a86fb`, `9c6c849e`, `21451a66`, and `005766b1` to keep Windows session kinds immutable for lock/unlock/RDP reuse and to materialize visible Linux shell parents for SSH, local, loose, and boundary process-create paths. Full verification passed (`uv run pytest -v --no-cov`: 3568 passed, 15 skipped; `ruff check .`; `ruff format --check .`). Regenerated output scored 96.576 across 80,000 records with no eval flags. Reviewer probes found zero RDP Type 10 -> Type 7 LogonID reuse, zero RDP LogonID workstation-event reuse, and zero Linux eCAR bash-parent misses (213/213 visible). Blind panel average synthetic-confidence decreased to 71.0 (Threat Hunter 78, Detection 70, Network 72, Host/EDR 64); the highest-leverage next family is Zeek SOA answer RDATA rendering.
  - Loop 192 family contract: source-native Zeek DNS SOA RDATA. Owner: canonical DNS answer generation and Zeek DNS rendering/parser fixtures. Invariant: SOA answers must render full source-native RDATA (MNAME, RNAME, serial, refresh, retry, expire, minimum) consistently across sensors and domains; qtype-specific DNS answer shapes must not collapse to partial generic strings. Entry paths: internal AD/domain DNS, public DNS registry answers, causal DNS expansion for network activity, multi-sensor Zeek fanout, DNS tunnel/non-tunnel DNS generation, and eval parser handling. Consumers: `zeek-core/dns.json`, `zeek-dmz/dns.json`, DNS eval parsers, SIEM/detection rules, and blind network/detection review. Sibling risk: nmap scan dark-address residue, Snort timestamp precision, Linux local-login volume, and eCAR/Sysmon timing distributions remain separate families unless touched by DNS answer shape fixes.
  - Loop 192 completed: committed `c6952e7e` to render full seven-field SOA RDATA for public and internal DNS answers, with focused DNS/Zeek regression coverage. Verification passed (`uv run pytest -v --no-cov`: 3570 passed, 15 skipped; `ruff check .`; `ruff format --check .`). Regenerated output scored 96.576 across 80,000 records with no eval flags. Reviewer probes found 17 SOA answers, zero malformed SOA rows, and zero XML source-leak/non-XML outputs after the clean regeneration. Final read-only blind panel average synthetic-confidence was 73.0 (Threat Hunter 72, Detection 72, Network 72, Host/EDR 76). Highest-leverage next family is source-native Linux SSH session ordering and sshd process ownership; repeated secondary findings are public PTR/DNS/TLS ownership realism, Kerberos 4769-vs-4624 ordering, and IDS/eCAR product-native texture.
  - Loop 193 family contract: source-native Linux SSH session ordering and sshd process ownership. Owner: SSH/session bootstrap plus canonical Linux process/session generation, with emitters rendering the already-correct ordering and process identities. Invariant: visible accepted SSH authentication and PAM/session-open evidence must precede eCAR `USER_SESSION LOGIN`, shell process creation, bash history command timestamps, and child process/network telemetry; inbound SSH sessions must not flatten unrelated listener, pre-auth child, authenticated child, shell, or file/process actor identities onto one generic PID when source telemetry exposes distinct process IDs. Entry paths: baseline SSH admin sessions, storyline SSH sessions, SCP/SFTP transfers, remote Linux command execution, bash-history process promotion, eCAR FLOW/PROCESS/USER_SESSION rendering, and syslog sshd/PAM/logind emission. Consumers: Linux syslog, eCAR, bash history, Zeek SSH conn rows, and blind host/threat review. Sibling risk: public PTR/DNS/TLS ownership, Kerberos event ordering, IDS alert texture, and broad eCAR product-native schema remain separate families unless touched by shared session/process ownership fixes.
  - Loop 193 completed: committed `2535ad69`, `252bac12`, `9b2a354c`, `1a5f31a5`, `473b7fba`, and `3ae36035` to carry SSH source-readiness timing through canonical session state, materialize/reuse destination-side sshd responder process identity, normalize standalone syslog sshd PIDs, and reserve SSH source tuples globally. Full verification passed after the final fix (`uv run pytest -v --no-cov`: 3577 passed, 15 skipped; `ruff check .`; `ruff format --check .`). Regenerated output scored 96.116 across 79,819 records with no eval flags. Reviewer probes found zero syslog accepted/PAM ordering violations and zero eCAR `USER_SESSION LOGIN` timing violations. Blind panel average synthetic-confidence dropped to 55.0 (Threat Hunter 66, Detection 64, Network 62, Host/EDR Real 72 => synthetic-equivalent 28), the lowest in the current round. Highest-leverage next family is Windows explicit-credential 4648 source/process ownership, followed by Linux bash/sudo texture and web attack/page-response messiness.
  - Loop 194 family contract: Windows explicit-credential event source/process ownership. Owner: canonical Windows process/session activity generation plus explicit-credential Security 4648 construction, with emitters rendering the already-correct event context. Invariant: same-host 4648 explicit-credential events must reference an existing emitting process whose Security 4688, Sysmon Event 1, and EDR process-create visibility precede or source-natively coincide with the credential-use event; `NetworkAddress`/source fields must come from the emitting host/session or a consciously modeled remote-origin context, never from an unrelated workstation or narrative target. Entry paths: `runas.exe`, `mstsc.exe`, MMC/RSAT/admin-tool flows, remote admin/bootstrap actions, RDP/explicit credential use, baseline privileged admin activity, and storyline domain-admin/service-account actions. Consumers: Windows Security XML, Sysmon, eCAR/EDR, Kerberos/logon correlation, detection-rule review, and blind host/detection review. Sibling risk: Linux bash/sudo texture, web attack response-size/storyboard realism, public DNS/PTR/TLS ownership, and IDS/eCAR product-native schema remain separate families unless touched by shared process/session ownership fixes.
  - Loop 194 completed: committed `a1fdb87f` to clamp Security 4648 explicit-credential events after their visible caller process-create timestamps and to ignore unrelated source-IP overrides that do not belong to the emitting host. Full verification passed (`uv run pytest -v --no-cov`: 3579 passed, 15 skipped; `ruff check .`; `ruff format --check .`). Regenerated output scored 96.116 across 79,819 records with no eval flags. Reviewer probes found 39 explicit-credential events, zero source-IP violations, and zero visible caller ordering violations. Blind panel average synthetic-confidence dropped to 52.5 (Threat Hunter 68, Detection Real 68 => 32 synthetic-equivalent, Network Real 72 => 28 synthetic-equivalent, Host/EDR 82), the lowest in the current round. Highest-leverage next family is SSH/SCP endpoint timing and tuple/process reuse.
  - Loop 195 completed as a false-positive verification loop: the Host/EDR SSH/SCP timing claim was caused by incorrect epoch conversion (`1710782215` is `2024-03-18T17:16:55Z`, not `17:36:55Z`; `1710782122` is `2024-03-18T17:15:22Z`, not `17:35:22Z`). No code changed. A family-wide rendered-output probe inspected 396 SSH syslog rows and 406 SSH eCAR flows, finding zero inbound flow/syslog timing violations over 10 seconds and zero `sshd`-owned file creates without recent same-PID SSH syslog. No data was regenerated and no blind panel was rerun; the score state remains loop 194's scored output.
  - Loop 196 family contract: web response-size and app-log texture realism. Owner: canonical web request/profile generation plus web/proxy access rendering inputs, with Zeek HTTP/files and Apache/web access logs consuming the same request facts. Invariant: repeated static resources, redirects, exploit responses, and proxied browsing should vary by asset identity, cache state, compression/client profile, status, and source-native logging perspective; high-volume static files must not render as suspiciously identical sizes or overly clean proxy/app-log sequences across visitors. Entry paths: baseline browsing, proxy browsing, web exploit/storyline requests, Apache access/syslog fragments, Zeek HTTP/files construction, explicit proxy tunnels, and static asset pools. Consumers: web access logs, proxy access logs, Zeek HTTP/files/conn, ASA/proxy correlation, and blind network/detection/threat review. Sibling risk: broader attacker-storyline messiness, eCAR product-native normalization, and source coverage imperfection remain separate families unless touched by shared web request/profile state.
  - Loop 196 completed: committed `a6a25651` and `559e7b69` to route baseline browsing, inbound web sessions, storyline URL/process activity, and generic auto-HTTP enrichment through shared client/profile-aware transfer-size variance. Full verification passed (`uv run pytest -v --no-cov`: 3581 passed, 15 skipped; `ruff check .`; `ruff format --check .`). Regenerated output scored 95.821 across 80,909 records with no eval flags. Web response-size probes found 12 repeated static-resource groups with multiple client profiles, 12 varied across those profiles, and 0 cloned cross-client static groups. Blind panel average synthetic-confidence was 75.0 (Threat Hunter 68, Detection 78, Network 82, Host/EDR 72). The highest-leverage next family is proxy CONNECT/upstream TLS causality, followed by Windows 4624 `IpPort` tuple binding and Linux local-console session noise.
  - Loop 197 family contract: explicit proxy tunnel causality and leg timing. Owner: canonical explicit-proxy transaction generation plus network event/source-timing construction for client→proxy CONNECT and proxy→upstream TLS/HTTP legs, with proxy access, Zeek, and ASA consuming one ordered transaction. Invariant: for a visible explicit-proxy tunnel, client-to-proxy TCP/HTTP CONNECT must start before the proxy-to-upstream connection; proxy access byte accounting must preserve client/proxy/upstream deltas without allowing same-sensor Zeek or ASA evidence to show the upstream leg before the CONNECT leg. Entry paths: baseline explicit-proxy browsing, system/software-update proxy traffic, C2/exfil via proxy, proxy error/deny paths, Zeek HTTP/conn/ssl fanout, ASA build/teardown/NAT rendering, and proxy access rendering. Consumers: `zeek-dmz/conn.json`, `zeek-dmz/http.json`, `zeek-dmz/ssl.json`, `fw-perimeter/cisco_asa.log`, proxy access logs, network/detection blind review, and eval parsers. Sibling risk: Windows 4624 `IpPort` binding, Linux local-console session volume, endpoint heterogeneity, and TLS fingerprint/client identity coupling remain separate families unless touched by shared proxy transaction state.
  - Loop 197 completed: committed `3e208048` to order explicit proxy tunnel legs from source-visible client CONNECT timing before proxy-originated upstream TLS/HTTP, including delayed client process visibility and source port ownership. Full verification passed (`uv run pytest -v --no-cov`: 3582 passed, 15 skipped; `ruff check .`; `ruff format --check .`). Regenerated output scored 96.378 across 75,288 records with no eval flags. Proxy ordering probes found 1,416 CONNECT 200 rows, 647 paired upstream TLS rows, 553 exact proxy-overhead matches, and 0 upstream-before-CONNECT violations. Blind panel average synthetic-confidence dropped to 62.0 (Threat Hunter 72, Detection 76, Network Real 63 => synthetic-equivalent 37, Host/EDR 63); the proxy causality anchor was cleared and the next highest-leverage family is Linux command/session texture.
  - Loop 198 completed: committed `dcf26461` to vary Linux command/session texture and per-identity shell habits by tightening exact-repeat budgets, preventing workstation command pools from leaking onto servers, reducing seeded typo artifacts, diversifying sudo command templates from YAML, thinning generic server login/logind noise, and resetting visible-window command memory after warm-up. Verification passed (`uv run pytest -v --no-cov`: 3588 passed, 15 skipped; `ruff check .`; `ruff format --check .`; `eforge validate-config`; regeneration). Regenerated output scored 96.511 across 75,581 records. Probes found 695 bash rows, 342 unique commands, 15 exact commands repeated 5+ times, zero server desktop-command leaks, 27 sudo `COMMAND=` rows, 22 unique sudo commands, and zero sudo commands repeated 5+ times. Blind independent average synthetic-confidence improved to 56.5 (Threat Hunter 78, Detection 78, Network Real 64 => synthetic-equivalent 36, Host/EDR Real 66 => synthetic-equivalent 34), but deliberation moved all reviewers to Synthetic (82/84/58/61) because of repeated eCAR timestamp ownership contradictions in SSH/SCP/process/flow/file evidence.
  - Loop 199 family contract: eCAR endpoint occurrence-time ownership for Linux SSH/SCP/process/flow/file evidence. Owner: Linux shell/session planning, storyline SSH/SCP generation, canonical ProcessContext/NetworkContext/FileContext construction, SourceTimingPlan/eCAR occurrence timestamps, and eCAR renderer inputs. Invariant: visible sender-side process creation must precede same-action eCAR FLOW rows; Zeek/syslog SSH session evidence for a tuple must not precede the owning endpoint process beyond modeled source latency; receiver-side file creation must not precede sender-side transfer action for matching SCP/SSH tuples; independent SSH/session rows must not be batch-stamped with render time. Entry paths: storyline SCP/file transfer, SSH session generation, Linux bash command process promotion, eCAR PROCESS/FLOW/FILE side effects, syslog sshd/PAM session emission, Zeek SSH/conn rendering, and source-timing delays. Consumers: Linux eCAR, syslog, Zeek conn/ssh-adjacent flows, bash history, blind detection/threat review, and rendered-output ordering probes. Sibling risk: PsExec lifecycle, OCSP status realism, inter-sensor timing texture, and scenario narrative legibility remain separate families unless touched by shared source-timing contracts.
  - Loop 199 completed: committed `815f3827`, `226f1480`, and `7568ba52` to preserve Linux eCAR occurrence timing, align foreground Linux source-visible termination timing, and keep canonical sshd PIDs for visible syslog sessions. Verification passed (`uv run pytest -v --no-cov`: 3592 passed, 15 skipped; `ruff check .`; `ruff format --check .`; regeneration). Regenerated output scored 96.511 across 75,581 records with no eval flags. Hard probes found zero Linux eCAR same-ms clump, flow-before-process, SSH syslog-before-process, or DB-PROD -> APP-INT SCP ordering violations. Blind-style reviewer scores were Threat Hunter 56, Detection 60, Network 54, and Host/EDR 58, for an average synthetic-confidence score of 57.0; deliberation triggered on the below-60 average and kept the consensus at Synthetic, with next highest-leverage targets being eCAR endpoint coverage/product texture and Linux shell/attacker friction.
- [x] Prepare the `dev` -> `main` PR for the loop 189-199 realism follow-up batch — inspected `origin/main..dev`, applied the required v0.9.1 patch version/changelog bump, ran release checks (`ruff check`, `ruff format --check`, `pytest --no-cov`), pushed `dev`, and opened draft PR #227.
- [x] Run ten additional family-level iteration-test assessment/fix loops using the strengthened loop prompt, starting at loop 179 from loop-178 Windows source-native and session-continuity findings. Completed loops 179-188 with owning-layer fixes, focused regression tests, regenerated data, eval artifacts, and reviewer-finding probes.
- [x] Loop 188 family fix — populated source-native proxy identities in the canonical `ProxyContext` owner path instead of patching rendered proxy output; regenerated loop-188 output scored 96.15 across 78,731 records, with 745 authenticated proxy rows, 14 distinct proxy usernames, all 745 assigned-workstation non-407 rows authenticated, all 188 machine-context rows using machine accounts, and zero authenticated 407/server-origin rows.
- [x] Loop 187 family fix — moved external scanner destination-port selection into source-sticky profile config and applied it to both ASA deny and Linux UFW-block scan paths; regenerated loop-187 output scored 96.15 across 78,731 records, with 633 S0 scan-like rows, zero high-volume balanced flat-port-menu scanners, and zero S0 ports outside the assigned source profile for high-volume scanner sources.
- [x] Loop 186 family fix — replaced readable DNS tunnel TXT response templates with opaque token/sequence shapes and added config validation rejecting human-readable literal grammar in tunnel response templates; regenerated loop-186 output scored 96.16 across 76,549 records, with 237 tunnel TXT records, 217 unique NOERROR answers, and zero `xid`/`path`/`ok` readable-response hits.
- [x] Loop 185 family fix — added host-sticky infrastructure User-Agent selection for Windows Update domain overrides, diversified the Windows Update build/client-protocol pool, and moved trust-list `ctldl.windowsupdate.com` traffic to CryptoAPI identity; regenerated loop-185 output scored 96.16 across 76,549 records, with 3 Windows Update UA variants, zero old pre-release UA rows, and zero CTL-download rows using Windows Update Agent.
- [x] Loop 184 family fix — moved OCSP-over-HTTP GET request paths into the TLS realism model/config so rendered Zeek HTTP evidence uses long DER-shaped encoded OCSPRequest URIs instead of short synthetic hex tokens; regenerated loop-184 output scored 95.93 across 76,267 records, with 59/59 OCSP HTTP rows using long encoded paths and zero short-hex paths.
- [x] Loop 183 family fix — added sensor-local timing texture for locked DNS/ICMP Zeek packet-accounting rows while preserving exact packet sizes; regenerated loop-183 output scored 95.93 across 76,267 records, and dual-sensor fully cloned `conn.json` rows dropped from 1,019 to 0.
- [x] Loop 182 family fix — aligned web-exploit source-native identity, process ownership, and network provenance across WEB-EXT-01 web access, Apache syslog, eCAR, Zeek, and firewall evidence; final output scored 95.93 across 76,267 records, with all web-exploit provenance probes passing.
- [x] Loop 181 family fix — enforced Linux foreground command serialization and dependent side-effect ordering across bash history, eCAR process lifetimes, and network transfer evidence; regenerated loop-181 output scored 96.25 across 79,550 records, with zero eCAR foreground-shell violations and the DB dump/gzip/scp rendered probe passing.
- [x] Loop 180 blind-review deliberation summary — synthesized the four provided expert reports into `scenarios/iteration-test/blind-test/loop-180/deliberation.md` without inspecting generated data.
- [x] Strengthen the family-level iteration-loop prompt so future fixes must define the owning abstraction, inventory sibling paths, prove family-wide behavior, reject path-shaped patches, and self-grade narrow/partial fixes.
- [x] Run five family-level iteration-test assessment/fix loops using `eforge-assess`, starting from loop 173 P0/P1 findings — completed loops 174-178 with focused code fixes, regression tests, full normal pytest/Ruff verification, regenerated data, automated eval artifacts, probe artifacts, and blind-panel reports. The loop-178 final panel averaged 66.0 synthetic confidence with the next highest-leverage family now Windows source-native 1102/lock-state continuity, Linux bash-history individuality, and network timing/domain regularity.
- [x] Loop 178 durable Windows interactive-session follow-up — extended the loop-177 fix from second-scale Type 2 bursts to longer-window same-user workstation session reuse across baseline, RSAT, RDP, and suspicious CLI paths. Final probes found zero Type 2 clusters within 60 seconds, one residual active overlap, and 4672 human special-privilege events scoped to `marcus.chen`; blind reports are saved under `scenarios/iteration-test/blind-test/loop-178/`.
- [x] Loop 177 blind assessment and family-target selection — tight same-user Type 2 bursts dropped to zero in rendered probes, but blind average synthetic-confidence rose to 66.50 as reviewers surfaced longer-window interactive-session reuse, 4672 privilege realism, Zeek multi-sensor independence, and Sysmon PE metadata fidelity.
- [x] Loop 176 blind assessment and family-target selection — bash workflow texture improved average synthetic-confidence from 70.50 to 61.50, and the next highest-leverage root cause is repeated same-user Type 2 workstation logon bursts from baseline session planning.
- [x] Check regression-test coverage pattern for loop fixes and create a family-level iteration-loop improvement prompt — recent loop-era code fixes overwhelmingly include focused tests (136/138 sampled code `fix:` commits touched tests), so the new prompt emphasizes family-level model changes and probes instead of historical test backfill.
- [x] Analyze iteration-test assessment loops for repeated findings, plateau causes, and durable realism improvements — conclusion: specific hard defects usually stay fixed, but broad source-native model families recur and the automated eval is saturated/misaligned with current expert findings.
- [x] Loop 163 blind-review deliberation summary — synthesized the four expert reports into `scenarios/iteration-test/blind-test/loop-163/deliberation.md` without introducing outside evidence.
- [x] Extract license-neutral output target rendering and evaluation support from the SOF-ELK external parser branch while leaving parser pipeline code/docs/tests out of dev.
- [x] Run the full slow-enabled pytest suite on the output-target extraction branch — `uv run pytest --include-slow --no-cov` passed with `3296 passed, 24 skipped`; no regressions or fixes were needed.
- [x] Run the full slow-enabled pytest suite on current `dev` after Loop 127 — `uv run pytest --include-slow --no-cov` passed with `3369 passed, 2 skipped` in 83.34s; no regressions or fixes were needed.
- [x] Update eforge skill layout guidance so scenario-authored files, optional artifacts, and generated data stay under `scenarios/<slug>/` regardless of output target. Verification passed: stale-layout grep returned no active old output paths, generated Codex skill validation passed for all eforge skills, `uv run pytest tests/unit/test_install_skills.py --no-cov` passed, and `git diff --check` passed.
- [x] Prepare the `dev` → `main` PR for output-target rendering — inspected `main..dev`, applied the required v0.8.0 minor version/changelog bump, ran non-coverage release checks, pushed `dev`, and opened the PR into `main`.
- [x] Automated PR Review & Merge run 2026-05-20 — reviewed open Codex PRs #221 and #222 against current `dev`; landed adapted root-cause fixes for output-target marker safety and ASA raw severity/PRI rendering with focused tests; local `gh` retarget/merge was unavailable due sandbox network restrictions and the GitHub connector rejected PR write actions.
- [ ] **P1** Reduce syslog memory pressure in long scenarios by allowing barrier flushes to write year-partitioned syslog files, while preserving final sort/logind normalization at close.
- [ ] **P2** Revisit proxy access log realism and parser compatibility; consider switching `proxy_access.log` from W3C Extended format to Apache/Nginx combined-style output with absolute URLs and CONNECT targets.
- [x] Prepare `dev` → `main` PR for final PR-review integration batch — inspected `main..dev`, applied the required v0.7.2 patch version/changelog bump, ran release checks, pushed `dev`, and opened the PR into `main`.
- [x] Run slow comprehensive pytest suite after final PR integration — `uv run pytest -v --include-slow --no-cov` passed on current `dev` with `3265 passed, 24 skipped`; no regressions or follow-up fixes were needed.
- [x] Move coverage to the release lane — default/local and feature-PR tests now run without coverage, final `dev` → `main` readiness keeps explicit coverage and slow comprehensive gates, CI/docs/agent guidance are updated, workflow YAML parses, Ruff checks pass, and `uv run pytest --no-cov` passed in 34.74s.
- [x] Refresh the v0.7.0 `dev` -> `main` release PR after PR #162 merged into `dev` — confirmed the release PR head includes merge commit `87ac753`, kept the version at `0.7.0`, and updated the changelog with the calibration-cleanup work from PR #162.
- [x] Split slow comprehensive tests from coverage instrumentation in CI and update contributor/agent testing guidance — normal coverage gate passed at 79.38% with slow tests skipped; slow comprehensive suite passed separately with `--no-cov` in 2m36s; Ruff checks passed.
- [x] Prepare `dev` → `main` PR for the slow-test CI split — inspected `main..dev`, applied the required v0.7.0 version/changelog bump, stabilized the slow gate, ran release checks, pushed `dev`, and opened the PR into `main`.
- [x] Prepare `dev` → `main` PR — inspected `main..dev`, applied the required v0.6.2 version/changelog bump on `dev`, ran release checks, pushed, and opened the PR into `main`.
- [x] Retargeted and merged PR fixes #138-#141 into `dev`, then reworked PR #137 against current `dev` so Windows event spool hardening preserves all current emitter fixup passes before merge.
- [x] Remediate Windows singleton PID path traversal telemetry suppression — canonicalize Windows singleton paths with ntpath before seeded PID reuse and cover traversal variants with a unit test.
- [x] Fix DNS tunnel label payload accounting so sequence metadata does not truncate counted exfiltration bytes.
- [x] Fix actor-scoped LogonID inheritance for typed `log_cleared` storyline events.
- [x] Automated PR Review & Merge run 2026-05-16 — reviewed open PR #163 against `dev`; found the underlying issues real but the branch needed two follow-up fixes before merge. Prepared and verified local amended commit `a0a3528`, but could not publish/comment/reject/merge because direct git push could not resolve GitHub and the GitHub write action was rejected.
- [x] Automated PR Review & Merge follow-up 2026-05-16 — re-evaluated amended PR #163 (`044b097`) against `dev`; prior inbound web-session status issue is fixed, but CLI HTTP command parsing still treats `curl -sI` as GET and bare lowercase `curl -i` as HEAD, so the PR is not ready to merge. PR remains draft and also calls out a verified DB bash-history/eCAR timing mismatch as next work.
- [x] Validate loop blind-review timing/jitter root-cause hypothesis by mining loop artifacts and reviewer reports, weighting later loops as more representative of current behavior — conclusion: emitter/source-local jitter contributes to a meaningful subset of timing/order findings, but recent hard ordering defects skew toward lifecycle/state/canonical tuple ownership; centralize source timing constraints around canonical event relationships rather than only shifting `SecurityEvent.timestamp`.
- [x] Implement source-aware timing planner on feature branch `codex/source-timing-planner`, starting with eCAR dependent timing, Zeek protocol/analyzer timing, Kerberos audit tuple/order, and source/sensor timing tests. Verified with full normal pytest, Ruff checks/format checks, config validation, regenerated iteration-test timing probes, and blind reviewer follow-up.
- [x] Expand workstation-normal activity config with curated application/process, command, web/DNS, and host-background packs in source defaults. Verified with `uv run eforge validate-config`, focused config/activity tests, smoke generation to `/private/tmp/eforge-config-expansion-smoke`, and spot-checks across process, DNS/proxy, bash/syslog, and Windows/Sysmon output.
- [ ] **P2** Design richer persona/host story packs, including industry-specific bundles, once the first broad workstation-normal expansion lands.

**Goal:** Fix all expert-identified issues that would cause an analyst to reject the data. Consolidated from 6 blind expert panel improvement loops (Threat Hunter, DFIR, Network Eng, Detection Eng) plus infrastructure issues. Work top to bottom.

### Security Fixes

- [x] Integrate final approved PR review outcomes into `dev` — accepted and adapted the remaining real fixes (#170, #171, #172, #188, #189, #191, #192, #194, #195, #196, #197, #212), then closed duplicate, superseded, or already-addressed PRs (#167, #168, #174, #190, #193, #213) with rationale.
- [x] Reworked remaining real-but-not-ready PR fixes #165, #169, #204, and #207 against current `dev` — landed sidecar symlink-safe writes, bounded bash template expansion, raw Zeek OCSP optional-field defaults, and generation-safe extra syslog weights with focused coverage.
- [x] Fix unbounded session offset allocation caches for far-future scenario times — replaced minute/four-hour catch-up caches with direct deterministic offsets and covered far-future allocation cases.
- [x] Harden Windows spool JSON encoding and flushing against scenario-triggered denial-of-service — replaced collision-prone datetime sentinels with typed spool field wrappers and kept final spooled rendering on SQLite-backed streaming fixup passes instead of materializing all rows.
- [x] Fix TCP DNS fallback packet overhead so TCP SERVFAIL accounting preserves TCP/IP header distributions.
- [x] Fix unbounded Sysmon parent process-create shift loop for cyclic raw ProcessGuid relationships with cycle detection and bounded parent-ordering passes.
- [x] Harden beacon URL host extraction against malformed bracketed authorities so invalid evaluator input cannot disable causality scoring.
- [x] Fix missing target PID skips so storyline ground truth does not claim create_remote_thread/process_access evidence when generation emits no evidence.
- [x] Fix Cisco ASA connection ID normalization to avoid repeated full-file rewrites during emitter barrier flushes by deferring whole-file normalization to final close.
- [x] Fix causal-ordering scoring so diagnostic sample caps do not hide additional violations — diagnostic failure samples remain capped at 10, but every non-allow_missing_prior inversion now contributes to causal-ordering totals; covered by a regression test with 20 inversions plus 5 valid pairs.
- [x] Fix stale storyline source PID handling so skipped create_remote_thread/process_access evidence is reflected in ground truth instead of silently claiming generated evidence.
- [x] Fix fallback DNS SERVFAIL Zeek packet accounting so rewritten UDP history, packet counts, and IP-byte totals remain consistent.
- [x] Restore bounded Windows emitter flushing by spooling deferred Windows event dictionaries to an on-disk chronological spool at buffer/barrier flush boundaries, keeping in-memory retention bounded while preserving final RecordID ordering.

### Skill Installer Agent Support

- [x] Fix Codex skill installer symlink clobber vulnerability by validating/writing nested destination files safely.
- [x] Fix RDP user coercion session/ground-truth desynchronization vulnerability and malformed fallback user crash — aligned preallocated RDP session usernames with coerced Windows credentials, records RDP ground truth from canonical session identity, and sanitizes fallback email domains.
- [x] **P1** SCP receiver source-port correlation — fixed Linux SCP storyline receiver-side sshd/file artifacts so target syslog messages reuse the same client source port as the generated SSH network flow evidence; added focused unit coverage.
- [x] Harden storyline command-line URL inference against malformed scenario-controlled URLs so invalid hosts/ports cannot crash generation.
- [x] Harden evaluator beacon destination matching to parse URL hosts exactly and avoid substring false positives in proxy/HTTP beacon evidence records.
- [x] Fix DHCP setup for storyline hosts so setup leases remain warm-up-only state and cannot emit visible out-of-window REQUEST/ACK records.
- [x] Fix eCAR process object ID lookup so missing processes do not fabricate orphan UUID references; missing lookups now return empty IDs, and remote-thread/process-access generation skips absent targets.
- [x] Add dual-agent `eforge install-skills` workflow — default to Claude project installs, add explicit `--agent claude|codex`, keep Claude project/global behavior, add Codex user-level `~/.codex/skills/` installs, reject invalid Codex/global combinations, and cover installer safety/stale-cleanup behavior with tests.
- [x] Reduce Codex skill reference duplication — bundle only the references each Codex skill needs and rely on installer stale cleanup to prune no-longer-needed reference files from prior installs.
- [x] Import existing Claude `/eforge assess` command as a Codex skill without modifying the source skill.
- [x] Fix official Codex skill installs so generated `SKILL.md` frontmatter is valid and user-managed `eforge-*` skills are preserved.
- [x] Create, validate, generate, evaluate, and blind-assess the iteration-test scenario from `scenarios/ITERATION-TEST-PROMPT.md`.
- [x] Run three additional iteration-test assessment loops, fixing prior-loop P0/P1 root causes, committing each pass, regenerating/evaluating as needed, and preserving blind-review artifacts.
- [x] **P0** Loop 3 blind-review follow-up / up-to-10 assessment loop — completed the requested up-to-10 loop run. Final loop eval passed at 94/100 and rendered probes found zero DNS response-accounting contradictions, zero Sysmon parent-create-after-visible-parent-termination cases, and zero eCAR post-termination references. Blind reviewers still scored synthetic across all four roles (avg synthetic confidence 79.5/100, avg realism 71/100). Top remaining root-cause finding: Sysmon Event 5 can carry an EventData `UtcTime` earlier than later Event 7 module-load telemetry for the same ProcessGuid, even when XML System `TimeCreated` ordering looks normalized. Next fixes should also address all-zero Sysmon `TerminalSessionId`, SYSTEM subject-domain rendering, IP CN-only public-CA X.509 records, DNS tunnel/web scan regularity, and source-specific collection imperfection profiles.
- [x] Harden web scan preset `max_effective_rate` validation to prevent overlay-driven generation crashes or hangs.
- [x] Aardvark beacon timing vulnerability fix — verified HEAD still routed generic beacons through DNS-tunnel pacing, restored exact periodic beacon timing, added regression coverage, and ran targeted tests plus Ruff checks.
- [x] **SUPERSEDED** **P0** Loop 10 continuation / second up-to-10 assessment run — superseded by later assessment loops through the mid/high 90s and the current Post-Loop 95 roadmap. The remaining active issues from this era are now tracked as narrower web/session, Zeek timing, eCAR variance, imperfect observation, and statistical-polish TODOs.
  Loop 20 fix pass: stabilized repeated Zeek certificate hashes, ordered Zeek certificate file rows after SSL analyzer rows, suppressed static ASA NAT xlate churn, ordered same-second systemd lifecycle syslog rows, and added SCP receiver-side Linux/file artifacts. Verified with full `uv run pytest -v`, Ruff, and `eforge validate-config`.
  Loop 21 fix pass: preserved caller-pinned successful TLS handshakes, carried explicit POST body sizes through proxy egress, emitted visible early DHCP renewals for storyline-DHCP hosts, aligned DNS FLOW PID inference with the DNS Client service, localized SCP receiver-side eCAR actor IDs, and normalized remote-thread target image paths. Local loop scenario NAT now includes the DMZ so proxy egress renders with mapped public sources. Verified with full `uv run pytest -v`, Ruff, and `eforge validate-config`.

### P1 Syslog BSD Timestamp Year Inference

- [x] **P1** Syslog year-bearing timestamp fix — generated Linux syslog now renders RFC 5424 with full ISO/RFC3339 timestamps and PRI/version/procid fields, removing the yearless BSD output path that caused future-date eval drift. `eforge eval` keeps parser-marked BSD/RFC3164 and legacy ISO input as compatibility fallbacks for older datasets, while strict validation requires RFC 5424 for unmarked/generated syslog. Verified with config validation, focused syslog/eval tests, Ruff, and full normal pytest.

### P0 Cross-Source Timing Audit

- [x] Security hardening: bounded `workstation_lock.min_unlock_gap_seconds` with schema upper-bound validation and runtime clamping to prevent `timedelta` overflow from malicious local overlays.
- [x] **P1** Security fix: prevent quadratic behavior in Linux `systemd-logind` session ID allocation for warm-up/pre-boot and same-second event bursts.
- [x] **P0** Comprehensive correlated-event timing audit — completed through the timing-audit loop and subsequent iteration-test loops. The concrete findings were fixed or split into narrower follow-ups; remaining lower-return work now lives in the post-timing statistical polish, well-synced Zeek sensor timing, web/session realism, source-observation profile, and process-lifecycle architecture TODOs.
- [x] **P0** Timing-audit baseline blind review follow-up — completed or superseded by the same-session lifecycle timestamp guard, process follow-on timestamp guard, source-native Windows auth timing profiles, cross-source network timing profiles, proxy domain-class/path profiles, ElevatedToken variance, anonymous logon source-field fixes, and DC/auth semantic fixes.
- [x] **P0** Same-session lifecycle ordering guard — fixed baseline scheduling so planned logoffs are known before user activity, user activity/lock-unlock events skip inactive sessions, activity updates session last-use time, and Windows Security rendering has a narrow 4634-after-dependent backstop. Generated-output probe on `/private/tmp/eforge-timing-loop3-output/data` found zero same-host/same-LogonID 4688/4801 events after visible 4634 logoff.
- [x] **P0** Process follow-on timestamp guard — fixed process-dependent generation for module loads, registry noise, process access, and remote-thread evidence to clamp after process start and carry process start metadata where needed; added a Sysmon render-time ProcessGuid ordering backstop. Generated-output probe on `/private/tmp/eforge-timing-loop3-output/data` found zero Sysmon follow-on records before their Event 1 for the same ProcessGuid.
- [x] **P1** Cross-source network timestamp profile — Windows Security 5156, Sysmon Event 3, and eCAR FLOW now use data-driven `source.*` timing profiles so host audit/EDR telemetry renders after the canonical wire event instead of tying Zeek conn timestamps exactly. Generated-output probe on `/private/tmp/eforge-timing-loop12-output/data` found 20,892 Zeek/eCAR common tuples with zero exact or millisecond timestamp matches.
- [x] **P2** Proxy domain-class path/content profile completion — inspected proxy GET rows can still pair vendor/update user agents and hosts with generic browser paths such as `/login` or `/favicon.ico`. Current domain-class path selection and non-browser site-map exclusions cover the generated timing scenario; probe on `/private/tmp/eforge-timing-loop17-output/data` found zero infra/update/cert proxy GET rows with browser-generic paths, favicons, CSS, or webp assets.
- [x] **P3** Time-window-aware blind-eval prompt/library — completed for the current workflow in the local `eforge-assess` skill briefing/prompt guidance. There is still no repo-side assessment helper to update, so this is no longer an open repository TODO.
- [x] **P0** Sysmon transitive parent-create ordering guard — `_shift_process_creates_after_visible_parent()` now iterates until stable so cascading parent shifts in multi-level ProcessGuid chains cannot leave a child Event 1 before its shifted visible parent Event 1; added focused unit coverage for three-level chains.
- [x] **P0** Follow-up timing blind review findings — completed by the Sysmon Event 5 lifecycle floor, SSH syslog lifecycle/source-port ordering fixes, and systemd-logind session ID ordering fixes.
- [x] **P1** Harden IDS DNS template validation/rendering against unsafe format fields — enforce that `dns_query_templates` only allow the `token` replacement field with sane format syntax/width and reject malformed or resource-exhausting templates in both config validation and runtime rendering.
- [x] **P0** Harden timing profile overlay parsing in generation path — enforced safe integer parsing and range clamps for `relationships.*` and `windows_event_time.collision_spacing` so malformed `.eforge/config/activity/timing_profiles.yaml` values cannot crash generation or produce pathological timing offsets; added focused unit coverage for invalid-type and extreme-value overlays.
- [x] **P0** Sysmon process-termination lifecycle guard — fixed Sysmon rendering so Event 5 process termination cannot appear before later visible telemetry for the same ProcessGuid; added focused unit coverage.
- [x] **P1** SSH syslog lifecycle ordering/profile — fixed SSH syslog compound events to use whole-second connection→accepted→pam→logind ordering and per-host monotonic small systemd-logind session IDs; added focused unit coverage.
- [x] Windows auth timing findings from blind review — fixed the confirmed Case A ordering bug where visible 4688 process-create records could precede a later visible same-host/same-LogonID 4624. The root causes were future-session identity selection, parent-chain process backdating before session start, and later 4624 rendering for already-registered sessions. Also fixed account-management SubjectLogonId selection to avoid future sessions, prevented desktop baseline process activity from reusing Type 3 network logons, stopped random 4672 assignment for ordinary users, and added explicit storyline process_access handling so credential-dump actions render Sysmon Event 10.
- [x] **P0** Post-logoff same-LogonID lifecycle contradictions — latest data-only blind review found visible 4634 logoff followed by later 4688 process activity and later Type 7 unlock/4624 for the same host + LogonID. Fixed through session activity/logoff ordering guards and verified against `/private/tmp/eforge-timing-loop12-output/data`: zero visible same-host/same-LogonID `4688`, Type 7 `4624`, or `4801` records after a visible `4634`.
- [x] **P1** Windows 4624 ElevatedToken realism — fixed 4624 rendering so `ElevatedToken` comes from `AuthContext.elevated` instead of the template default, and fixed anonymous logons so they use `NT AUTHORITY`, remote source IP/port/workstation metadata when available, and non-elevated token semantics. Added focused generator and emitter coverage.
- [x] **P1** Anonymous logon field realism — fixed anonymous Type 3 logons so they render `NT AUTHORITY`, remote source IP/port/workstation metadata when available, and non-elevated token semantics.
- [x] **P1** Security/Sysmon logoff source-offset margin — follow-up review of `/private/tmp/eforge-timing-loop6-output/data` found visible Security 4634 logoffs tens of milliseconds before later Sysmon Event 1 process creates for the same LogonID, caused by Security's render-time lifecycle guard ignoring Sysmon source-native collection offsets. Fixed the generator logoff margin after session activity and widened the Windows Security 4634 render-time guard to clear downstream endpoint source offsets. Generated-output probe on `/private/tmp/eforge-timing-loop9-output/data` found zero Sysmon Event 1 records after a same-session visible Security 4634.
- [x] **P0** IDS DNS alert/query contradiction — fixed Snort DNS alert/Zeek DNS payload disagreement by making DNS IDS signatures carry data-driven `dns_query_templates`, loading them with overlay support, and building a canonical `DnsContext` from the selected signature during IDS false-positive generation. Generated-output probe on `/private/tmp/eforge-timing-loop10-output/data` found 13 DNS IDS alerts and zero same-tuple Zeek query suffix mismatches.
- [x] **P0** Timestamp compression bursts — added overlay-aware `timing_profiles.yaml` for causal/source-latency/teardown timing and Windows/Sysmon tied-timestamp collision spacing. Causal DNS/Kerberos/remote-thread/audit offsets and logoff margins now use the profile, and Windows/Sysmon render-time normalization keeps small tied clusters near-zero while spreading large tied clusters across seconds. Generated-output probe on `/private/tmp/eforge-timing-loop11-output/data` found worst 1ms windows of 7 Security events and 4 Sysmon events, down from earlier 174/106 event spikes.
- [x] **P2** ASA static NAT teardown cadence — resolved: static NAT mappings no longer emit per-flow 305011/305012 xlate churn, dynamic NAT teardown uses connection duration and out-of-window suppression, and ASA regression coverage documents the behavior.
- [x] **SUPERSEDED** **P2** Deterministic cross-source offset fingerprints — superseded by source timing profiles and the narrower well-synced Zeek sensor timing TODO. Any remaining source-offset work should be handled there rather than as a duplicate broad item.
- [x] **P0** Blind-review time-window context — every blind reviewer prompt should explicitly state that the dataset is an extract for a bounded collection window, so initiating events that occurred before the window can still have in-window echoes. Acceptable: processes, sessions, connections, leases, or logoffs whose creation/start event predates the extract and is therefore absent. Error: a visible initiating event for the same identifier appears later than its dependent event, such as a 4688 before a later same-host 4624 with the same LogonID. Added this guidance to `/eforge evaluate` for blind qualitative reviews and used it in the current blind-eval prompt.
- [x] **P0** Source-native timestamp precision/rendering profiles — include rendered precision and per-source formatting in the timing audit. Known example: Windows Security XML now renders EVTX-like 100ns precision, but a blind review caught that the 7th fractional digit was previously always `0`. Audited the current renderers: Windows Security/Sysmon share EVTX-like 100ns formatting with deterministic 7th-digit variation, Zeek renders microsecond epoch seconds, eCAR renders integer milliseconds, and proxy/web/ASA/syslog render source-native second precision. Generated-output probe on `/private/tmp/eforge-timing-loop12-output/data` found 17,698 Windows Security timestamps with 7th-fractional-digit coverage across all digits 0-9.
- [x] **P0** Windows auth/network timing examples to include in the audit — completed as part of the Windows auth, DNS, proxy, ASA, and cross-source timing audit fixes. Remaining sensor calibration is tracked separately under the well-synced Zeek timing TODO.
- [x] **P0** Loop 12 blind-review timing findings — data-only review of `/private/tmp/eforge-timing-loop12-output/data` scored **92% synthetic**. Fixed the critical Sysmon parent GUID ordering issue by preserving parent start time on `ProcessContext`, filtering future-started parents during parent resolution, and normalizing visible Sysmon parent/child Event 1 ordering. Fixed the high Zeek TLS lifetime issue by data-driving Zeek SSL/X.509 analyzer offsets, enforcing a completed-TLS conn duration floor, and suppressing ssl/x509 analyzer rows for partial TCP handshakes. Generated-output probe on `/private/tmp/eforge-timing-loop17-output/data` found zero Sysmon parent-after-child cases, zero Zeek ssl/x509 rows after conn end, and zero ssl/x509 rows on non-`SF` connections. Low follow-up remains: web access logs can rotate bot/script/browser User-Agents rapidly from the same public IP; track as web-client identity/session realism polish after P0/P1 timing fixes.
- [x] **P1** Loop 17 blind-review follow-up — data-only review of `/private/tmp/eforge-timing-loop17-output/data` scored **87% synthetic**. Fixed actionable findings: Linux `systemd-logind` session IDs/PIDs now come from shared state and timestamp-derived per-host sequencing; ASA connection IDs no longer collide across adjacent-second high-volume bursts; generated SSH session `conn.json` rows include `orig_ip_bytes`/`resp_ip_bytes`; RSA-named TLS issuers no longer choose ECDSA certificate metadata and `eforge validate-config` rejects that combination. Prompt-calibration finding: Type 7 unlock 4624 events are not original session initiators, so reviewers should not treat pre-unlock process activity with the same LogonID as impossible when the original Type 2/10 session began before the extract. Generated-output probe on `/private/tmp/eforge-timing-loop19-output/data` found zero logind PID/order issues, zero ASA concurrent connection-ID reuse, zero SSH rows missing IP byte counters, zero RSA/ECDSA x509 mismatches, and zero process-before-later-non-Type7-4624 cases.
- [x] **P1** Loop 19 blind-review follow-up — data-only review of `/private/tmp/eforge-timing-loop19-output/data` scored **92% synthetic**. Fixed the critical SSH disconnect timing issue by recording SSH transport close time on session state, making logoff/disconnect evidence wait for the latest session end marker, and reusing the same baseline SSH duration for conn.log and syslog disconnect timing. Fixed DNS TXT conn accounting so response-bearing TXT rows retain originator query payload. Fixed denied explicit-proxy CONNECT accounting so proxy-access rows use proxy denial byte/time scale rather than inherited tunnel byte counts. Quick tests, ruff, `eforge validate-config`, and generated-output probes on `/private/tmp/eforge-timing-loop22-output/data` passed for DNS TXT and denied CONNECT, and found zero matching SSH disconnect-before-conn-close cases.
- [x] **P1** Loop 22 blind-review follow-up — data-only review of `/private/tmp/eforge-timing-loop22-output/data` scored **82% synthetic**. Fixed the critical eCAR MODULE/LOAD after PROCESS/TERMINATE issue by tracking process last-activity time in state, marking image-load activity, and delaying stale-process termination after process-dependent telemetry. Fixed the high SSH close mismatch by recording SSH transport close time even for preallocated sessions, suppressing stale tuple-bearing disconnect syslog in favor of generic PAM session-close messages, avoiding standalone disconnect tuples in baseline SSH noise, and treating self-sourced SSH cleanup as generic. Fixed the medium DNS tunnel RTT/duration issue with overlay-aware `dns_tunnel_rtt` config, `eforge validate-config` schema coverage, and DNS conn duration propagation from `DnsContext.rtt`. Generated-output probe on `/private/tmp/eforge-timing-loop29-output/data` found zero eCAR module-after-terminate cases, zero SSH disconnect/Zeek close contradictions, and zero DNS tunnel RTT/duration issues.
- [x] **P1** Loop 23 blind-review follow-up — data-only review of `/private/tmp/eforge-timing-loop29-output/data` scored **82% synthetic**. Fixed the critical SSH same-PID close mismatch by suppressing Linux `sshd` syslog close evidence when the backing SSH transport is stale or self-sourced, leaving bounded-window extracts with unmatched visible opens rather than impossible visible closes. Tightened the data-driven DNS tunnel RTT default from `0.04-1.5s` to `0.04-0.35s` while preserving overlay support and `eforge validate-config` range validation. Generated-output probe on `/private/tmp/eforge-timing-loop30-output/data` found zero SSH same-PID close/Zeek contradictions, zero tuple-bearing disconnect lines, and zero DNS tunnel RTTs above 1s.
- [x] **P1** Loop 24 blind-review follow-up — data-only review of `/private/tmp/eforge-timing-loop30-output/data` scored **85% synthetic**. Fixed the critical eCAR FLOW after visible PROCESS/TERMINATE issue by making connection generation update the owning process last-activity marker, dropping stale non-system PID attribution when the process is no longer running, and protecting Windows PID 4/System in the seeded process map. Fixed the medium Zeek identical-timestamp burst with data-driven `source.zeek_conn_start` jitter in `timing_profiles.yaml`. Generated-output probe on `/private/tmp/eforge-timing-loop31-output/data` found zero eCAR FLOW-after-visible-terminate cases and reduced exact Zeek conn timestamp bursts to a max of 2 rows.
- [x] **P2** Security hardening: validate `dns_tunnel_rtt` overlay shape/range at load/runtime boundary so malformed overlay values cannot crash generation.
- [x] **P2** Post-timing-audit statistical polish — fixed repeated generator-owned Zeek duration constants by jittering default-derived connection durations while preserving caller-authored values and DNS RTT locks. Added data-driven `auth_noise.yaml` scheduling for stale credential noise, replaced rigid modulo cadence with deterministic irregular intervals/skips/backoff, expanded service-account defaults, and made remote Linux failed-password syslog rows emit matching Zeek SSH tuples with the same source port. Verification passed: `eforge validate-config`, targeted unit coverage, Ruff check/format check, full normal pytest, and full slow-inclusive `uv run pytest -v --include-slow` (`3033 passed, 1 skipped`).
- [x] **P1** Explicit proxy origin-error egress regression — in explicit forward-proxy mode, preserve proxy→origin egress emission for canonical origin HTTP 4xx/5xx responses (`cache_result=MISS`) and only short-circuit egress for proxy-generated deny/auth/gateway failure outcomes.
- [x] Loop 26 Host/EDR blind authenticity report — analyzed only `scenarios/iteration-test/data` for endpoint evidence realism and saved the persona report under `scenarios/iteration-test/blind-test/loop-26/host-forensics-report.md`.
- [x] Loop 30 iteration-test assessment continuation — fixed observation-anchored X.509 validity windows, Linux eCAR parent PID semantics, short-command process lifetimes, Linux-native eCAR failed-logon fields, Linux parent anchors for minimal state, and nmap probe side-effect leakage. Verification passed: focused tests, full normal `uv run pytest -v`, Ruff, `eforge validate-config`, regeneration, quantitative eval, and blind review. Blind scores: Threat Hunter 82, Detection 78, Network 78, Host/EDR 74 synthetic confidence.
- [x] Loop 31 iteration-test assessment continuation — fixed loop-30 P1 root causes by rendering Zeek x509 fingerprints as SHA1-style values with stock-shaped stable FUIDs, splitting proxy UA pools by OS/source role/destination, jittering simple bash-history dwell intervals, adding PE metadata for Microsoft admin binaries, removing WDigest from ambient HKLM noise, and deriving suspicious benign internal domains from the scenario AD domain. Verification passed: focused tests, full normal `uv run pytest -v`, Ruff, and `eforge validate-config`.
- [x] Loop 32 iteration-test assessment continuation — fixed loop-31 high-impact findings at their owning layers: Zeek X.509/file SHA1 identity consistency, Windows 1102 subject visibility, one-shot CLI lifetimes, and stable static web response sizing. Verification passed: focused probes found zero X.509/file SHA1 mismatches, zero varied static-resource size groups, visible 1102 subject context, and no long `sqlcmd -Q` lifetimes; quantitative eval passed at 93/100. Blind scores: Threat Hunter 78, Detection 68, Network 78, Host/EDR 78 synthetic confidence.
- [x] Loop 33 iteration-test assessment continuation — fixed loop-32 high-impact root causes: reserved storyline-created service account names from pre-creation baseline explicit-credential noise, constrained baseline IDS false positives to signatures whose protocol artifacts can be represented canonically, made Security 1102 the first post-clear record ID, and prevented duplicate command-owned eCAR file-create side effects. Verification passed: focused probes found zero pre-creation `svc_sqlreader` 4648s, zero disallowed baseline IDS signatures, clean 1102 post-clear record ordering, and zero duplicate eCAR file-create side effects. Quantitative eval passed at 94/100. Blind scores: Threat Hunter 62 synthetic confidence, Detection 78 synthetic confidence, Network 72 real confidence, Host/EDR 86 synthetic confidence.
- [x] Loop 34 iteration-test assessment continuation — fixed loop-33 root-cause findings: DHCP DORA source-native tuple semantics and lease identity reuse, explicit hostname DNS/SNI/destination consistency for unregistered domains, and Linux `systemd-logind` session IDs that leaked elapsed seconds. Follow-up probes found DHCP MAC reuse, DNS prerequisite gaps, and explicit-proxy egress DNS/SSL mismatches still present, so the root-cause repair also makes setup leases honor storyline-pinned host MACs, forces address-record DNS prerequisites for generated TCP connections, and resolves proxy egress destinations from the proxy host perspective. Verification passed: focused tests, full normal `uv run pytest -v`, Ruff, and `eforge validate-config`; loop-34 quantitative eval passed at 94/100. Blind scores: Threat Hunter 72 synthetic confidence, Detection 80 synthetic confidence, Network 68 synthetic confidence, Host/EDR 78 synthetic confidence.
- [x] Loop 35 iteration-test assessment continuation — fixed loop-34 root-cause findings: Sysmon Event 1 `CurrentDirectory` now comes from process context instead of universally defaulting to `C:\Windows\system32\`, local Linux failed logons no longer render impossible `sshd ... from - port` messages, duplicate workstation locks are guarded until unlock, and PsExec-style follow-on commands parent under a live remote-execution wrapper instead of flattening under `services.exe`. Verification passed: focused tests, full normal `uv run pytest -v`, Ruff, regeneration, quantitative eval at 94/100, and blind review. Blind scores: Threat Hunter 72 synthetic confidence, Detection 70 synthetic confidence, Network 72 synthetic confidence, Host/EDR 76 synthetic confidence.
- [x] Loop 36 iteration-test assessment continuation — fixed loop-35 root-cause findings: generated scheduled-task 4698 `TaskContent` now carries full Task Scheduler XML, ambient Sysmon registry value writes render as Event 13 `SetValue` instead of Event 12 `DeleteKey`, and Microsoft OS binary file-version metadata is normalized per host OS so workstation/server build stamps do not mix on one host. Verification passed: focused tests, full normal `uv run pytest -v`, Ruff, regeneration, quantitative eval at 94/100, and blind review. Blind scores: Threat Hunter 78 synthetic confidence, Detection 72 synthetic confidence, Network 64 real confidence, Host/EDR 58 synthetic confidence; average synthetic confidence 61.0.
- [x] Loop 37 iteration-test assessment continuation — fixed loop-36 root-cause findings: Windows Security 5156 no longer falls back to `ProcessID=4`/`Application=System` for ordinary flows with unknown process ownership, explicit successful TCP connections now choose varied Zeek `SF` histories instead of collapsing to `ShADadfF`, and web-scan IDS rate alerts now use a variable seeded gate instead of a fixed near-one-minute cadence. Verification passed: focused tests, full normal `uv run pytest -v`, Ruff, regeneration, quantitative eval at 93.6/100, and blind review. Blind scores: Threat Hunter 72, Detection 72, Network 62, Host/EDR 78 synthetic confidence; average synthetic confidence 71.0. No early exit: the score worsened because reviewers surfaced deeper fixable issues, including one concrete P0 process-lineage bug, not because the last fix set made realism fundamentally worse.
- [x] Loop 38 iteration-test assessment continuation — implemented root-cause fixes for the loop-37 P0/P1 findings before regeneration: Windows `explorer.exe` parent selection is anchored to the logon chain instead of browser/app history; extra syslog program entries now have validated selection weights and the suspicious web/proxy sudo-denial pool is rare and varied; Windows 4672 emission rates are data-driven per account class instead of always-on for every eligible service/machine/admin logon; package/update proxy domains are non-browser and path/content aware; and ASA ICMP faddr/gaddr/laddr rendering now follows inbound/outbound address roles. Verification passed: `eforge validate-config`, focused tests, full normal `uv run pytest -v` (2770 passed, 37 skipped), Ruff check, Ruff format check, regeneration, quantitative eval at 94/100, and blind review. Blind scores: Threat Hunter 76, Detection 78, Network 74, Host/EDR 72 synthetic confidence; average synthetic confidence 75.0. No early exit: reviewers converged on deeper fixable regularity issues rather than evidence that the loop-38 fixes made realism worse.
- [x] Loop 39 iteration-test assessment continuation — implemented root-cause fixes for high-agreement loop-38 P1/P2 findings before regeneration: multi-sensor Zeek rendering now applies per-sensor observation variance for timings, durations, byte/packet counters, and HTTP body lengths instead of cloning rows across sensors; bash history treats `shred -u .bash_history` as destructive; Linux SSH/syslog baseline is server-scoped, rarer, and scenario-roster based instead of generic `root/admin/ubuntu` churn everywhere; extra sudo noise is lower-weight and no longer includes repeated root-to-root apt update; Sysmon registry Events 12/13 render `User`; ambient registry noise is lower volume and prefers dynamic key pools instead of repeatedly touching static Office/Winlogon/EventLog keys; and web-scan path selection now shuffles/skips between passes with a lower Nikto rate cap. Verification passed: `eforge validate-config`, focused tests, full normal `uv run pytest -v` (2777 passed, 37 skipped), Ruff check, Ruff format check, regeneration, quantitative eval at 94/100, and blind review. Blind scores: Threat Hunter 78, Detection 74, Network 88, Host/EDR 76 synthetic confidence; average synthetic confidence 79.0. No early exit: the score spike was caused by a concrete new P0 in the loop-39 Zeek observation-variance layer, not by plateau or a broad realism regression.
- [x] Loop 40 iteration-test assessment continuation — implemented root-cause fixes for the loop-39 P0/P1/P2 findings before regeneration: Zeek multi-sensor observation variance now preserves `*_ip_bytes >= *_bytes + packet overhead` invariants; ICMP Zeek conn rows render source-native type/code ports and echo request/reply history; Zeek files.log transfer rows are delayed after their referenced connection start and share the referenced connection UID as the multi-sensor timing basis; Office reading-location registry `Datetime` values are materialized before the Event 13 timestamp; eCAR failed logons carry an `attempt_failed` session lifecycle qualifier; Sysmon ProcessGuid timestamp segments no longer expose tiny boot-relative counters; and the data-driven dsquery command pool now varies query targets and limits. Verification passed: `eforge validate-config`, focused regression tests, full normal `uv run pytest -v` (2781 passed, 37 skipped), Ruff check, Ruff format check, regeneration, quantitative eval at 93/100, and blind review. Blind scores: Threat Hunter 44, Detection Engineer 45, Network Forensics 42, Host/EDR 63 synthetic confidence; average synthetic confidence 48.5. Early exit triggered because average synthetic confidence is <=60. New prioritized findings: P1 DHCP lease/syslog timing and DNS/DHCP state conflicts; P1 source-native Windows 4625 subject semantics; P1 high-volume benign Sysmon Event 8 remote-thread noise; P1 eCAR parent/child process ordering; P1 proxy HTTPS app-log semantics exceeding Zeek tunnel visibility; P1 OS/user-agent drift for Linux hosts; P2 eCAR processless flow attribution; P2 same-interface ASA denies on perimeter firewall; P2 Sysmon ImageLoad metadata blanks; P3 Linux SSH session duplication/orphaning; P3 homogeneous TLS scanner cadence; P4 HTTP file metadata inferred from extension on redirects.
- [x] **SUPERSEDED** Loop 41-60 iteration-test assessment continuation — superseded by later completed assessment loops through the mid/high 90s. The durable work from this continuation is represented by the remaining statistical polish, source-observation, web/session, Zeek timing, and eCAR variance TODOs.
- [x] Loop 42 realism fixes — aligned Kerberos PKINIT issuer names with scenario AD org, restricted DHCP baseline leases to DHCP-like client systems instead of static infrastructure, made Linux systemd-logind session IDs monotonic under out-of-order generation, and populated eCAR thread IDs where endpoint telemetry can derive a source thread. Verification passed: focused tests, full normal `uv run pytest -v`, Ruff, `eforge validate-config`, regeneration, quantitative eval at 93.4/100, and blind review. Blind scores: Threat Hunter 74 real confidence, Detection Engineer 78 synthetic confidence, Network Forensics 82 synthetic confidence, Host/EDR 72 synthetic confidence; average synthetic-equivalent confidence 64.5.
- [x] Loop 43 realism fixes — addressed loop-42 concrete root causes: preserved Zeek cleartext HTTP body facts across multi-sensor observations and clamped conn byte counters to protocol body floors, enriched Zeek DHCP source-native lease fields, kept OCSP responder choice stable for a certificate identity, rendered scheduled-task SYSTEM principals as local authority instead of AD-domain users, and derived Sysmon hashes from host OS build when OS binary metadata differs. Verification passed: focused tests, `eforge validate-config`, full normal `uv run pytest -v`, Ruff check, Ruff format check, regeneration, quantitative eval at 93.3/100, and blind review. Blind scores: Threat Hunter 78, Detection Engineer 64, Network Forensics 76, Host/EDR 78 synthetic confidence; average synthetic confidence 74.0.
- [x] Loop 44 realism fixes — addressed loop-43 concrete root causes: kept Windows Security EventRecordID order monotonic with rendered TimeCreated values, made scheduled-task SYSTEM XML use service-account semantics, bounded short-lived Windows utility lifetimes for gpresult/gpupdate/dsquery-like commands, dropped stale one-shot utility PID attribution for later network flows, and preserved Zeek same-flow payload bytes across multi-sensor observations while still varying source-native packet/IP counters. Verification passed: focused tests, `eforge validate-config`, full normal `uv run pytest -v`, Ruff check, Ruff format check, regeneration, quantitative eval at 94.2/100, generated-output probes, and blind review. Blind scores: Threat Hunter 72, Detection Engineer 86, Network Forensics 76, Host/EDR 82 synthetic confidence; average synthetic confidence 79.0. The higher confidence appears to reflect deeper concrete issues surfaced after the prior obvious tells were removed, not an early-exit regression.
- [x] Loop 45 realism fixes — addressed loop-44 concrete root causes: preserved Sysmon ProcessGuid across system-process create/terminate pairs, enforced visible Windows Security parent-process creation before child 4688 records, corrected workstation/DC source perspective for Kerberos/logon loopback addresses, added realistic source-native delay for Sysmon DNS relative to Zeek DNS, and replaced per-event positive Zeek multi-sensor timestamp jitter with stable per-sensor clock skew plus small propagation delay. Verification passed: focused tests, `eforge validate-config`, full normal `uv run pytest -v`, Ruff check, Ruff format check, regeneration, quantitative eval at 93.2/100, generated-output probes, and blind review. Blind scores: Threat Hunter 74, Detection Engineer 72, Network Forensics 74, Host/EDR 78 synthetic confidence; average synthetic confidence 74.5. Remaining high-signal root causes are eCAR parent-chain process objectID drift, rogue Linux proxy/client identity mismatch, Zeek analyzer timing boundedness, DNS answer-template regularity, and Zeek `conn.json` site-extension field realism.
- [x] Loop 46 realism fixes — addressed loop-45 concrete root causes. eCAR PROCESS/CREATE now carries StateManager objectID/actorID for auto-created parent-chain processes so later PROCESS/TERMINATE reuses the same durable object; proxy generation infers source host context from `src_ip` before selecting/normalizing User-Agent strings, preventing Linux hosts from rendering Windows browser identity when callers omit `source_system`; Zeek DNS timestamps now use data-driven source-native analyzer offsets, and Zeek HTTP/SSL analyzer timing windows were widened in `timing_profiles.yaml`. Verification passed: focused tests, `uv run eforge validate-config`, `uv run pytest -v`, `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 46 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved loop-46 verbose/JSON eval artifacts, and completed the four bounded-window blind reviews. Automated eval was 93.01 overall across 52,932 parsed records; rendered-output probes showed zero eCAR process object drift, zero rogue Linux Windows/Edge proxy UAs, and zero exact Zeek DNS/HTTP/SSL analyzer timestamp matches. Blind scores: Threat Hunter synthetic 68, Detection Engineer real 62, Network synthetic 82, Host/EDR synthetic 67. New actionable root cause: Zeek DNS analyzer timestamps were jittered after very short matching conn lifetimes; 1102 empty-EventData finding was verified false-positive because source-native 1102 uses `UserData/LogFileCleared`.
- [x] Loop 47 realism fixes — fixed loop-46 concrete findings by keeping Zeek DNS analyzer timestamps inside the same-UID conn lifetime while preserving non-exact source-native timing, and normalized Sysmon `RuleName` default rendering across event types. Verification passed: focused tests, `uv run eforge validate-config`, `uv run pytest -v`, `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 47 regenerated assessment — regenerated `scenarios/iteration-test/data` and saved loop-47 verbose/JSON eval artifacts. Automated eval stayed flat at 93.01 overall across 52,932 parsed records; rendered-output probes show zero same-UID DNS records after conn end, zero exact DNS/conn timestamp matches, and zero empty Sysmon `RuleName` fields. Blind scores: Threat Hunter synthetic 64, Detection Engineer synthetic 78, Network Forensics synthetic 68, Host/EDR synthetic 72. Top loop-48 root-cause targets are rejected inbound eCAR flow attribution to server listener PIDs, Linux bash-history command timestamps lagging correlated eCAR process events, and cross-sensor observation regularity.
- [x] Loop 48 realism fixes — fixed loop-47 source-native contradictions by preventing rejected/SYN-only inbound eCAR FLOW attempts from claiming destination listener PIDs and by making Linux process activities use the shell scheduler's command time for both bash history and the correlated process/network/termination artifacts. Verification passed: focused tests, full normal `uv run pytest -v` (`2807 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 48 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts and rendered-output probes under `blind-test/loop-48`, and completed the four bounded-window blind reviews. Automated eval stayed flat at 93.01 across 52,932 parsed records; rendered probes showed zero failed inbound eCAR flows with listener PIDs and zero matched bash-history entries lagging process-create rows. Blind scores: Threat Hunter synthetic 78, Detection Engineer synthetic 78, Network Forensics synthetic 82, Host/EDR synthetic 78; average synthetic confidence 79.0. The highest-impact non-deferred root cause is overly uniform cross-sensor Zeek timing; source-coverage-perfection findings remain deferred to the collection-imperfection/profile work.
- [x] Loop 49 realism fixes — fixed concrete loop-48 timing regularity by making Zeek multi-sensor observations use stable per-sensor clock skew plus per-flow capture-timing variance instead of the prior narrow fixed core/DMZ offset band. Verification passed: focused Zeek multiplex/fanout tests, full normal `uv run pytest -v` (`2808 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 49 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts, rendered-output probes, and the four bounded-window blind-review reports under `blind-test/loop-49`. Automated eval stayed flat at 93.01 across 52,932 parsed records; rendered probes showed core/DMZ Zeek conn offsets now span about 1.10s with every matched offset unique. Blind scores: Threat Hunter synthetic 78, Detection Engineer synthetic 72, Network Forensics synthetic 67, Host/EDR synthetic 71; average synthetic confidence 72.0. The prior cross-sensor timing tell is fixed; top non-deferred loop-50 targets are self-originating Linux SSH failure telemetry, Defender platform path/version inconsistency, and DNS tunnel regularity/thematic infrastructure.
- [x] Loop 50 realism fixes — fixed concrete loop-49 root causes by rendering self-sourced Linux failed logons as local auth failures instead of sshd-from-self and normalizing Windows Defender Platform paths to one stable host-specific version across seeded processes, module loads, process access, remote-thread contexts, and EDR side-effect pools. Treat the 1102 empty-EventData claim as a false positive because source-native 1102 uses `UserData/LogFileCleared`. Verification passed: focused tests, `uv run eforge validate-config`, full normal `uv run pytest -v` (`2811 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 50 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts, rendered-output probes, and the four bounded-window blind-review reports under `blind-test/loop-50`. Automated eval improved to 94.19 across 49,436 parsed records; probes found zero self-sourced rogue SSH failures, a local PAM-style auth failure replacement, and zero Defender Platform version inconsistencies. Blind scores: Threat Hunter synthetic 72, Detection Engineer synthetic 78, Network Forensics synthetic 78, Host/EDR synthetic 72; average synthetic confidence 75.0. Top loop-51 root-cause targets are Zeek `files.local_orig` direction semantics, DNS tunnel byte/packet accounting, DC self-sourced RDP, and DHCP lease timing consistency.
- [x] Loop 51 realism fixes — fixed concrete loop-50 root causes at their owning layers: Zeek `files.local_orig` now follows the transmitting host after sensor projection, DNS response byte/packet accounting is derived from visible answer size, RDP generation avoids self-sourced target logons by selecting a real remote workstation, dhclient syslog now comes from the same canonical lease transaction as Zeek DHCP, and same-second syslog sorting preserves dhclient REQUEST → ACK → bound order after traditional timestamp precision is rendered. Verification passed: focused regression tests, `uv run eforge validate-config`, full normal `uv run pytest -v` (`2816 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 51 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts, rendered-output probes, and four bounded-window blind-review reports under `blind-test/loop-51`. Automated eval was 93.48 across 55,108 parsed records; probes verified the loop-51 fixes with zero `files.local_orig` contradictions, zero oversized DNS tunnel responses, zero DC self-sourced RDP logons, and zero dhclient order issues. Blind scores: Threat Hunter synthetic 78, Detection Engineer synthetic 82, Network Forensics synthetic 82, Host/EDR synthetic 78; average synthetic confidence 80.0. Top loop-52 root-cause targets are Sysmon Event 10 process-user drift, remote-thread address/stack semantics, ICMP echo accounting/duration, Windows DHCP interface GUID persistence, and web-shell trigger/noninteractive bash-history semantics.
- [x] Loop 52 realism fixes — fixed concrete loop-51 root causes at the owning generation/data layers: credential-dump processes now carry high-integrity token evidence, remote-thread start addresses and eCAR stack bases use plausible user-mode ranges, process-access target users come from canonical target process ownership, ICMP echo request/reply accounting uses matched payload sizes with subsecond durations, Windows DHCP registry side effects reuse stable host/IP interface GUIDs, and noninteractive web-shell side effects avoid `.bash_history` artifacts. Verification passed: full normal `uv run pytest -v` (`2821 passed, 37 skipped`), focused regression tests, `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 52 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts, rendered-output probes, and four bounded-window blind-review reports under `blind-test/loop-52`. Automated eval was 93.33 across 52,576 parsed records; probes verified the loop-52 fixes with zero Sysmon Event 10 user conflicts, zero remote-thread address/stack violations, zero ICMP echo semantic violations, zero DHCP interface GUID violations, and zero web-service `.bash_history` artifacts. Blind scores: Threat Hunter synthetic 68, Detection Engineer synthetic 58, Network Forensics synthetic 74, Host/EDR synthetic 43; average synthetic confidence 60.75. Top loop-53 root-cause targets are DNS tunnel distribution, C2/proxy envelope realism, ICMP distribution diversity, eCAR flow/process attribution, and Linux eCAR process coverage relative to bash history.
- [x] Loop 53 realism fixes — fixed highest-impact loop-52 root causes at their owning layers: DNS tunnel response-code mix and answer tokens are now data-driven/overlay-aware with `validate-config` coverage, storyline C2/proxy HTTP contexts use non-browser-like MIME/referrer/body variation, ICMP echo payload/duration generation avoids clamp-shaped plateaus while preserving request/reply symmetry, direct Linux bash-history commands can emit correlated eCAR/process lifecycle telemetry from active session state, and generic HTTP/HTTPS connection PID inference has Linux/Windows process fallbacks. Verification passed: focused tests, `uv run eforge validate-config`, full normal `uv run pytest -v` (`2825 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 53 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts, rendered-output probes, and four bounded-window blind-review reports under `blind-test/loop-53`. Automated eval improved to 93.85 across 49,088 parsed records; probes verified DNS tunnel response-code/answer consistency, ICMP variation, C2/proxy envelope diversity, and HTTP PID attribution fixes. Blind scores: Threat Hunter synthetic 82, Detection Engineer synthetic 68, Network Forensics synthetic 78, Host/EDR synthetic 88; average synthetic confidence 79.0. Top loop-54 root-cause targets are Linux shell-command process telemetry for typos/aliases and duplicate storyline process creates, Zeek multi-sensor packet accounting divergence, large-exfil size/duration regularity, residual DNS tunnel label repetition, and over-modeled journald space messages.
- [x] Loop 54 realism fixes — fixed the highest-impact loop-53 Linux shell/process root causes at the owning generation layer: bash-history process telemetry now only promotes recognized executables, expands common aliases to the real executable, keeps interpreters as the image for script invocations, can be disabled by typed storyline process events so one storyline command does not create duplicate Linux eCAR process telemetry, and Linux temp-directory script noise now uses interpreter images instead of script paths. Verification passed: focused bash/temp-dir regression tests, full normal `uv run pytest -v` (`2829 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 54 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts, rendered-output probes, and four bounded-window blind-review reports under `blind-test/loop-54`. Automated eval was 94.12 across 49,553 parsed records; probes verified zero bad shell/script process images and zero high-signal duplicate storyline Linux command process creates. Blind scores: Threat Hunter synthetic 48, Detection Engineer synthetic 62, Network Forensics synthetic 62, Host/EDR synthetic 78; average synthetic confidence 62.5, average realism/usefulness 77.5. Top loop-55 root-cause targets are Zeek TCP IP-byte invariants, invalid 4771 missing-source IP rendering, invalid NetworkManager same-state transitions, and 1102 SYSTEM logon ID semantics.
- [x] Loop 55 realism fixes — fixed concrete loop-54 P1 root causes at their owning layers: Zeek TCP `*_ip_bytes` now include protocol-aware L3/L4 header minimums, Kerberos preauth failures preserve missing source IP as `-` instead of rendering invalid IPv6-mapped text, Security 1102 system clears use canonical well-known logon IDs, and NetworkManager same-state transitions were removed from data config with `validate-config` coverage. Verification passed: focused regression tests, `uv run eforge validate-config`, full normal `uv run pytest -v` (`2832 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 55 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts, rendered-output probes, and four bounded-window blind-review reports under `blind-test/loop-55`. Automated eval was 92.77 across 50,144 parsed records; probes verified zero Zeek TCP IP-byte violations, zero invalid Kerberos missing-IP renderings, zero bad 1102 SYSTEM logon IDs, and zero NetworkManager same-state transitions. Blind scores: Threat Hunter synthetic 42, Detection Engineer synthetic 58, Network Forensics synthetic 62, Host/EDR synthetic 62; average synthetic confidence 56.0, average realism/usefulness 81.25. Per the revised loop criteria, low synthetic confidence is not an early exit. Top loop-56 root-cause targets are Sysmon dependent-event ordering, Windows lock/unlock after visible logoff, ASA deny vs Zeek DNS response suppression, canonical web exploit linkage, SYSTEM process token semantics, duplicate Zeek sensor packetization, and dynamic C2 proxy cache actions.
- [x] Loop 56 realism fixes — fixed loop-55 P0/P1 root causes at their owning layers: causal expansion now renders LSASS process access before remote-thread creation, workstation lock/unlock suppresses ended-session reuse, firewall-denied DNS no longer fabricates Zeek DNS responses, Apache raw syslog normalizes embedded request timestamps/client source ports from canonical context, Windows SYSTEM processes use well-known token logon IDs, duplicate lossless Zeek sensor views preserve packetization, and dynamic/C2 proxy requests cannot be cache hits. Verification passed: focused regression tests, `uv run eforge validate-config`, full normal `uv run pytest -v` (`2835 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 56 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts, rendered-output probes, and four bounded-window blind-review reports under `blind-test/loop-56`. Automated eval was 93.88 across 51,728 parsed records; probes verified zero ended-session unlock reuse, zero SYSTEM process LogonID violations, zero denied DNS responses, zero C2 proxy cache hits, and zero lossless Zeek packetization violations. Blind scores: Threat Hunter synthetic 72, Detection Engineer synthetic 74, Network Forensics synthetic 82, Host/EDR synthetic 92; average synthetic confidence 80.0. Top loop-57 root-cause targets are post-logoff process creation with the same LogonID, TLS certificate files exceeding conn responder bytes, fleet-inconsistent executable hashes for identical rendered metadata, and exfil path/process attribution realism.
- [x] Loop 57 realism fixes — fixed hard loop-56 source-native contradictions at their owning layers: late same-session process creation is now clamped before a visible logoff using a data-driven timing profile, TLS certificate byte accounting is shared between Zeek files rendering and canonical connection payload budgeting, and Sysmon hashes now derive from rendered binary identity so identical `Image/FileVersion/OriginalFileName` tuples stay stable across hosts. Verification passed: focused regression tests, `uv run eforge validate-config`, related unit files (`225 passed`), full normal `uv run pytest -v` (`2838 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 57 regenerated assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts and rendered-output probes under `blind-test/loop-57`, and ran four bounded-window blind reviews. Automated eval was 93.88 across 51,728 records; probes verified zero TLS certificate byte violations and zero corrected Sysmon hash identity inconsistencies, but still found two same-LogonID post-logoff process creates. Blind scores: Threat Hunter synthetic 72, Detection Engineer synthetic 74, Network Forensics synthetic 78, Host/EDR synthetic 78; average synthetic confidence 75.5. Top loop-58/59 root-cause targets are storyline logoff ordering, workstation lock/unlock state transitions, Sysmon Event 10 target-user formatting, web-scan TLS duration/byte distribution, DNS tunnel vocabulary, and broader scenario cleanliness.
- [x] Loop 58 storyline logoff ordering fix — fixed the remaining hard loop-57 lifecycle contradiction by allowing Windows Security rendering to move storyline-origin 4634 logoffs after same-session dependent records when final source-native chronology requires it. Verification passed: focused regression tests, full normal `uv run pytest -v` (`2839 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 58 regenerated assessment and loop 59 fixes — loop 58 regeneration/eval completed with automated eval 93.63 across 51,728 records; hard probes verified zero post-logoff 4688/Sysmon process creates, zero TLS certificate byte violations, and zero corrected Sysmon hash identity inconsistencies. Loop 59 fixes enforced chronological workstation lock/unlock state transitions, Sysmon Event 10 `TargetUser` domain formatting, and broader TLS web-scan duration/byte distributions. Verification passed: focused regression tests, full normal `uv run pytest -v` (`2842 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 59 assessment and loop 60 fixes — loop 59 regeneration/eval completed with automated eval 93.71 across 49,666 records; hard probes verified zero lock-state ordering violations, zero same-session post-logoff 4688 records, zero bare Sysmon Event 10 target users, and zero corrected Sysmon hash identity groups in process-create telemetry. Blind scores: Threat Hunter synthetic 74, Detection Engineer synthetic 78, Network Forensics synthetic 88, Host/EDR synthetic 86; average synthetic confidence 81.5. Loop 60 fixes hardened concrete source-native findings: impossible Zeek UDP byte accounting, multi-sensor Zeek timestamp offset sign flips, Sysmon image-load hash/metadata contradictions, and fleet-wide seeded process GUID epoch alignment. Verification passed: focused regression tests, related unit files (`135 passed`), full normal `uv run pytest -v` (`2844 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 60 assessment and loop 61 prioritized fixes — loop 60 regeneration/eval completed with automated eval 93.71 across 49,666 records. Blind scores: Threat Hunter synthetic 62, Detection Engineer synthetic 76, Network Forensics synthetic 84, Host/EDR synthetic 78; average synthetic confidence 75.0. Loop 61 fixes targeted highest expected score leverage first: bounded Zeek files.log observations inside parent conn.log lifetimes, removed per-sensor duration drift for lossless duplicate Zeek flow observations, suppressed duplicate module-load events per process instance at the canonical activity layer, and created named 4648 caller processes when storyline explicit-credential events lacked a PID. Verification passed: focused regression tests, related unit files (`169 passed`), full normal `uv run pytest -v` (`2847 passed, 37 skipped`), `uv run ruff check .`, and `uv run ruff format --check .`.
- [x] Loop 61 regeneration and assessment — regenerated `scenarios/iteration-test/data`, saved eval/probe artifacts under `blind-test/loop-61`, and ran bounded-window blind reviews. Automated eval was 93.99 across 49,503 records. Hard probes verified zero Zeek files outside parent conn lifetimes, zero Sysmon duplicate module loads, zero eCAR duplicate module loads, and zero named 4648 null PIDs. Blind scores: Threat Hunter synthetic 70, Detection Engineer synthetic 63, Network Forensics synthetic 78, Host/EDR synthetic 72; average synthetic confidence 70.75. Top loop-62 targets by expected score leverage are high-count Zeek analyzer timing inversions, Linux SSH self-source/lifecycle gaps, 4648 caller PID/image provenance, revoked OCSP metadata, and then broader authoredness/behavioral issues.
- [x] Loop 62 prioritized fixes — fixed source-native/high-leverage findings first: Zeek files.log observations now render after related http.log/ssl.log analyzer rows, self-sourced Linux SSH accepted-password syslog artifacts are suppressed, 4648 ProcessId values no longer point at a different running process image, and revoked OCSP responses include revocation metadata. Verification passed: focused regression tests, related unit files (`187 passed`), full normal `uv run pytest -v` (`2852 passed, 37 skipped`), `uv run ruff check .`, `uv run ruff format --check .`, and `uv run eforge validate-config`.
- [x] Loop 62 regeneration and assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts and hard probes under `blind-test/loop-62`, and ran the bounded-window blind reviewer panel. Automated eval was 93.91 across 50,475 records. Hard probes verified zero HTTP file-before-parent rows, zero SSL certificate-at/before-parent rows, zero revoked OCSP metadata gaps, zero self-sourced SSH accepted-password syslog rows, and zero reused 4648 PIDs with conflicting caller process names. Blind scores: Threat Hunter synthetic 82, Detection Engineer synthetic 78, Network Forensics synthetic 82, Host/EDR synthetic 82; average synthetic confidence 81.0. Score movement classified as deeper concrete issue surfacing, not a latest-fix regression.
- [x] Loop 63 prioritized fixes — fixed hard/source-native findings first: suppressed Zeek analyzer service labels on SYN-only/no-payload TCP connections while emitting zero count fields consistently, preserved TLS certificate file ordering within a chain, generated SSH public-key fingerprints per user identity rather than per source host, kept browser renderer/content-process commands out of top-level app launch templates, and suppressed TLS inference for storyline raw `/dev/tcp` callbacks decoded from shell base64. Verification passed: focused regression tests, related unit files (`141 passed`), full normal `uv run pytest -v` (`2858 passed, 37 skipped`), `uv run ruff check .`, `uv run ruff format --check .`, and `uv run eforge validate-config`.
- [x] Loop 63 regeneration and hard-probe assessment — regenerated `scenarios/iteration-test/data`, saved eval/probe artifacts under `blind-test/loop-63`, and ran source-native hard probes before launching blind reviews. Automated eval was 93.64 across 48,402 records. Probes verified zero SYN-only service labels, zero missing zero-byte S0 responder byte fields, zero raw `/dev/tcp` callbacks rendered as TLS, zero browser renderer/content commands parented directly to Explorer, and zero SSH public-key reuse across different users from the same source. The remaining hard probe found two TLS certificate chain timestamp inversions, so loop 64 targets the flow-scoped certificate analyzer spacing root cause before spending blind-review cycles.
- [x] Loop 64 certificate chain ordering fix — made Zeek TLS certificate analyzer chain gaps flow-scoped and cumulative rather than seeded by each certificate FUID, and expanded regression coverage to a three-certificate chain. Verification passed: focused Zeek files tests (`18 passed`), full normal `uv run pytest -v` (`2858 passed, 37 skipped`), `uv run ruff check .`, `uv run ruff format --check .`, and `uv run eforge validate-config`.
- [x] Loop 64 regeneration and assessment — regenerated `scenarios/iteration-test/data`, saved eval/probe artifacts and four bounded-window blind-review reports under `blind-test/loop-64`. Automated eval was 93.64 across 48,402 records. Hard probes verified zero S0 service-label leaks, zero missing S0 responder byte fields, zero TLS certificate depth-order inversions, zero raw `/dev/tcp` callbacks rendered as TLS, zero browser renderer/content commands parented directly to Explorer, and zero SSH public-key reuse across different users from the same source. Blind scores: Threat Hunter synthetic 64, Detection Engineer synthetic 62, Network Forensics synthetic 82, Host/EDR synthetic 76; average synthetic confidence 71.0. Top loop-65 targets by hard contradiction and expected score leverage are perimeter/NAT vantage consistency, exact visible parent process LogonID/session inheritance, Zeek SSL file lifetime bounds, exploit/upload HTTP provenance, and eCAR session source semantics.
- [x] Loop 65 P0 root-cause fixes — fixed exact parent process session/logon identity mismatches by rejecting visible non-system Windows parent PIDs from a different LogonID during parent selection, and fixed perimeter/NAT vantage contradictions by resolving external storyline scans and baseline inbound IDS false positives through the public inbound VIP contract while skipping unpublished private hosts. Verification passed: focused regression tests, full normal `uv run pytest -v` (`2861 passed, 37 skipped`), `uv run ruff check .`, `uv run ruff format --check .`, and `uv run eforge validate-config`.
- [x] Loop 65 hard-probe follow-up fixes — fixed regenerated-output blockers before launching blind reviews: Linux UFW/DMZ background flow evidence now uses the public inbound address contract, failed TLS handshakes retain partial handshake byte accounting instead of no-payload service labels, and TLS connection duration is extended to cover delayed Zeek certificate analyzer rows. Verification passed: focused regression tests, full normal `uv run pytest -v` (`2861 passed, 37 skipped`), `uv run ruff check .`, `uv run ruff format --check .`, and `uv run eforge validate-config`.
- [x] Loop 65 final regeneration and assessment — regenerated `scenarios/iteration-test/data`, saved verbose/JSON eval artifacts, hard-probe results, four bounded-window blind-review reports, scores, and final report under `blind-test/loop-65`. Automated eval was 93.19 across 52,625 records. Hard probes verified zero parent LogonID mismatches, zero unpublished private ASA inbound builds, zero Zeek files outside parent connection lifetimes, zero no-payload service labels, zero missing S0 responder byte fields, and zero TLS certificate depth-order inversions. Blind scores: Threat Hunter synthetic 74, Detection Engineer synthetic 84, Network Forensics synthetic 72, Host/EDR synthetic 76; average synthetic confidence 76.5. Final reported issues: Zeek S0 responder byte contradiction, eCAR actor-before-process ordering, multi-sensor Zeek flow cloning, missing SMB/RPC evidence for PsExec-style execution, repeated `userinit.exe` parenting of many `explorer.exe` shells, proxy request/UA drift, missing `Compress-Archive` file artifacts, narrative-polish labels, and thread ID distribution realism.
- [x] Loop 66 documented issue fixes — fixed loop-65 hard/source-native findings for Zeek S0 responder byte accounting, eCAR actor-before-process source offsets, multi-sensor Zeek observation cloning, PsExec-style SMB/RPC causal evidence, proxy request/User-Agent preservation, interactive `userinit.exe`/`explorer.exe` lifecycle realism, and `Compress-Archive` file artifacts. Verified with focused regression tests, Ruff, and full `uv run pytest -q` (`2903 passed, 15 skipped`).
- [x] **SUPERSEDED** Loops 67-76 iterative assessment run — superseded by the later completed assessment runs through Loop 96. The durable work from this batch is captured in the remaining statistical polish, source-observation, web/session, Zeek timing, and eCAR variance TODOs.
  Loop 66 final panel after regenerated hard-probe-clean output scored 93.05 automated eval across 51,785 records, with blind synthetic-confidence scores: Threat Hunter 82, Detection Engineer 72, Network Forensics 73, Host/EDR 82 (average 77.25). Top Loop 67 targets are concrete source-native file-artifact bugs: dangling command-shell quote residue in `Compress-Archive` file telemetry and PSReadLine history artifacts from `cmd.exe`/noninteractive SYSTEM PowerShell; broader exact Security/Sysmon/eCAR process-coverage modeling remains the highest-leverage architectural follow-up.
  Loop 67 final panel after shell file-artifact fixes scored 93.62 automated eval across 52,248 records, with blind synthetic-confidence scores: Threat Hunter 72, Detection Engineer 72, Network Forensics 74, Host/EDR 78 (average 74.0). Hard probes verified zero dangling quote file paths, zero wrong-shell/noninteractive PSReadLine artifacts, and preserved `Compress-Archive` zip artifacts. Top Loop 68 targets are concrete source-native fingerprints: failed 4625 network logons must not show `IpAddress=-` with a nonzero `IpPort`, and TLS/X.509 certificate serials should preserve deterministic identity without making every serial exactly 32 hex characters. The Event 1102 `EventData` complaint is currently treated as a reviewer false positive because fields render under `UserData/LogFileCleared` by source contract and unit test.
  Loop 68 fix pass: Windows 4625 failed-logon rendering now suppresses `IpPort` whenever the source address is unavailable, preventing `IpAddress=-`/nonzero-port contradictions; TLS certificate serial generation now uses data-driven deterministic byte-length variation instead of fixed 128-bit serials, with config-schema/validator coverage. Verification passed: focused tests, related unit files (`108 passed`), `uv run eforge validate-config`, Ruff checks, and full normal `uv run pytest -q` (`2909 passed, 15 skipped`).
  Loop 68 final panel after regenerated hard-probe-clean output scored 93.24 automated eval across 49,639 records, with blind synthetic-confidence scores: Threat Hunter 76, Detection Engineer 67, Network Forensics 74, Host/EDR 68 (average 71.25). Hard probes verified zero `IpAddress=-`/nonzero-port 4625 cases, zero shell file-artifact regressions, preserved archive artifacts, and seven X.509 serial lengths. Top Loop 69 targets are concrete source/correlation issues: explicit proxy traffic with browser/curl-like User-Agents should not map to endpoint `svchost.exe`/`NETWORK SERVICE`; related command bursts should avoid sub-millisecond duplicate `dsquery.exe` executions; then ensure member-server Kerberos logons get target SPN service-ticket prerequisites.
  Loop 69 fix pass: explicit-proxy client legs now create/reuse UA-appropriate user processes instead of falling back to `svchost.exe`; repeated one-shot Windows CLI launches such as `dsquery.exe` are deterministically spaced to avoid sub-millisecond duplicate bursts; logon-driven Kerberos TGS events now target member-server SPNs instead of `krbtgt/` renewals. Generation retry exposed and fixed a baseline 4648 path where sessions without `explorer_pid` passed `None` as the caller PID. First hard-probe retry found residual browser-UA proxy legs when upstream PID inference had already supplied `svchost.exe`, so proxy client-leg ownership now overrides mismatched service PIDs as well. Second hard-probe retry isolated DC service-backed beacon traffic with a browser-like C2 UA still owned by `svchost.exe`; storyline service installs now seed/reuse the installed service binary as the beacon-owning process. Verification passed: focused regressions, related unit files (`180 passed` and `142 passed`), focused explicit-credentials regression (`10 passed`), Ruff checks, and full normal `uv run pytest -q` (`2914 passed, 15 skipped`).
  Loop 69 final panel scored 93.25 automated eval across 51,365 records, with blind synthetic-confidence scores: Threat Hunter 72, Detection Engineer 72, Network Forensics 68, Host/EDR 84 (average 74.0). Hard probes verified the loop-69 proxy/client-process, command-burst, service-beacon ownership, 4625, shell artifact, PSReadLine, archive, and X.509 serial checks. Top Loop 70 targets are source-native contradictions with high score leverage: successful HTTPS/web evidence must not retain `ssl.established=false`, and storyline-authored hostname+destination-IP pairs should preserve the same destination in DNS, TLS, proxy, and ASA evidence.
  Loop 70 final panel after regenerated hard-probe-clean output scored 93.71 automated eval across 52,338 records, with blind synthetic-confidence scores: Threat Hunter 74, Detection Engineer 72, Network Forensics 67, Host/EDR 78 (average 72.75). Hard probes verified zero failed-SSL/successful-HTTP contradictions, zero `cdn-assets-update.com` TLS destination-IP mismatches, zero 4625 `IpAddress=-`/nonzero-port cases, zero 1102 subject-field gaps, zero PSReadLine artifacts, preserved archive file evidence, and seven X.509 serial lengths. Top Loop 71 targets are concrete, source-owned regularities: DC Kerberos/NTLM companion events should not be mechanically adjacent or complete for every 4768, and eCAR failed-logon records should preserve richer native failure reasons instead of flattening distinct substatus values to `bad_password`. Broader source-coverage perfection, dual-sensor completeness, and TLS extraction gaps remain deferred to the collection-imperfection/profile work unless they become the only remaining high-leverage issues.
  Loop 71 final panel after auth-companion and eCAR failure-reason fixes scored 94.95 automated eval across 46,936 records, with blind synthetic-confidence scores: Threat Hunter 84, Detection Engineer 76, Network Forensics 73, Host/EDR 68 (average 75.25). Hard probes verified same-user 4776-within-1s-of-4768 dropped to zero, immediate TGT-to-TGS ratio dropped to 42.41%, eCAR has zero non-password substatus values flattened to `bad_password`, and prior TLS/4625/1102/file-artifact hard gates stayed clean. Top Loop 72 targets are concrete source-native findings: exact duplicate Zeek ICMP summaries, apache daemon activity rendered as an eCAR `USER_SESSION LOGIN`, Sysmon `HKCU` registry paths/noise grafting, and Zeek `conn.service` analyzer vocabulary (`krb`, `tds`, consistent `dce_rpc`).
  Loop 72 fix pass: removed exact Zeek ICMP tuple/timestamp duplicates at the network event layer, rendered Zeek-native analyzer names, suppressed fabricated Linux daemon service-logon evidence for apache/www-data/nginx-style storyline processes, rendered Sysmon HKCU registry paths as HKU SID hives, stopped generic registry side-effect noise from being grafted onto storyline tool processes, and filtered high-frequency `DriverDesc` ambient registry noise. Verification passed: focused regressions, related unit files (`368 passed, 1 skipped`), Ruff checks, and full normal `uv run pytest -q` (`2929 passed, 15 skipped`).
  Loop 72 final panel after regenerated hard-probe-clean output scored 94.83 automated eval across 52,946 records, with blind synthetic-confidence scores: Threat Hunter 76, Detection Engineer 86, Network Forensics 70, Host/EDR 86 (average 79.5). Hard probes verified zero exact duplicate Zeek ICMP rows, zero non-native Zeek service labels, zero Sysmon `HKCU` target objects, zero apache eCAR user-session logins, zero DriverDesc registry events, and all prior TLS/4625/1102/file-artifact gates stayed clean. Top Loop 73 targets are deeper source-native state/modeling bugs: repeated `explorer.exe` parent PIDs spawning children under many unrelated LogonIDs, browser executable versions disagreeing with HTTP user-agent versions, third-party Sysmon Event 7 module metadata rendered as Microsoft Windows, and endpoint/proxy flow attribution or byte accounting collapsed across distinct applications/layers.
  Loop 73 fix pass: fixed exact-session Windows explorer parent selection so seeded/global `explorer.exe` PIDs cannot parent children under unrelated LogonIDs, aligned browser PE versions with configured UA major versions, added data-driven loaded-module PE metadata so Program Files DLLs do not fall back to Microsoft Windows OS fields, and added TLS proxy client-leg framing bytes so Zeek wire counters do not exactly equal proxy application accounting for inspected HTTPS uploads. Verification passed: focused regressions, related unit files (`178 passed`, `345 passed, 1 skipped`, `105 passed`), `uv run eforge validate-config`, Ruff checks, and full normal `uv run pytest -q` (`2933 passed, 15 skipped`).
  Loop 73 hard-probe follow-up: added explicit source-native PE metadata for remaining Microsoft Program Files module-load paths (`SenseCncProxy.dll`, `MpOAV.dll`, and versioned OneDrive `FileSyncShell64.dll`) before launching the blind panel. Verification passed: focused module metadata regression, related unit files (`144 passed, 1 skipped`), `uv run eforge validate-config`, Ruff checks, and full normal `uv run pytest -q` (`2936 passed, 15 skipped`).
  Loop 73 final panel after regenerated hard-probe-clean output scored 94.23 automated eval across 51,910 records, with blind synthetic-confidence scores: Threat Hunter 68, Detection Engineer 73, Network Forensics 72, Host/EDR 74 (average 71.75). Hard probes verified all prior source-native contradiction gates stayed clean. Top Loop 74 target is host-local Windows LogonID/LUID morphology: replace random per-host LogonID draws with deterministic boot-relative monotonic allocation while preserving session reuse semantics.
  Loop 74 code fix pass: replaced random per-host LogonID draws with deterministic boot-relative host-local LUID allocation in `StateManager`, preserving deterministic replay, global uniqueness for current state keys, and same-session reuse semantics. Verification passed: focused state/logon/emitter tests (`128 passed`), Ruff checks, and full normal `uv run pytest -q` before formatting (`2938 passed, 15 skipped`); rendered-output verification follows after commit/regeneration.
  Loop 74 rendered-output follow-up: aligned standalone machine/anonymous logons, service logons, and preplanned RDP sessions to event-time host-local LUID allocation. Regenerated-output probes verified zero non-Type-7 first-4624 monotonicity violations across 411 first-visible LogonIDs and kept prior hard gates clean. Loop 74 final panel scored 94.16 automated eval across 49,544 records, with blind synthetic-confidence scores: Threat Hunter 78, Detection Engineer 82, Network Forensics 76, Host/EDR 72 (average 77.0). Top Loop 75 target is Snort fast-alert source-native identity: render `[gid:sid:rev]`, preserve stable `(gid, sid)` rule meaning, and ensure generated Snort logs are parsed by eval.
  Loop 75 fix pass: fixed Snort fast-alert rendering/parser/config validation. The fix carries `gid` through `IdsContext`, renders `[gid:sid:rev]`, accepts generated `snort_alert.log` in the parser, separates the VNC scan SID from rapid HTTP scan alerts, and validates that each `(gid, sid)` maps to one message across IDS config. Verification passed: focused Snort/parser/config tests (`66 passed`), related generation/format tests (`97 passed, 1 skipped`), `uv run eforge validate-config`, Ruff checks, and full normal `uv run pytest -q` (`2944 passed, 15 skipped`).
  Loop 75 eval follow-up: now that generated `snort_alert.log` is parsed, strict eval exposed a false-positive Snort timestamp spec failure. Parseability normalization now validates source-native Snort timestamps via the parser's parsed datetime instead of treating the raw fast-alert timestamp as ISO. Verification passed: focused parser/parseability tests (`4 passed`), Ruff checks, and full normal `uv run pytest -q` (`2945 passed, 15 skipped`).
  Loop 75 final panel scored 94.16 automated eval across 49,565 records, with blind synthetic-confidence scores: Threat Hunter 82, Detection Engineer 72, Network Forensics 72, Host/EDR 74 (average 75.0). Snort disappeared as a reviewer finding and generated Snort rows parse cleanly. Top Loop 76 target is the concrete Windows 4771 source-native contradiction where `IpAddress="-"` rendered with a nonzero `IpPort`; next non-deferred targets are generated-looking stale/service account suffixes such as `svc_backup5` and repeated Linux shell command pools.
  Loop 76 fix pass: normalized Kerberos 4771 pre-auth failure source semantics so missing source addresses keep `IpAddress="-"` and `IpPort=0` at canonical event construction, with a Windows Security render-time guard for defensive consistency. Verification passed: focused 4771 generator/emitter tests (`2 passed`), related auth/emitter tests (`227 passed`), Ruff checks, and full normal `uv run pytest -q` (`2946 passed, 15 skipped`).
  Loop 76 final panel scored 94.02 automated eval across 51,432 records, with blind synthetic-confidence scores: Threat Hunter 78, Detection Engineer 74, Network Forensics 72, Host/EDR 84 (average 77.0). Hard probes verified zero 4771 or 4625 missing-IP/nonzero-port contradictions and preserved prior Snort/HKCU/apache/ICMP/TLS gates. Top Loop 77 targets are concrete source-native Windows/endpoint defects: Security 1102 missing subject fields, Sysmon/Security LogonGuid disagreement for the same LogonID, Linux PID monotonicity, missing shell child process expansion, and LUID low-nibble regularity. The prior `svc_backup5` stale-service-account suffix issue remains present but lower priority unless bundled with a higher-impact fix.
  Loop 77 fix pass: verified the 1102 finding as a reviewer false-positive because rendered 1102 correctly uses `UserData/LogFileCleared` with subject fields. Fixed real Windows session artifacts by adding canonical session `LogonGuid` state, making Sysmon Event 1 reuse the Security 4624 session GUID, and varying host-local LUID low nibbles while preserving boot-relative ordering. Also clamped explicit child process creation after parent start to preserve eCAR actorID parent linkage, and fixed stale service-account noise to prefer clean unsuffixed aliases over generated-looking numeric suffixes. Verification passed: focused state/Sysmon/baseline/process-link tests (`227 passed`), Ruff checks, and full normal `uv run pytest -q` (`2950 passed, 15 skipped`).
  Loop 77 final panel scored 94.70 automated eval across 50,314 records, with blind synthetic-confidence scores: Threat Hunter 74, Detection Engineer 74, Network Forensics 72, Host/EDR 82 (average 75.5). Hard probes verified zero LogonGuid mismatches, distributed LUID low nibbles, zero numbered `svc_backupN` principals, and prior source-native gates clean. Top Loop 78 targets are Security 4689 before later Sysmon/eCAR same-process activity, Linux eCAR PID chronology, and Windows CBS registry OS-build/value entropy.
  Loop 78 fix pass: fixed host-side source-native contradictions by suppressing brittle Security 4689 audit evidence for long-lived browser GUI processes, allocating Linux PIDs from host event time instead of generator visit order, and making EDR CBS registry package paths host-OS-build aware while replacing repeated low-entropy UserAssist/AccentPalette values with dynamic data. Verification passed: focused PID/emitter/EDR/activity/baseline tests (`306 passed, 1 skipped` plus `332 passed` earlier), `uv run eforge validate-config`, Ruff checks, and full normal `uv run pytest -q` (`2955 passed, 15 skipped`).
  Loop 78 final panel scored 94.70 automated eval across 50,286 records, with blind synthetic-confidence scores: Threat Hunter 72, Detection Engineer 68, Network Forensics 72, Host/EDR 78 (average 72.5). Hard probes verified zero same-ProcessGuid Security 4689 conflicts, zero Linux eCAR PID decreases, zero CBS build mismatches, zero static AccentPalette/UserAssist values, zero numbered `svc_backupN` principals, and zero LogonGuid mismatches. Top Loop 79 targets are concrete endpoint process-tree realism issues: user browsers parented by `winlogon.exe`, dense simultaneous multi-browser launches, and exact Security/Sysmon process-termination timing.
  Loop 79 fix pass: fixed Windows GUI parent selection so user-facing browser launches repair/reuse per-session `explorer.exe` instead of accepting `winlogon.exe`, applied browser affinity to `process_user_apps`, reused active top-level browsers for repeated navigation bursts while preserving renderer child PIDs, spaced repeated top-level browser launches to avoid same-second bursts, and added source-native Sysmon Event 5 termination latency so Security 4689/Sysmon 5 do not timestamp as exact twins. Verification passed: focused process/catalog/Sysmon/Windows emitter tests (`145 passed`), related activity/baseline/world tests (`341 passed, 1 skipped`), `uv run eforge validate-config`, Ruff checks, and full normal `uv run pytest -q` (`2960 passed, 15 skipped`).
  Loop 79 final panel scored 93.89 automated eval across 48,938 records, with blind synthetic-confidence scores: Threat Hunter 78, Detection Engineer 68, Network Forensics 68, Host/EDR 78 (average 73.0). Hard probes verified zero browser parent contradictions, zero same-second multi-browser launch clusters, zero browser Security 4689 records, and zero near-exact Security/Sysmon termination pairs. Top Loop 80 targets are concrete source-native network/statistical tells: perfect ASA built/teardown lifecycle closure, bounded Zeek protocol analyzer timing, near-fixed cross-sensor Zeek skew, and generated-looking Windows registry/DHCP maintenance texture.
  Loop 80 fix pass: ASA now preserves in-window Built/NAT-build records while suppressing source-native Teardown/NAT-teardown records whose close timestamp falls after the collection window, so firewall logs can show slice-boundary dangling connections instead of perfect built/teardown closure. Verification passed: focused ASA unit tests (`53 passed`), related NAT/inbound/firewall tests (`38 passed`), Ruff checks, and full normal `uv run pytest -q` (`2962 passed, 15 skipped`). Loop 80 final panel scored 93.89 automated eval across 48,936 records, with blind synthetic-confidence scores: Threat Hunter 72, Detection Engineer 76, Network Forensics 78, Host/EDR 64 (average 72.5). Hard probes verified ASA no longer has perfect connection lifecycle closure: 2 built connection IDs remained unpaired at the slice boundary, with zero unpaired teardowns and zero NAT pairing gaps. Top Loop 81 target is DNS tunnel response realism: weighted/skewed response TTLs and broader answer vocabulary instead of even short-TTL counts and repeated `status`/`node`/`cdn`/`edge`-style templates.
  Loop 81 fix pass: reduced DNS tunnel response TTL and answer-vocabulary regularity at the data/config and storyline generation layers by moving response TTL selection into weighted `network_params.yaml` config, adding campaign-level TTL skew and response-template stickiness, expanding neutral answer templates with validated placeholders, and counting only visible exfil label payload bytes. Verification passed: focused DNS/config tests (`186 passed`), `uv run eforge validate-config`, repo-wide Ruff checks, and full normal `uv run pytest -q` (`2965 passed, 15 skipped`). Loop 81 final panel scored 93.60 automated eval across 49,049 records, with blind synthetic-confidence scores: Threat Hunter 74, Detection Engineer 74, Network Forensics 78, Host/EDR 84 (average 77.5). DNS hard probes verified zero old-template TXT answers, zero placeholder leaks, and zero equal `1/2/5` TTL triplet; Network review no longer flagged DNS TTL/template regularity and called DNS one of the most realistic areas. Top Loop 82 target is the P0 plain-HTTP proxy contradiction where client-proxy and proxy-origin Zeek HTTP legs disagree on URI path, User-Agent, status, and response semantics.
  Loop 82 fix pass: fixed plain HTTP proxy source-native semantics so client-to-proxy and proxy-origin Zeek HTTP legs preserve the forwarded request's path/query, User-Agent, status, and response body length where appropriate, while keeping terminal proxy errors proxy-native. Verification passed: focused proxy/HTTP tests (`127 passed`), repo-wide Ruff checks, and full normal `uv run pytest -q` (`2966 passed, 15 skipped`). Loop 82 final panel scored 94.25 automated eval across 49,791 records, with blind synthetic-confidence scores: Threat Hunter 82, Detection Engineer 62, Network Forensics 68, Host/EDR 72 (average 71.0). Hard proxy probes verified zero plain-HTTP origin-leg URI/User-Agent/status/response-body mismatches; Network review no longer flagged the prior proxy P0. Top Loop 83 target is the concrete Docker command-line template leak where `docker.exe images --format` renders quadruple braces instead of Docker-native Go-template double braces.
  Loop 83 fix pass: fixed Docker command template escaping in the application catalog so rendered process command lines preserve Docker-native Go-template double braces instead of leaking quadruple-brace upstream escaping syntax. Added catalog regression coverage and a `validate-config` guard that rejects escaped literal brace leaks in command templates. Verification passed: focused catalog/config tests (`55 passed`), `uv run eforge validate-config`, repo-wide Ruff checks, full normal `uv run pytest -q` (`2968 passed, 15 skipped`), and commit `ff5ce1c`. Loop 83 final panel scored 94.25 automated eval across 49,791 records, with blind synthetic-confidence scores: Threat Hunter 72, Detection Engineer 68, Network Forensics 74, Host/EDR 72 (average 71.5). Hard probe verified 0 quadruple-brace occurrences and 3 Docker rows using source-native Go-template double braces. Top Loop 84 target is the multi-reviewer near-power-of-two exfil payload size anchor; next targets are DNS tunnel cadence/response sequencing and short interactive editor lifetimes.
  Loop 84 fix pass: reduced the storyline exfil payload-size fingerprint by replacing exact/power-of-two-style upload byte counts with deterministic, non-round archive/package sizes at the owning storyline/canonical-network layer while preserving cross-source byte consistency. Verification passed before commit: focused storyline/proxy/Zeek tests (`67 passed, 1 skipped`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2970 passed, 15 skipped`), then commit `6e37ef4`. Loop 84 final panel scored 94.19 automated eval across 48,218 records, with blind synthetic-confidence scores: Threat Hunter 86, Detection Engineer 82, Network Forensics 76, Host/EDR 78 (average 80.5). Hard probe verified zero old round exfil values and non-round byte sizes across proxy, Zeek client, and proxy-egress legs. Top Loop 85 target is Windows provider PID/TID and Sysmon thread-ID allocation realism.
  Loop 85 fix pass: aligned Windows Event XML `System/Execution` provider PIDs/TIDs and Sysmon thread IDs with Windows-native allocation semantics so rendered values are coherent, aligned, and sourced from shared helpers instead of ad hoc raw random integers. Verification passed before commit: focused Windows/Sysmon/eCAR tests (`117 passed`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2973 passed, 15 skipped`), then commit `560c50f`. Loop 85 final panel scored 94.19 automated eval across 48,218 records, with blind synthetic-confidence scores: Threat Hunter 74, Detection Engineer 74, Network Forensics 82, Host/EDR 72 (average 75.5). Hard probe verified zero odd provider PID/TID values across 4,768 Security events and 3,199 Sysmon events, zero odd Sysmon Event 8/10 thread IDs, and zero odd eCAR remote-thread IDs. Top Loop 86 target is passive Zeek TLS 1.3 X.509 extraction.
  Loop 86 fix pass: suppressed passive Zeek certificate-chain FUIds, SSL-derived files, and X.509 rows for TLS 1.3 sessions unless the data model explicitly represents decryption, while preserving TLS 1.2 and earlier certificate extraction. Verification passed before commit: focused TLS/certificate tests (`73 passed`), related Zeek/activity/certificate tests (`312 passed`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2975 passed, 15 skipped`), then commit `2665cef`. Loop 86 final panel scored 95 automated eval across 45,912 records, with blind synthetic-confidence scores: Threat Hunter 76, Detection Engineer 82, Network Forensics 82, Host/EDR 62 (average 75.5). Hard probe verified 1,009 TLS 1.3 SSL rows with zero certificate-chain FUIds, zero SSL-derived file rows, and zero X.509 rows, while preserving 235 TLS 1.2 rows with certificate chains. Top Loop 87 target is Windows Security 5156 `FilterRTID` semantics.
  Loop 87 fix pass: reused realistic Windows Filtering Platform `FilterRTID` values per host/policy/layer for Security 5156 events instead of assigning mostly unique per-event identifiers. Verification passed before commit: focused Windows/WFP emitter tests (`78 passed`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2976 passed, 15 skipped`), then commit `ba976a0`. Loop 87 final panel scored 95 automated eval across 45,912 records, with blind synthetic-confidence scores: Threat Hunter 76, Detection Engineer 72, Network Forensics 72, Host/EDR 78 (average 74.5). Hard probe verified 1,660 Security 5156 rows used only 50 global `FilterRTID` values with zero single-use IDs; the prior `WS-AJOHNSON-01` example dropped from 274 unique IDs across 274 rows to 8 unique IDs. Top Loop 88 target is the visible `health.ps1` process-to-network attribution mismatch.
  Loop 88 fix pass: aligned explicit-proxy client process attribution so caller-provided canonical processes are preserved when their image already matches the proxy-visible application, while still repairing mismatched service-process fallbacks for browser traffic. Verification passed before commit: focused explicit-proxy regression tests (`2 passed`), full explicit-proxy unit coverage (`28 passed`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2977 passed, 15 skipped`), then commit `605c3aa`. Loop 88 final panel scored 94.67 automated eval across 45,899 records, with blind synthetic-confidence scores: Threat Hunter 68, Detection Engineer 78, Network Forensics 76, Host/EDR 78 (average 75.0). Hard probe verified the `health.ps1` PowerShell download uses PID 4352 and source port 50779 consistently across eCAR, Sysmon Event 3, Security 5156, Zeek, and proxy logs, with zero attribution mismatches. Top Loop 89 target is browser/Electron process-tree modeling after Host/EDR found Chrome utility/GPU/renderer children parented by Firefox in both Sysmon and eCAR.
  Loop 89 fix pass: repaired browser/Electron process-tree modeling at the canonical generation layer. Baseline app effects now resolve the actual process returned by PID reuse before spawning child/utility processes, network correlation, bash history, or foreground terminations; Windows parent sanitization now requires same-family parents for browser renderer/utility children and Teams Electron utility children, auto-creating a matching top-level app parent when needed. Verification passed before commit: focused spawn-rule tests (`28 passed`), activity+spawn-rule tests (`171 passed`), explicit-proxy regression coverage (`28 passed`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2980 passed, 15 skipped`).
  Loop 89 final panel scored 95.20 automated eval across 43,814 records, with blind synthetic-confidence scores: Threat Hunter 90, Detection Engineer 64, Network Forensics 82, Host/EDR 78 (average 78.5). Hard probe verified zero browser/Electron same-family child-parent mismatches across eCAR and Sysmon. Top Loop 90 target is Linux eCAR shell argv semantics after Threat Hunter found `/usr/bin/mysqldump`, `/usr/sbin/ss`, `/usr/bin/cat`, and `/usr/bin/grep` process-create rows carrying shell redirection/pipeline operators that should belong to shell history or separate pipeline children.
  Loop 90 fix pass: split Linux shell-history command text from source-native eCAR process argv so redirection, pipeline, and shell-control operators are not rendered as arguments to non-shell executables, while preserving shell history and emitting separate child process telemetry for pipeline/control stages. Verification passed before commit: focused activity tests (`147 passed`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2984 passed, 15 skipped`).
  Loop 90 final panel scored 94.87 automated eval across 43,722 records, with blind synthetic-confidence scores: Threat Hunter 68, Detection Engineer 72, Network Forensics 78, Host/EDR 91 (average 77.25). Hard probe verified zero non-shell operator violations across 790 eCAR process-create rows and preserved the `/tmp/patient_claims.sql` file artifact. Top Loop 91 target is source-native session identifier allocation after Host/EDR found Windows LogonIDs and Linux `systemd-logind` session IDs advancing as wall-clock-derived formulas.
  Loop 91 fix pass: replaced clock-derived Windows LogonID/LUID and Linux `systemd-logind` session-ID allocation with deterministic per-host allocator counters that preserve replay and chronological sanity without exposing a constant elapsed-time stride, then tightened logind collision bumps and SSH allocation timing so rendered whole-second syslog timestamps cannot recreate elapsed-second deltas. Verification passed before commit: focused state/logind tests (`94 passed`), related auth/logon/emitter tests (`97 passed`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2988 passed, 15 skipped`).
  Loop 91 final panel scored 95.26 automated eval across 48,808 records, with blind synthetic-confidence scores: Threat Hunter 76, Detection Engineer 78, Network Forensics 64, Host/EDR 86 (average 76.0). Hard probe verified zero elapsed-time identifier pairs across 453 Windows 4624 records and 380 Linux `systemd-logind` sessions. Top Loop 92 target is Linux PID allocation after Host/EDR found eCAR process PIDs advancing almost one-for-one with wall-clock seconds across DB-PROD-01, WEB-EXT-01, WS-LNGUYEN-01, and PROXY-01; next targets are Sysmon file identity stability and dual-Zeek sensor counter semantics.
- [x] Loop 92 fix pass — decoupled Linux process PID allocation from visible wall-clock elapsed seconds by replacing `base + elapsed_seconds` with deterministic per-host coarse PID blocks, irregular same-bucket churn, used-PID tracking, and an elapsed-delta collision guard while preserving chronological ordering for out-of-order generation. Verification passed before commit: focused PID/state tests (`66 passed`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2989 passed, 15 skipped`).
  Loop 92 final panel scored 95.26 automated eval across 48,808 records, with blind synthetic-confidence scores: Threat Hunter 72, Detection Engineer 72, Network Forensics 78, Host/EDR 84 (average 76.5). Hard probe verified zero elapsed-second PID matches across 172 Linux eCAR process-create rows and 154 adjacent positive PID/time pairs. Top Loop 93 target is Windows singleton service lifecycle after Detection and Host/EDR independently found overlapping active `dns.exe`, `spoolsv.exe`, and `dfsr.exe` service processes; next targets are Linux `systemd-logind` monotonicity and ICMP echo byte symmetry.
- [x] Loop 93 fix pass — enforced source-native singleton lifecycle for Windows service processes by reusing active `spoolsv.exe`, `dns.exe`, `dfsr.exe`, `ismserv.exe`, and `msdtc.exe` processes in canonical system-process generation instead of emitting overlapping concurrent create events, while preserving multi-instance service hosts such as `WmiPrvSE.exe`. Verification passed before commit: focused system-process/service-lifecycle tests (`12 passed`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2991 passed, 15 skipped`).
  Loop 93 final panel scored 94.72 automated eval across 44,583 records, with blind synthetic-confidence scores: Threat Hunter 84, Detection Engineer 78, Network Forensics 70, Host/EDR 84 (average 79.0). Hard probe verified zero overlapping active singleton service intervals across Security, Sysmon, and eCAR; the panel no longer reported singleton service overlaps. Top Loop 94 target is Linux `systemd-logind` monotonicity after Detection and Host/EDR independently found repeated visible New-session ID regressions on the same host/logind PID; next targets are Windows fractional timestamp-base clustering, TLS CA chain semantics, and DNS recursion visibility.
- [x] Loop 94 fix pass — normalized rendered syslog `systemd-logind` session IDs in final per-host line order so visible New-session records are monotonic per logind process while matching Removed-session rows retain the rewritten ID. Verification passed before commit: focused syslog/SSH tests (`33 passed`), repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2992 passed, 15 skipped`).
  Loop 94 final panel scored 94.72 automated eval across 44,583 records, with blind synthetic-confidence scores: Threat Hunter 63, Detection Engineer 82, Network Forensics 82, Host/EDR 72 (average 74.75). Hard probe verified zero visible `systemd-logind` New-session inversions across 366 rows and 359 adjacent same-PID pairs; the panel no longer reported the logind regression. Top Loop 95 target is Sysmon Event 7 file identity stability after Detection found same-host DLL paths rendering different hashes; next targets are Linux eCAR systemd PID semantics, OCSP/CRL HTTP modeling, DNS RR corpus realism, TLS CA chain semantics, and Windows fractional timestamp clusters.
- [x] Loop 95 fix pass — stabilized Sysmon Event 7 module hashes so a loaded module path/version has one file identity per host instead of event-instance/signature-validation-derived hashes, and canonicalized the VMware Tools credential-provider DLL metadata. Verification passed before commit: focused Sysmon hash tests (`4 passed`), broader Sysmon emitter tests (`88 passed`), `uv run eforge validate-config`, repo-wide Ruff checks/format checks, and full normal `uv run pytest -q` (`2993 passed, 15 skipped`).
  Loop 95 final panel scored 94.72 automated eval across 44,583 records, with blind synthetic-confidence scores: Threat Hunter 76, Detection Engineer 82, Network Forensics 78, Host/EDR 78 (average 78.5). Hard probe verified zero same-host Sysmon Event 7 module-hash drift across 45 path/version groups, and the panel did not repeat the prior DLL-hash finding. Top next target is a stronger Linux `systemd-logind` lifecycle invariant after Detection found repeated visible `Removed session X` rows before later visible `New session X` rows for the same host/logind PID; next high-leverage targets are Zeek HTTP/SSL integer-millisecond analyzer timestamp offsets, overly perfect nmap scan outcomes, and Windows source-native task XML/WinSxS/SearchProtocolHost artifacts.

### Post-Loop 95 Realism Roadmap

- [x] Capture the post-loop remediation strategy so future sessions do not lose the plan: handle narrow deferred defects first, then group the remaining work into three realism sprints before tackling broader imperfect-observation architecture.
- [x] **Immediate deferred fixes before sprinting** — fixed small, concrete, high-signal issues that can be handled independently: rendered Linux `systemd-logind` pre-window removals now cannot reuse later visible New-session IDs; TLS destination selection excludes OCSP/CRL responder domains so certificate-status traffic stays HTTP/80; servicing-stack `TiWorker.exe` uses a full WinSxS component path; `SearchProtocolHost.exe` uses paired source-native search pipe arguments without malformed SID fragments. Verification passed: focused and related tests (`132 passed`), `uv run eforge validate-config`, Ruff checks/format checks, full normal `uv run pytest -q` (`2996 passed, 15 skipped`), regenerated `scenarios/iteration-test`, hard probes found zero logind remove-before-new violations, zero OCSP/CRL/lencr SSL rows, and zero truncated WinSxS/SearchProtocolHost fingerprint matches; quantitative eval passed at 95.56 across 44,312 records.
- [x] **Sprint 1: Lifecycle and timing realism** — removed modeled-clock fingerprints and hard lifecycle contradictions by adding deterministic sub-millisecond Zeek HTTP/SSL analyzer timing, adding tiny data-driven eCAR session source latency that preserves login-before-process ordering, and rechecking Linux `systemd-logind` lifecycle output. Verification passed: focused Zeek/eCAR/activity tests (`211 passed`), full normal `uv run pytest -q` (`2998 passed, 15 skipped`), Ruff checks/format checks, `uv run eforge validate-config`, regenerated `scenarios/iteration-test`, hard probes found zero whole-millisecond Zeek HTTP/SSL analyzer offsets across 3,419 rows, zero duplicate successful eCAR login millisecond groups, and zero `systemd-logind` remove-before-later-new violations; quantitative eval passed at 95.56 across 44,313 records.
- [x] **Sprint 2: Messier network behavior** — made attack scans and DNS tunnel traffic less tidy by adding host/service-aware nmap and storyline port-scan probe profiles, sparse runtime web-scan status drift, and variable DNS tunnel label depth while preserving first-label payload accounting. Verification passed: focused scan/DNS tests (`351 passed` before final RNG isolation and `266 passed` after), repo-wide Ruff checks/format checks, `uv run eforge validate-config`, full normal `uv run pytest -q` (`3001 passed, 15 skipped`), regenerated `scenarios/iteration-test`, hard probes showed mixed internal nmap states/services (`SF`, `RSTO`, `REJ`, `S0`), mixed external scan states/services including filtered outcomes, and DNS tunnel label depths of 4/5/6; quantitative eval passed at 94.74 across 47,433 records.
- [x] **Sprint 3: Windows source-native fidelity** — made scheduled-task 4698 XML match source `schtasks.exe` commands by threading the original command line through causal/storyline generation, parsing `/SC`, `/MO`, `/TR`, and `/RU`, and rendering source-native trigger repetition/principal/action XML for `/SC HOURLY`. Verification passed: focused scheduled-task/storyline/causal tests (`207 passed`), repo-wide Ruff checks/format checks, `uv run eforge validate-config`, full normal `uv run pytest -q` (`3003 passed, 15 skipped`), regenerated `scenarios/iteration-test`, hard probe confirmed DC-01 Event 4698 `TaskContent` now contains `<Interval>PT1H</Interval>`, `ServiceAccount`, `HighestAvailable`, and `C:\Windows\System32\HealthMonitorSvc.exe`; quantitative eval passed at 94.74 across 47,433 records.
- [x] **Slow-inclusive pytest verification for sprint stack** — `uv run pytest -v --include-slow` passed from the current stacked branch with `3017 passed, 1 skipped` in 1610.78s (0:26:50). No failures surfaced, so no code fixes were needed.
- [x] **Loop 96 blind reviewer pass after sprint merges** — regenerated and evaluated `scenarios/iteration-test` from the merged sprint stack, then ran a blind expert-panel realism review against a neutral copy of the generated data only. Automated eval passed at 94.74 across 47,433 records; blind synthetic-confidence scores were Threat Hunter 76, Detection Engineer 68, Network Forensics 68, Host/EDR 76 (average 72.0, down from Loop 95's 78.5). The panel did not repeat the prior `systemd-logind` remove-before-new issue, whole-millisecond Zeek analyzer offset issue, or Windows scheduled-task/WinSxS/SearchProtocolHost source-native defects; top new findings were web application static response/session realism, too-complete source coverage/correlation, bounded cross-sensor Zeek skew, curated traffic/attack naming, and endpoint/eCAR uniformity.
- [x] **P2** Scenario skill anti-curation guidance follow-up — Revised the dev scenario skill so attacker-controlled domains, service accounts, scheduled tasks, files, and process names blend into ordinary naming conventions without becoming semantic breadcrumbs that reveal the attack narrative. Verification: `uv run pytest tests/unit/test_install_skills.py -q --no-cov` passed (`30 passed`); the same focused test file also passed under the default coverage run, but that command failed the whole-repo coverage threshold because it intentionally ran only one test module.
- [x] **P1** Web application response/session realism follow-up — Added data-driven inbound `web_server` visitor profiles so human visitors consume `traffic_rates.web` as top-level actions, then fan out into required page assets/API calls through `site_maps.yaml`; crawler, health-check, API-client, and opportunistic-probe traffic now uses source-native configured request/status/User-Agent profiles. Static resource sizes are stable per host/path, human navigation and render fanout timing use `timing_profiles.yaml`, and docs/skill references now explain the budget and config ownership. Verification passed: focused web/timing/baseline tests (`107 passed, 1 skipped`), config-related tests (`64 passed`), `uv run eforge validate-config`, repo-wide Ruff checks/format checks, full normal `uv run pytest -q` (`3012 passed, 15 skipped`), and `git diff --check`.
- [x] **P1** Well-synced network sensor timing follow-up — Replaced hardcoded multi-sensor Zeek +/-400ms skew plus broad path delay with a validated `network_sensor_observation` timing profile. The default `well_synced` profile keeps stable per-sensor clock skew within +/-1.5ms and per-flow capture/path delay within 50-2000us while preserving canonical packet/byte truth unless source-native observation variance is explicitly enabled. Verification passed with focused Zeek/timing tests, `uv run eforge validate-config`, repo-wide Ruff checks/format checks, full normal `uv run pytest -q` (`3012 passed, 15 skipped`), and `git diff --check`.
- [x] **P1** Source identity and endpoint baseline realism sprint — completed TLS/X.509 issuer-compatible chain signatures, Sysmon Event 7 native third-party module identity, config-driven Windows scheduled-process timing, and DHCP registry emission policy tied to lease activity. Verified with `uv run eforge validate-config`, focused regressions, Ruff, normal pytest, and slow-inclusive pytest.
- [x] **P2** Endpoint/eCAR baseline variance follow-up — addressed through the host/activity profile realism layer. Host family, role, persona, and stable per-host multipliers now shape endpoint, process, registry, scheduled-task, syslog, bash, eCAR, Windows, Zeek, firewall, IDS, web, and proxy rates; config-driven encoded PowerShell variants and benign endpoint texture reduce repeated per-host artifacts. Verification passed with focused host-activity/config/ASA/baseline tests, `uv run eforge validate-config`, Ruff checks/format checks, full normal `uv run pytest -v`, and slow-inclusive `uv run pytest -v --include-slow --no-cov` (`3057 passed, 1 skipped`).
- [x] **Later architectural sprint: imperfect observation and source coverage** — implemented a training-friendly `complete` default plus overlay-compatible named observation profiles that apply deterministic source-level drop/delay/coverage semantics without modeling contradictions. The policy covers endpoint, network, proxy/web, firewall, IDS, Windows, Sysmon, Zeek, syslog, bash history, and eCAR source families, while ground truth preserves canonical truth and records source evidence status. Verification passed: focused observation/config/ground-truth tests, `uv run eforge validate-config`, Ruff checks/format checks, full normal `uv run pytest -v` (`3036 passed, 15 skipped`), and slow-inclusive `uv run pytest -v --include-slow` (`3050 passed, 1 skipped`).
- [x] Observation-aware automated eval and manifest — generation now writes `OBSERVATION_MANIFEST.json` beside ground truth, `eforge eval` loads it when present, coverage-style causality metrics report raw and observation-adjusted scores for expected non-visible evidence, and correctness/contradiction checks remain strict. Verification passed with config validation, Ruff checks/format checks, focused eval/manifest tests, and full normal `uv run pytest -v` (`3047 passed, 15 skipped`).
- [x] Post-host-activity score check — synced `dev`, cleaned up stale TODOs, regenerated/evaluated `scenarios/iteration-test` from the current iteration-test prompt with `enterprise_standard` observation, and ran one blind expert-panel review without entering another fix loop. Automated eval passed at `92.39` over `108,858` records; blind synthetic-confidence averaged `82.75`. Highest-leverage follow-ups are Linux SSH/syslog lifecycle ordering, Zeek observation-tree consistency, X.509 metadata coherence, Windows OS-build/local-SID identity, and static web asset manifests.
- [x] Current-dev calibration pass — regenerated and evaluated `scenarios/iteration-test` from current `dev`, fixed actionable cleanliness issues in OCSP optional-field rendering, observation-manifest accounting for sensor-filtered network evidence, Kerberos/domain-logon causal ordering, storyline event timing, storyline trace matching, temporal trace comparison, and visible Windows logon-before-process ordering. Verification passed with `uv run eforge validate-config`, scenario validation with only expected sensor/observation/pivot-linkability warnings, quantitative eval at `94.64` with all hard gates passing, Ruff checks, focused regressions (`164 passed`), and full normal `uv run pytest -v` (`3075 passed, 15 skipped`).
- [x] Current-dev assessment continuation loops 107-110 — stopped after Loop 110 at user request after completing the current iteration; four additional EvidenceForge realism improvement loops were run from the latest Loop 106 assessment state.
  - [x] Loop 107 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `67ea9d8`: fixed X.509 public CA chain crypto coherence by preventing root-like ECDSA subject names (`Root R4`, `Root X2`) from being coerced to RSA and adding `GlobalSign Atlas R3` to the RSA CA subject profile. Verification passed with `uv run eforge validate-config`, focused certificate tests (`55 passed`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3277 passed, 15 skipped`). Regenerated eval passed at `96.3042/100` across `75,502` records; hard probe found zero x509 chain signature/key violations across `910` x509 rows. Blind synthetic-confidence scores were Threat Hunter `74`, Detection `70`, Network `72`, Host/EDR `66` (average `70.5`). Top Loop 108 target is the high-volume eCAR `USER_SESSION/LOGIN` unknown `properties.logon_type` schema warning; next targets are AD SRV locator DNS semantics and proxy failure reason phrase diversity.
  - [x] Loop 108 eCAR login schema warning — declared `logon_type` as a source-native eCAR `USER_SESSION/LOGIN` string property and added a regression so the evaluator accepts rendered login records without unknown-field warnings. Verification passed with focused eCAR tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3278 passed, 15 skipped`). Re-eval on the current Loop 107-generated data stayed at `96.3042/100` across `75,502` records; hard probe found zero `Unknown field in ecar (USER_SESSION/LOGIN): logon_type` warnings while preserving `687` eCAR login rows with `properties.logon_type`. Blind synthetic-confidence scores were Threat Hunter `74`, Detection `66`, Network `72`, Host/EDR `64` (average `69.0`). Top Loop 109 target is AD SRV locator DNS semantics; proxy failure phrase diversity remains next.
  - [x] Loop 109 AD SRV locator DNS semantics — moved the real Default-First-Site AD locator name out of resolver-miss NXDOMAIN noise, added it as a legitimate AD SRV query template, and covered both positive SRV answers and NXDOMAIN probe selection. Verification passed with focused DNS tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3280 passed, 15 skipped`). Regenerated eval improved to `96.7188/100` across `78,217` records; hard probe found zero `_ldap._tcp.Default-First-Site-Name._sites.meridianhcs.local` NXDOMAIN rows. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `64`, Network `66`, Host/EDR `64` (average `66.5`). Top Loop 110 target is proxy failure phrase diversity.
  - [x] Loop 110 proxy failure phrase diversity — made generated proxy/Zeek HTTP CONNECT failure status text status-aware and data-driven instead of collapsing `407`, `502`, `503`, and `504` to generic `Proxy Error`. Verification passed with focused proxy/config tests (`3 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3283 passed, 15 skipped`). Regenerated eval held at `96.7188/100` across `78,217` records; hard probe found zero generic `Proxy Error` rows across `292` failed CONNECTs and confirmed varied source-native phrases for `403`, `407`, `502`, `503`, and `504`. Blind synthetic-confidence scores were Threat Hunter `68`, Detection `60`, Network `58`, Host/EDR `61` (average `61.75`). Stopped after this iteration at user request.
- [x] Current-dev assessment continuation loops 111-120 — completed 10 iterative EvidenceForge realism loops from Loop 110, fixing the highest-leverage verified findings each pass, committing each completed fix before regeneration/review, and preserving loop artifacts.
  - [x] Loop 111 fix, regeneration, hard probes, quantitative eval, and blind review completed from commits `8cd071b` and `74f6a29`: diversified Linux maintenance/syslog texture, accepted generated RFC5424 syslog in strict eval, removed cron PAM chatter, and reduced the hardcoded snapd branch. Verification passed with `uv run eforge validate-config`, focused syslog/config/eval parser tests, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3315 passed, 15 skipped`) before both commits. Regenerated eval passed at `96.4821/100` across `82,676` records; hard probes confirmed cron/anacron coverage, zero cron PAM rows, `59` distinct sudo commands, zero stale rsyslog phrase rows, and reduced snapd texture. Blind synthetic-confidence scores were Threat Hunter `82`, Detection `82`, Network `64`, Host/EDR `76` (average `76.0`). Top Loop 112 target is Linux syslog source-native correctness: monotonic kernel bracket timestamps and bounded unattended-upgrades phased percentages; next target is TLS cert history/FUID semantics.
  - [x] Loop 112 syslog source-native correctness — fixed per-host Linux kernel bracket timestamp generation, added final syslog-emitter kernel uptime clamping in rendered order, and bounded Ubuntu phased-update percentage rendering. Verification passed with `uv run eforge validate-config`, focused syslog/config tests, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3317 passed, 15 skipped`; then `3318 passed, 15 skipped` after the emitter clamp). Regenerated eval passed at `96.7994/100` across `74,861` records; hard probes found `0` kernel uptime regressions across `672` kernel bracket rows and `0` phased-update values over `100`. Blind synthetic-confidence scores were Threat Hunter `71`, Detection `88`, Network `68`, Host/EDR `82` (average `77.25`). Top Loop 113 target is Linux anacron source-native correctness; next targets are Security 4697 service start-type derivation and Linux PID/TID/polkit namespace realism.
  - [x] Loop 113 anacron source-native correctness — fixed malformed `Anacron 2.3 started on <number>` messages, removed `cron.weekly` from anacron job selection, emitted coherent daily anacron lifecycles only inside the visible collection window, and preserved one run per host/day. Verification passed with focused anacron/config tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3320 passed, 15 skipped`) before amended commit `508cb3d`. Regenerated eval passed at `96.4952/100` across `78,911` records; hard probes found `30` anacron rows, `0` malformed started-on rows, `0` `cron.weekly` starts, and max `1` anacron start per host/day. Blind synthetic-confidence scores were Threat Hunter `68`, Detection `72`, Network `62`, Host/EDR `72` (average `68.5`). Top Loop 114 targets are concrete source-native contradictions: DC Kerberos 4769-before-4768 ordering and Security 4697 service start-type derivation from `sc.exe create ... start= auto`.
  - [x] Loop 114 Windows audit invariants — fixed visible DC Kerberos 4769-before-4768 ordering in final Windows Security rendering and preserved `sc.exe create ... start= auto` semantics in typed storyline 4697 service-install audit records. Verification passed with focused Windows emitter/storyline tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3323 passed, 15 skipped`) before commit `1862d37`. Regenerated eval passed at `96.5452/100` across `78,911` records; hard probes found `0` Kerberos TGS-before-TGT inversions and `0` service auto-start mismatches. Blind synthetic-confidence scores were Threat Hunter `37` (Real/63 confidence), Detection `36` (Real/64 confidence), Network `78`, Host/EDR `68` (average `54.75`). Top Loop 115 target is Zeek HTTP/conn byte-accounting consistency for non-CONNECT single-transaction flows.
  - [x] Loop 115 Zeek HTTP byte accounting — fixed plain HTTP conn.log payload accounting to derive from source-native HTTP request/response header and body sizes, preserved explicit parent-flow body totals only where a visible HTTP flow can own multiple transactions, and removed unsafe web-server aggregation when collection policy may hide child HTTP rows. Verification passed with focused HTTP/baseline tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3324 passed, 15 skipped`) before amended commit `8a51954`. Regenerated eval passed at `95.8915/100` across `84,387` records; hard probe found `0` single-transaction Zeek HTTP/conn byte-accounting violations across `158` `zeek-core` rows and `375` `zeek-dmz` rows, including 304, redirect, and static-asset cases. Blind synthetic-confidence scores were Threat Hunter `74`, Detection `86`, Network `82`, Host/EDR `78` (average `80.0`). Top Loop 116 targets are global Windows LogonID/LUID morphology and browser module ownership contradictions.
  - [x] Loop 116 Windows endpoint source-native correctness — fixed global Windows LogonID/LUID morphology by keeping generated rendered values inside the normal 32-bit LUID band while preserving deterministic host-local ordering, and fixed browser module ownership so Edge no longer receives Google Chrome package DLLs/metadata. Verification passed with focused state/module tests, broader related tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3325 passed, 15 skipped`) before commit `15623a4`. Regenerated eval passed at `96.4025/100` across `74,166` records; hard probes found `0` LogonID fields above `0xffffffff` across `5,863` rendered fields, max LogonID `0xf8f6592`, `0` browser process metadata mismatches, and `0` Edge/Chrome module package mismatches. Blind synthetic-confidence scores were Threat Hunter `70`, Detection `64`, Network `72`, Host/EDR `76` (average `70.5`). Top Loop 117 targets are browser subresources rendered as new top-level browser processes and SSH syslog connection messages preceding matching Zeek connection starts; next targets are dual-sensor Zeek near-cloning, archive staging visibility, and Linux eCAR OS-aware session semantics.
  - [x] Loop 117 browser/session and SSH timing source-native correctness — browser asset launch-command normalization committed as `97969d0`; follow-up source timing alignment for baseline/SCP SSH syslog committed as `72b144a` after rendered hard probes showed syslog still preceding Zeek due to `source.zeek_conn_start` latency. Focused tests passed (`10 passed`), related unit files passed (`354 passed, 1 skipped`), `uv run eforge validate-config` passed, Ruff checks passed, and full normal `uv run pytest --no-cov -q` passed (`3327 passed, 15 skipped`). Regenerated eval passed at `96.7589/100` across `72,987` records; hard probes found `0` browser asset process launches in eCAR/Security/Sysmon and `0` SSH syslog-before-Zeek timing inversions across `185` matched connection lines. Blind synthetic-confidence scores were Threat Hunter `68`, Detection `64`, Network `32` (Real/68 confidence), Host/EDR `72` (average `59.0`). Top Loop 118 targets are dual-sensor Zeek observation near-cloning, browser launch ownership for OCSP/CDN/service-resource URLs, and Linux eCAR OS-aware session semantics.
  - [x] Loop 118 source-observation and OS-aware endpoint realism — reduced dual-sensor Zeek near-cloning by widening data-driven sensor observation timing and allowing HTTP-backed conn byte counters to diverge while preserving body-size floors; normalized browser/resource ownership so OCSP/CDN/service-resource hosts no longer create fresh top-level browser processes; and made non-Windows eCAR USER_SESSION records use `session_type` instead of Windows `logon_type`, including failed remote login attempts. Verification passed with focused regression suites, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` twice (`3330 passed, 15 skipped`) before commits `698f921` and `bc43b8c`. Regenerated eval passed at `96.0073/100` across `79,348` records; hard probes found `0` browser resource process launches in eCAR/Security/Sysmon, `0` Linux eCAR `logon_type` fields, `0` Linux eCAR missing `session_type` fields, `0` SSH syslog-before-Zeek inversions, and Zeek identical app-byte pairs within 1 ms reduced to `54`. Blind synthetic-confidence scores were Threat Hunter `68`, Detection `61`, Network `24` (Real/76 confidence), Host/EDR `67` (average `55.0`). Top Loop 119 targets are archive-to-upload provenance, residual Zeek cross-sensor timing uniformity, browser session reuse/repeated top-level launches, and Linux polkit source-native message detail.
  - [x] Loop 119 archive provenance and residual source-observation realism — added a visible archive-sized SMB transfer path before exfil upload, widened default Zeek cross-sensor observation timing, reused same-user browser sessions across LogonIDs while suppressing repeated exact top-level launches, and enriched Linux polkit source-native templates. Verification passed with related regression suites (`122 passed`), `uv run eforge validate-config`, Ruff checks, and full normal `uv run pytest --no-cov -q` (`3333 passed, 15 skipped`) before commit `5ec52ef`. Regenerated eval passed at `96.2957/100` across `77,906` records; hard probes found `1` SMB archive download of `313,531,505` bytes, `1` matching SMB files.log archive row, Zeek core/DMZ p50/p95/max deltas of `17.026/49.272/65.783 ms`, `0` repeated exact top-level browser launch groups, and `0` bare polkit boilerplate rows. Blind synthetic-confidence scores were Threat Hunter `36` (Real/64 confidence), Detection `68`, Network `68`, Host/EDR `38` (Real/62 confidence), average `52.5`. Top Loop 120 targets are stateful web/proxy response behavior, explicit proxy SSL-inspection semantics, network source-coordination messiness, and Security-log clear/export presentation cues.
  - [x] Loop 120 web/proxy statefulness and source-collection cues — implemented stateful web/proxy response behavior, explicit inspected-vs-tunneled proxy action semantics, per-host proxy W3C headers, and a guard against generator identity leaking into format headers before regeneration; amended commit `dc2cdc29` includes related tests/docs. Verification passed with focused proxy/format tests (`40 passed`), full normal `uv run pytest --no-cov -q` (`3335 passed, 15 skipped`), `uv run eforge validate-config`, Ruff check, and Ruff format check. Regenerated eval passed at `95.9224/100` across `75,234` records; hard probes found `1/1` proxy files with `x-proxy-action` headers, `0` `EvidenceForge` strings in generated data, `181` `ssl-inspect` proxy rows, `152` tunnel setup rows, `0` static web asset 4xx/5xx groups, `6` SMB archive file rows including `cache_7f3a.zip`, `1` archive-sized SMB conn of `313,115,941` bytes, Zeek core/DMZ p50/p95/max deltas of `16.325/45.608/61.130 ms`, and `0` bare polkit boilerplate rows. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `72`, Network `68`, Host/EDR `78` (average `72.5`). Top next targets are Windows `LogonGuid` UUIDv5 morphology, Zeek Kerberos-to-DC audit coverage, proxy time-taken vs Zeek duration coupling, local SID reuse, and same-user cross-host bash concurrency.
- [x] Current-dev assessment continuation loops 121-127 — stopped after Loop 127 at user request. Seven iterative EvidenceForge realism loops were completed from Loop 120, fixing the highest-leverage verified findings each pass, committing each completed fix before regeneration/review, and preserving loop artifacts.
  - [x] Loop 121 high-signal realism fingerprints — fixed Windows `LogonGuid` UUIDv5 morphology, successful Zeek Kerberos-to-DC audit coverage, proxy `time-taken`/Zeek duration coupling, SearchProtocolHost local SID reuse, same-user cross-host bash concurrency, web-scan URI variation, and DNS tunnel response vocabulary. Verification passed with focused regressions, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3344 passed, 15 skipped`) before amended commit `4064ba05`. Regenerated eval passed at `96.1917/100` across `78,747` records; hard probes found non-UUIDv5-only LogonGuid morphology, `502/502` successful Kerberos service flows covered by nearby DC audit, `0` SearchProtocolHost SID reuse groups, and `0` same-user same-second cross-host bash collisions. Blind synthetic-confidence scores were Threat Hunter `66`, Detection `36` (Real/64 confidence), Network `36` (Real/64 confidence), Host/EDR `72` (average `52.5`). Top Loop 122 target is the concrete APP-INT-01 eCAR SSH login without matching syslog authentication evidence; next targets are same-second proxy C2 pairing and remaining legible DNS tunnel response grammar.
  - [x] Loop 122 APP-INT-01 SSH session correlation — fixed self-sourced Linux type-10 logons so they normalize to local interactive sessions instead of rendering eCAR `session_type=ssh` without a valid remote transport trail; committed as `20e26ca6`. Verification passed with focused Linux SSH/logon tests, related activity/world/baseline/logoff tests (`267 passed, 1 skipped`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3344 passed, 15 skipped`). Regenerated eval passed at `96.1864/100` across `74,634` records; hard probes found `25/25` eCAR SSH logins had nearby `sshd` auth evidence, `0` missing SSH auth companions, and `0` self-sourced eCAR SSH rows. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `72`, Network `73`, Host/EDR `73` (average `72.5`). Top Loop 123 targets are concrete endpoint/source-native issues: Edge proxy traffic attributed to stale Internet Explorer processes with malformed launch URLs, Linux eCAR `tid`/PID semantics and host-banded PID ranges, shell-invalid MySQL command templates, and repeated documentation-range DNS TXT/SPF answers.
  - [x] Loop 123 endpoint source-native consistency — fixed browser/proxy process ownership and CONNECT launch URLs, Linux eCAR `tid == pid`, reduced Linux PID host-banding, shell-safe MySQL `-e` command rendering, and documentation-range background TXT/SPF answers; committed as `56f60444`. Verification passed with focused and proxy regressions (`14 passed` plus explicit-proxy suite `38 passed`), related unit suites (`381 passed`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3351 passed, 15 skipped`). Regenerated eval passed at `95.8544/100` across `80,702` records; hard probes found zero wrong concrete browser-family proxy attributions, zero malformed browser launch URLs, zero Linux eCAR `tid != pid`, zero unquoted MySQL `-e` commands, and zero documentation-range TXT answers. Blind synthetic-confidence scores were Threat Hunter `82`, Detection `84`, Network `36` (Real/64 confidence), Host/EDR `78` (average `70.0`); deliberation consensus revised final synthetic-confidence to Threat `86`, Detection `88`, Network `68`, Host `84` (average `81.5`). Top Loop 124 target is the concrete UFW/Zeek scan contradiction: UFW blocked SYN records with invalid/inconsistent packet lengths and unstable TTLs are paired with Zeek `REJ`/`Sr` responder-reset records for the same tuples.
  - [x] Loop 124 perimeter packet/flow consistency — code fix pass committed as `16114bc8`, aligning UFW `[BLOCK]` scan events with Zeek no-response `S0` semantics, valid source-stable SYN packet lengths, stable inbound TTL profiles, and shared packet-length accounting. Verification passed with focused UFW/Zeek tests, related baseline/conn-state/firewall/Zeek suites (`135 passed, 1 skipped`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3353 passed, 15 skipped`). Regenerated eval passed at `96.4358/100` across `76,915` records; hard probes found `678` UFW block rows, `676` visible Zeek matches, `0` invalid lengths, `0` invalid TTLs, `0` unstable TTL sources, `0` matched Zeek semantic mismatches, and `0` matched packet-length mismatches. Blind synthetic-confidence scores were Threat Hunter `68`, Detection `76`, Network `74`, Host/EDR `72` (average `72.5`). Top Loop 125 target is the concrete multi-sensor Zeek same-flow contradiction: duplicate core/DMZ observations with matching five-tuples, state, history, and zero `missed_bytes` diverge by tens of seconds in duration and packet/byte accounting.
  - [x] Loop 125 multi-sensor Zeek flow lifetime consistency — code fix pass committed as `c88a40ab`, preserving canonical duration/packet/byte accounting for lossless duplicate Zeek observations and allowing bounded per-sensor counter drift only when a row explicitly carries capture loss. Verification passed with focused fan-out/multiplex tests (`33 passed`), related Zeek/network suites (`171 passed`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3354 passed, 15 skipped`). Clean regenerated eval passed at `96.2858/100` across `76,915` records; hard probes found `1,166` lossless TCP/SF same-state/history core-DMZ matches, `0` lossless duration drift rows, `0` lossless counter drift rows, and `0` lossy duration-over-2s rows. The first blind panel was invalidated after review-time data contamination; data was regenerated, made read-only, rescanned, and the clean panel rerun. Clean blind synthetic-confidence scores were Threat Hunter `74`, Detection `72`, Network `68`, Host/EDR `72` (average `71.5`). Top Loop 126 target is the concrete Linux process namespace split where same-host syslog sudo PIDs and eCAR command PIDs look independently generated; next targets are Linux shell-history command/typo fingerprints, Security log clear export semantics, overbroad successful SSH, and DNS tunnel/database probing grammar.
  - [x] Loop 126 Linux process namespace consistency — code fix pass committed as `33222970`, adding a StateManager-owned transient Linux PID allocator for syslog-only short-lived processes and routing `sudo`, per-session/planner-owned `sshd`, failed-login `sshd`/`login`, and scheduled-detail fallback PIDs through the same per-host namespace as eCAR process records. Verification passed with focused StateManager/WorldPlanner/syslog/SSH tests (`178 passed`) plus related baseline/eCAR suites (`307 passed, 1 skipped`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3355 passed, 15 skipped`). Regenerated eval passed at `96.3843/100` across `73,253` records; PID probe found `1,090` transient syslog PID rows across `7` Linux hosts and `0` low transient PID rows on high-eCAR-PID hosts. Clean blind synthetic-confidence scores were Threat Hunter `68`, Detection `72`, Network `68`, Host/EDR `78` (average `71.5`). Top Loop 127 target is Windows source-native identifier morphology for Security `LogonGuid` and Sysmon `ProcessGuid`; next targets are Security log clear export semantics, public DNS/CDN/certificate ownership coherence, overbroad 4672 privilege assignments, and Linux interactive command burst timing.
  - [x] Loop 127 Windows native identifier morphology — code fix pass committed as `27130264`, making Security and fallback Sysmon `LogonGuid` values deterministic UUID4-shaped GUIDs and rendering Sysmon `ProcessGuid` as source-native machine/time/token identifiers with final rendered Event 1 time words and zero-heavy token suffixes instead of arbitrary UUID-like random tails. Verification passed with focused StateManager/Sysmon/realism tests (`171 passed` before final Event 1 rewrite; `109 passed` after), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3356 passed, 15 skipped`). Regenerated eval passed at `96.4343/100` across `73,253` records; GUID probe found `0` malformed/bad-version/bad-variant `LogonGuid` values across `799` nonzero Security/Sysmon GUIDs, `0` Sysmon Event 1 time-word mismatches across `720` process creates, and `0` non-zero-heavy Sysmon `ProcessGuid` tails across `4,967` process GUID fields. Clean blind synthetic-confidence scores were Threat Hunter `78`, Detection `74`, Network `67`, Host/EDR `82` (average `75.25`). Top remaining findings at stop are eCAR foreground shell lifecycle overlap, eCAR negative `pid`/`tid` sentinel values, public external IP role reuse across DNS/scan/service traffic, PowerShell WebClient user-agent mismatch, repeated Linux command/typo pools, and overly complete attack cadence.
- [x] Current-dev assessment continuation loops 128-132 — stopped after Loop 132 at user request after fixing the highest-leverage concrete Loop 131 findings and preserving loop artifacts.
  - [x] Loop 128 eCAR source-native identifiers and Linux shell foregrounding — code fix pass committed as `23f8025a`, omitting unavailable top-level eCAR `pid`/`tid`/`ppid` values instead of rendering `-1`, adding FLOW thread IDs when process-owned, backgrounding long-running shell commands in bash history, and keeping eCAR process argv clean. Verification passed with focused eCAR/activity tests, related eval tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3358 passed, 15 skipped`). Regenerated eval passed at `96.2731/100` across `80,718` records; hard probes found zero negative top-level eCAR IDs, zero foregrounded long-running bash-history commands, and zero process command lines ending in `&`. Blind synthetic-confidence scores were Threat Hunter `78`, Detection `64`, Network `78`, Host/EDR `76` (average `74.0`). Top Loop 129 targets are Linux cron/anacron schedule realism, cross-source sensor timing variance, proxy/TLS/web long-tail diversity, and less legible attack/artifact authoredness.
  - [x] Loop 129 Linux cron/anacron schedule realism — code fix pass committed as `16a53290`, removing schedule-native daily maintenance commands from generic extra-syslog noise, keeping daily jobs in `systemd_schedules.yaml`, moving `debian-sa1` to a sysstat-style 30-minute cron cadence, and adding config validation for schedule-native cron/systemd fragments. Verification passed with focused config/syslog tests, related Linux baseline/systemd/eval tests (`123 passed, 1 skipped`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3359 passed, 15 skipped`). Regenerated eval passed at `96.3773/100` across `78,080` records; hard probes found zero random daily syslog maintenance fragments. Blind synthetic-confidence scores were Threat Hunter `68`, Detection `67`, Network `72`, Host/EDR `72` (average `69.75`). Top Loop 130 targets are the source-native cron workload process tree (`/usr/sbin/cron` rendering shell syntax), short Linux command lifetime profiles, x509 public CA validity realism, and credential-dump process-access targeting.
  - [x] Loop 130 cron process-tree and trivial Linux command lifetimes — code fix pass committed as `4845fafc`, rendering cron scheduled workloads as cron daemon → `/bin/sh -c ...` → workload process trees, suppressing duplicate synthetic syslog context on shell/workload eCAR records, and adding sub-second lifetimes for trivial Linux foreground commands. Verification passed with focused cron/system process/lifetime tests (`14 passed`), related Linux baseline/systemd/eCAR tests (`356 passed, 1 skipped`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3361 passed, 15 skipped`). Regenerated eval passed at `96.4166/100` across `74,738` records; hard probes found zero cron-as-shell rows, `65` `debian-sa1` shell children, and `65/65` workloads parented by the shell. Blind synthetic-confidence scores were Threat Hunter `64`, Detection `72`, Network `62`, Host/EDR `42` (average `60.0`). Top Loop 131 targets are Linux child-PID monotonicity under visible parents, bare stdin-oriented command modeling such as `tail -30`, DNS tunnel/C2 grammar, and Security 1102 subject fields.
  - [x] Loop 131 Linux child PID monotonicity — code fix pass committed as `ab330da9`, making StateManager's Linux PID allocator keep child PIDs above visible parent PIDs when the parent already exists and adding a regression test for lower-candidate child allocation. Verification passed with focused StateManager/cron tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3362 passed, 15 skipped`). Regenerated eval passed at `96.4166/100` across `74,738` records; hard probes found `0` child-PID-not-greater-than-visible-parent cases and `0` parent-create-after-child cases across `105` visible parent/child links. Blind synthetic-confidence scores were Threat Hunter `64`, Detection `78`, Network `82`, Host/EDR `76` (average `75.0`). Top Loop 132 targets are Kerberos DC audit `IpPort` vs Zeek/eCAR flow port consistency, eCAR dependent records before process create, Zeek public-flow MTU-aware packet accounting, and DNS SOA RDATA completeness.
  - [x] Loop 132 Kerberos/eCAR correlation — code fix pass committed as `6d253f27`, reserving Kerberos TCP source ports across DC audit and Zeek/eCAR flow evidence, adding matching wire evidence for audit-only Kerberos preauth/DC machine-account cycles, and normalizing ECAR process-owned telemetry after reordered process-create rows. Verification passed with focused auth/eCAR/system traffic tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3364 passed, 15 skipped`). Regenerated eval passed at `95.5271/100` across `80,353` records; hard probes found `0` Kerberos source-port mismatches across `1,573` exact nearby audit/flow matches, `0` ECAR process-reference ordering inversions, `30` bare `tail -N` process rows, sampled Zeek DMZ MTU packet-accounting violations, and `20` malformed SOA rows. Blind synthetic-confidence scores were Threat Hunter `76`, Detection `82`, Network `76`, Host/EDR `68` (average `75.5`). Top remaining findings at stop are Linux daemon identifier realism (six-digit rsyslog fds, random polkit sessions, bus-name lifecycle), repeated human/admin command and typo pools, short-task endpoint process lifetimes clustered near the window end, curated public web/TLS health-check distributions, ECAR ICMP/session/principal enrichment weak spots, staged DNS/scan red herrings, SOA RDATA completeness, MTU-aware Zeek packet accounting, and bare `tail -N` command modeling.
- [x] Current-dev assessment continuation loops 133-142 — completed 10 additional iterative EvidenceForge realism loops from current `dev`, fixing Linux daemon identifier realism, repeated human/admin command and typo pools, endpoint lifetime realism, public web/TLS distribution texture, eCAR ICMP/session/principal enrichment, staged DNS/scan red herrings, SOA completeness, MTU-aware Zeek packet accounting, and bare `tail -N` command modeling. Printed a rolling last-10 score summary after every loop.
  - [x] Loop 133 Linux daemon identifier statefulness — code fix pass committed as `b4ee3d7d`, preserving caller-provided extra-syslog render values, making rsyslog acquired file descriptors small process-local integers, and adding stateful polkit session/bus/process fields plus monotonic D-Bus bus IDs. Verification passed with focused daemon/config tests, related system/config/dispatcher tests (`142 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3381 passed, 15 skipped`). Regenerated eval passed at `96.1304/100` across `75,833` records; hard probes found `0` rsyslog fd-over-999 rows, `0` polkit unix-session-over-999 rows, `0` D-Bus bus-over-999 rows, and `0` eCAR ICMP zero-port rows. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `72`, Network `68`, Host/EDR `72` (average `71.0`). Top Loop 134 targets are duplicate core/DMZ Zeek observation metrics (`1,343/1,479` matched overlapping flows), Linux eCAR PID monotonicity, Sysmon ProcessGuid boot-time regularity, source-native DKIM TXT payloads, and suspicious DNS/web-scan grammar.
  - [x] Loop 134 Zeek multi-sensor observation variance — code fix pass committed as `19b7d317`, verifying the duplicate core/DMZ Zeek metric finding first (`1,348/1,479` matched overlaps had identical duration/byte/packet/history metrics), then adding lossless observation texture at the Zeek sensor-rendering layer so independent non-locked TCP taps no longer clone duration, packet, or IP-byte metrics. Verification passed with focused Zeek multiplex/fanout tests, broader Zeek format/baseline tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3382 passed, 15 skipped`). Regenerated eval passed at `95.7270/100` across `80,629` records; hard probes found `0/1,278` non-locked overlapping flows with identical metrics while preserving exact accounting for locked DNS/ICMP observations. Blind synthetic-confidence scores were Threat Hunter `70`, Detection `78`, Network `72`, Host/EDR `74` (average `73.5`). Top Loop 135 targets are the regression-like fixed `+0.750000s` SSH cross-sensor duration delta introduced by the lossless duration cap, polkit action/program mismatches, CRON/sysstat second-level jitter, Linux pipeline ECAR coverage, and the File-SRV 17:01 lateral-use network gap.
  - [x] Loop 135 Zeek lossless duration cap texture — code fix pass committed as `1bc5f0ad`, replacing the flat lossless duration cap with bounded per-flow/per-sensor texture and adding a long-SSH regression test so capped observations do not flatline at `+0.750000s`. Verification passed with focused Zeek multiplex/fanout tests (`35 passed`), related Zeek format/files/baseline tests (`106 passed, 1 skipped`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3383 passed, 15 skipped`). Regenerated eval passed at `96.1770/100` across `80,629` records; hard probes found `0/113` overlapping SSH flows with exact `+0.750000s` duration deltas and `113` unique SSH duration deltas. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `64`, Network `88`, Host/EDR `62` (average `71.5`). Top Loop 136 target is the high-confidence Zeek SSL/X509 file-analysis timing contradiction: certificate chain depth rows can appear out of order and X.509 rows can precede their matching files rows. Other next targets are proxy domain/User-Agent/content-type profile binding, Sysmon ProcessGuid suffix entropy, SearchProtocolHost profile SID modeling, and PE metadata for signed updaters.
  - [x] Loop 136 Zeek SSL/X509 file-analysis timeline — code fix pass committed as `949108ea`, verifying the reported depth-order and X.509-before-files contradictions first, then making `files.log` and `x509.log` share an ordered TLS certificate-analysis timeline with per-chain depth spacing and X.509 rows after matching file-analysis rows. Verification passed with focused TLS/files/activity/multiplex tests (`71 passed`), related Zeek timing/source tests (`79 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3385 passed, 15 skipped`). Regenerated eval passed at `96.2770/100` across `80,629` records; hard probes found `0` certificate depth inversions, `0` adjacent fixed `0.001s` certificate gaps, `0` X.509-before-file rows, and `0` X.509-equal-file rows across core and DMZ sensors. Blind synthetic-confidence scores were Threat Hunter `66`, Detection `72`, Network `68`, Host/EDR `78` (average `71.0`). Top Loop 137 targets are the repeated Host/Detection source-native fidelity cluster: UserAssist value names/details, Sysmon ProcessGuid morphology, polkit action/program pairing, and missing PE metadata for common Microsoft binaries.
  - [x] Loop 137 endpoint source-native fidelity cluster — code fix pass committed as `1aacf04d`, verifying the UserAssist value-name/detail, polkit action/program, Sysmon ProcessGuid morphology, and missing PE metadata findings against rendered output first, then fixing UserAssist materialization, typed polkit action/process pairing, common Windows PE metadata, and low-entropy Sysmon ProcessGuid tails at the owning config/rendering layers. Verification passed with focused regressions, related endpoint/config tests (`231 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3388 passed, 15 skipped`). Regenerated eval passed at `96.1467/100` across `81,434` records; hard probes found `0` numbered UserAssist placeholders, `0` short UserAssist details, `0` polkit action/process mismatches, `0` invalid authentication-agent paths, `0` sampled missing PE metadata rows, and `0` low-entropy ProcessGuid tails. Blind synthetic-confidence scores were Threat Hunter `74`, Detection `66`, Network `76`, Host/EDR `77` (average `73.25`). Top Loop 138 target is the hard Zeek OCSP timing contradiction where OCSP analyzer rows occur after the linked HTTP response file and parent TCP connection have ended; next targets are Linux same-parent PID regression, journald/logind overproduction, Linux argv/command-pool repetition, and AD SRV DNS realism.
  - [x] Loop 138 Zeek OCSP source-native timing — code fix pass committed as `7cfccdfc`, verifying the OCSP-after-parent-connection contradiction first (`69/69` rows after linked file end and parent conn end), then bounding OCSP analyzer rows inside the owning HTTP response file/connection lifetime while preserving Zeek-native `ocsp.id` ↔ `files.fuid` linkage. Verification passed with focused Zeek SSL/OCSP tests (`25 passed`), adjacent Zeek timing/source suites (`104 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3389 passed, 15 skipped`). Regenerated eval passed at `95.3467/100` across `81,434` records; hard probes found `0` OCSP missing file/conn/http links, `0` OCSP-before-file rows, `0` OCSP-after-file-end rows, and `0` OCSP-after-conn-end rows. Blind synthetic-confidence scores were Threat Hunter `86`, Detection `82`, Network `74`, Host/EDR `70` (average `78.0`). Top Loop 139 targets are large TCP-transfer ACK-side packet accounting, Kerberos same-socket 4769-before-4768 inversions, DNS TXT/DMARC cache stability inside TTL windows, selective Sysmon Event 1 omissions, and Linux shell-history command-pool artifacts.
  - [x] Loop 139 large TCP transfer packet accounting — code fix pass committed as `8900bf5e`, verifying the high-byte Zeek transfer rows with single-digit ACK-side packet counts before fixing TCP payload/reverse-ACK packet floors in the network generation layer. Verification passed with focused packet-accounting regression coverage (`41 passed`), adjacent Zeek/network emitter suites (`187 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` before formatting (`3390 passed, 15 skipped`) plus post-format targeted verification. Regenerated eval passed at `96.6267/100` across `77,465` records; hard probe found `0` reverse-ACK floor violations across `6` large uploads and `5` large downloads. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `72`, Network `72`, Host/EDR `72` (average `72.0`). Top Loop 140 targets are the hard Snort TLS-failure versus Zeek-established-flow contradiction, Linux `sshd` child PID monotonicity, OEM updater domain/path/User-Agent inventory binding, Windows device-volume/SID templating, and threshold IDS alert support.
  - [x] Loop 140 IDS/protocol semantic consistency — code fix pass committed as `a4623f56`, verifying the Snort TLS-handshake-failure/Zeek-established-flow contradiction and unsupported Rapid POP3 threshold alerts before suppressing those signatures from generic baseline false-positive generation via the existing `baseline_fp_allowed` contract. Verification passed with focused IDS signature regression coverage (`1 passed`), related config/realism/validation tests (`105 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3390 passed, 15 skipped`). Regenerated eval passed at `96.0962/100` across `81,999` records; hard probe found `0` TLS-handshake-failure alerts and `0` Rapid POP3 alerts across `39` Snort alerts. Blind synthetic-confidence scores were Threat Hunter `78`, Detection `36` (assessed Real with confidence `64`), Network `70`, Host/EDR `66` (average `62.5`). Top Loop 141 targets are Linux eCAR shell/process semantics, repeated Linux/bash command templates by persona and host role, PTR/A mirror regularity, internal naming drift in command pools, and malicious DNS label/TXT grammar.
  - [x] Loop 141 Linux eCAR and bash command provenance — code fix passes committed as `075a64cd`, `b7867189`, and `a9d7fee5`, fixing Linux shell-command eCAR rendering, home expansion, server/workstation command scoping, scenario-domain internal URL parameterization, and planner-owned catalog process domains. Verification passed with focused regressions, related suites, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov` (`3396 passed, 15 skipped`). Regenerated eval passed at `96.1313/100` across `79,737` records; hard probes found zero bad bare-shell history-clear process rows, quoted-tilde process command lines, `*.corp.local` app URL commands, exact `kubectl get nodes -o wide` commands, and server-side desktop-command process rows. Blind synthetic-confidence scores were Threat Hunter `74`, Detection `74`, Network `78`, Host/EDR `76` (average `75.5`). Top Loop 142 targets are the literal `{username}` Sysmon/eCAR module path leak, same-UID Zeek DNS timestamp/RTT/conn-window contradiction, LDAP base-DN and short-process lifetime realism, repeated Linux command-pool fingerprints, and weak Sysmon metadata for known signed software.
  - [x] Loop 142 source-native endpoint/network contradictions — code fix pass committed as `249f3c09`, fixing literal `{username}`/`{user}` module-path leaks, deriving LDAP base-DN placeholders from the scenario domain, bounding Zeek DNS timestamps so `dns.ts + dns.rtt` stays inside the rendered conn lifetime, and keeping planner-owned `ldapsearch` helpers short-lived via foreground finalizers. Verification passed with focused regressions, nearby source-timing/activity/world-model suites (`291 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov` (`3400 passed, 15 skipped`). Regenerated eval passed at `96.6809/100` across `79,847` records; hard probes found zero `{username}`/`{user}` placeholder leaks, zero `{ldap_base_dn}` leaks, zero `dc=corp,dc=local` LDAP base-DN leaks, and zero same-UID Zeek DNS RTT rows outside the rendered conn lifetime. Blind synthetic-confidence scores were Threat Hunter `66`, Detection `72`, Network `64`, Host/EDR `72` (average `68.5`). Next high-leverage targets are Linux eCAR SSH logout/session identity alignment with syslog PAM closes, command-aware eCAR process/network/lifetime semantics for local commands such as `docker ps`, sudo denied-command source-native `PWD=` formatting and service-account pool diversity, opaque DNS-tunnel TXT payload grammar, and broader dynamic HTTP/User-Agent long-tail variation.
- [x] Iteration-test prompt attacker messiness refresh — updated the source prompt to require a messy, exploratory attacker path with 10-15 naturally interspersed mistakes/recon detours, phase-specific false starts, wrong pivots, typos, dead ends, abandoned names, and source-native evidence instead of merely descriptive notes.
- [ ] **IN PROGRESS** Current-dev assessment continuation loops 143-176 — run the active iterative EvidenceForge realism loops from updated `dev`, fixing the highest-leverage verified findings, committing completed fix passes, regenerating/evaluating, and preserving per-loop artifacts/scores.
  - [x] Loop 143 Linux SSH logout telemetry — code fix pass committed as `92a0ee2b`, carrying SSH source tuples into eCAR logout rows, rendering Linux SSH logoffs as PAM session-close syslog rows, preserving PAM SSH lifecycle rows through observation missingness, and covering Linux type-10 SSH logoffs without persisted `session_kind`. Verification passed with focused logoff/dispatcher/eCAR tests, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3402 passed, 15 skipped`). Regenerated eval passed at `96.6658/100` across `79,872` records; hard probes found `0` SSH logouts without PAM close, `0` without source tuples, and `0` login/logout object mismatches. Blind synthetic-confidence scores were Threat Hunter `70`, Detection `64`, Network `58`, Host/EDR `82` (average `68.5`). Top Loop 144 targets are DC Kerberos 4769-before-4768 ordering for matching principal/IP/port tuples, malformed Security 1102 clear-log EventData, WMI Provider Host path inconsistency, too-clean storyline breadcrumbs, and proxy product-personality polish.
  - [x] Loop 144 endpoint source-native ordering/path fix — code fix pass committed as `0658ec96`, making the final DC Kerberos 4768/4769 ordering repair key source port as well as principal/IP, fixing WmiPrvSE system-process templates to use `C:\Windows\System32\wbem\WmiPrvSE.exe`, and adding focused regressions. Verification passed with focused Kerberos/WMI tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3404 passed, 15 skipped`). Regenerated eval passed at `96.7658/100` across `79,872` records; hard probes found `0` Kerberos same-principal/IP/port inversions, `0` WmiPrvSE non-wbem paths, `0` malformed 1102 subject rows, and `1` Security 5156 before same-PID 4688 process-create skew (`20.54s`). Blind synthetic-confidence scores were Threat Hunter `72`, Detection `74`, Network `62`, Host/EDR `72` (average `70.0`). Top Loop 145 targets are the WS-PPATEL Edge/Citrix process/network timestamp contradiction, PROXY-01 eCAR inbound-flow principal attribution, workstation software-profile overloading, repeated Linux bash command pools, and TCP-only Kerberos network profile.
  - [x] Loop 145 process/network timestamp ownership — code fix pass committed as `e6952094`, preventing causal browser/explicit-proxy owner process launches from being moved after their socket evidence by anti-burst browser spacing, carrying WFP process timing from canonical state, and adding focused HTTP/proxy regressions. Verification passed with related focused tests (`62 passed`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3406 passed, 15 skipped`). Regenerated eval passed at `96.6658/100` across `79,869` records; hard probes found `0` Kerberos same-principal/IP/port inversions, `0` WmiPrvSE non-wbem paths, `0` malformed 1102 rows, and `0` Security 5156-before-same-PID-4688 skews. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `66`, Network `64`, Host/EDR `66` (average `67.0`). Top Loop 146 targets are TCP-only Kerberos transport distribution, inconsistent eCAR endpoint flow attribution/ICMP shape, workstation software-profile overloading, repeated Linux bash command pools, and too-clean attacker storyline behavior.
  - [x] Loop 146 Kerberos transport realism — code fix passes committed as `32d37f96` and `c578091a`, adding data-driven Kerberos UDP/TCP transport profiles and bounding UDP Kerberos duration/byte/state shapes while preserving DC audit and WFP evidence. Verification passed with focused Kerberos/config/activity tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3410 passed, 15 skipped`). Regenerated eval passed at `96.1067/100` across `78,465` records; hard probes found `0` Kerberos same-principal/IP/port inversions, `0` WmiPrvSE non-wbem paths, `0` malformed Security 1102 rows, `0` Security 5156-before-same-PID-4688 skews, Zeek Kerberos `tcp=383`/`udp=866`, WFP Kerberos Protocol `6=378`/`17=844`, and `0` UDP Kerberos shape violations. Blind synthetic-confidence scores were Threat Hunter `76`, Detection `68`, Network `72`, Host/EDR `68` (average `71.0`). The prior TCP-only Kerberos finding did not recur. Top Loop 147 targets are public CA/X.509 metadata contradictions, too-staged attack narrative, curated Linux bash/syslog texture, over-normalized endpoint/eCAR joins, and an isolated Snort/Zeek tuple gap.
  - [x] Loop 147 X.509 public CA realism — code fix pass committed as `6fb809ec`, adding data-driven stable authority profiles for public/enterprise CA roots and intermediates, teaching chain generation to use those profiles instead of observation-window-derived validity, and adding config/schema/focused certificate coverage. Verification passed with focused cert/config/Zeek tests (`201 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3414 passed, 15 skipped`). Regenerated eval passed at `96.1567/100` across `78,465` records; hard probes found `0` public CA profile violations across `758` X.509 rows, preserved Kerberos TCP/UDP mix, and kept prior ordering/1102/WMI gates clean. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `62`, Network `68`, Host/EDR `76` (average `69.5`). The prior concrete X.509 contradiction did not recur; Network called TLS/X.509/OCSP one of the dataset's strongest areas. Top Loop 148 targets are the Windows 4720 account-creation password/UAC contradiction, repeated Linux bash/typo pools, identical DHCP byte/packet shapes, Linux interface-name drift, bounded Sysmon EventRecordID gaps, and templated web/DNS/proxy distributions.
  - [x] Loop 148 Windows account-management semantics — code fix passes committed as `bd13f72c` and `c1330075`, adding source-native 4720/4724/4738 follow-up evidence for password-bearing `net user ... /add` commands and preserving those follow-ups when an explicit `account_created` storyline event owns the SID. Verification passed with focused causal/activity/storyline/timing tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3416 passed, 15 skipped`). Regenerated eval passed at `96.1567/100` across `78,467` records; hard probes found `1` password-bearing account-add command with `0` missing/contradictory follow-up sequences, `0` Kerberos same-principal/IP/port inversions, `0` malformed Security 1102 rows, `0` Security 5156-before-same-PID-4688 skews, preserved Kerberos TCP/UDP mix, and `0` public CA profile violations across `758` X.509 rows. Blind synthetic-confidence scores were Threat Hunter `78`, Detection `76`, Network `78`, Host/EDR `82` (average `78.5`). The prior 4720/password contradiction did not recur. Top Loop 149 targets are zero-payload UDP Kerberos rows labeled `krb`, placeholder DKIM TXT public keys, jittered CRON/sysstat timing, high-signal Sysmon registry-noise frequency, and the still-too-clean attacker storyline.
  - [x] Loop 149 Zeek Kerberos zero-payload service labels — code fix pass committed as `13b888b4`, clearing analyzer service labels on no-payload UDP failures outside DNS so UDP/88 `S0`/zero-byte rows no longer claim Kerberos parsing. Verification passed with focused Kerberos Zeek regressions, broader activity/Zeek suites (`268 passed`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3417 passed, 15 skipped`). Regenerated eval passed at `97.1861/100` across `80,995` records; hard probes found `8` zero-payload UDP/88 rows and `0` with Kerberos service labels, plus clean prior account-followup, Kerberos-ordering, Security 1102, WMI path, 5156/process-ordering, UDP Kerberos shape, and X.509 profile gates. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `82`, Network `90`, Host/EDR `64` (average `77.0`). The prior zero-payload Kerberos service-label contradiction did not recur. Top Loop 150 targets are established Zeek TLS `ssl_history` rows missing ServerHello markers, malformed DNS SOA RDATA, Sysmon Event 7 PE metadata/version templating, placeholder DKIM/TXT values, and Sysmon ProcessGuid morphology.
  - [x] Loop 150 Zeek TLS `ssl_history` semantics — code fix pass committed as `208694f9`, replacing conn-log-like SSL history strings with source-native TLS message-code histories and preserving deterministic certificate/resumption behavior. Verification passed with focused SSL/context/certificate regressions, related Zeek/SSL suites (`117 passed`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3418 passed, 15 skipped`). Regenerated eval stayed at `97.1861/100` across `80,995` records; hard probes found `2,032` established Zeek SSL rows with `0` missing ServerHello markers, `10` distinct established `ssl_history` values, `0` invalid history-code rows, and clean prior UDP/88 zero-payload service-label, X.509 public-CA, and Kerberos transport gates. Blind synthetic-confidence scores were Threat Hunter `64`, Detection `82`, Network `62`, Host/EDR `74` (average `70.5`). The prior Zeek `ssl_history` finding did not recur; Network explicitly called TLS logging source-native. Top Loop 151 targets are DNS TXT TTL/cache instability and short DKIM keys, one-shot PowerShell parentage, DC workstation-style proxy baseline leakage, OCSP status/client-behavior coupling, and malformed SOA RDATA.
  - [x] Loop 151 DNS TXT cache/key realism — code fix pass committed as `d500c61a`, adding stable source-native TXT answer generation for SPF/DMARC/DKIM/verification lookups, long base64-like DKIM public keys, and reuse from DNS-tunnel background TXT records. Verification passed with focused DNS support tests (`7 passed`), broader DNS/Zeek suites (`104 passed`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3419 passed, 15 skipped`). Regenerated eval passed at `96.8241/100` across `80,587` records; hard probes found `0` TXT answer changes inside resolver TTL windows, `0` short DKIM keys, `0` hex-only DKIM keys, and clean prior Zeek SSL/Kerberos gates, while SOA RDATA remains incomplete (`18` rows). Blind synthetic-confidence scores were Threat Hunter `68`, Detection `68`, Network `78`, Host/EDR `66` (average `70.0`). The prior TXT/DKIM cache/key finding did not recur. Top Loop 152 targets are public DNS IPv6 answer-corpus fingerprints, duplicate ASA ICMP build/teardown messages, exact TLS `duration: 1.2` constants, curated bash/syslog mess, and malformed SOA RDATA.
  - [x] Loop 152 public DNS IPv6 corpus realism — code fix pass committed as `b2168bc3`, replacing the single unknown-public IPv6 fallback prefix with a deterministic provider-style fallback pool plus a missing AWS-range mapping, so unrelated public domains no longer collapse onto `2a09:bac0:*`. Verification passed with the new DNS registry regression, DNS registry/support-query tests (`36 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3420 passed, 15 skipped`). Regenerated eval passed at `96.9741/100` across `80,587` records; hard probes found `0` target public-domain AAAA rows under `2a09:bac0` across `20` target rows, while `40` exact TLS `duration: 1.2` rows, `20` duplicate ASA ICMP messages, and `18` incomplete SOA rows remain. Blind synthetic-confidence scores were Threat Hunter `74`, Detection `82`, Network `76`, Host/EDR `67` (average `74.75`). The prior public IPv6 prefix finding did not recur. Top Loop 153 targets are public X.509 CA profile validity/reuse, proxy DNS resolver policy, duplicate ASA ICMP messages, exact TLS duration constants, and endpoint PE metadata/hash realism.
  - [x] Loop 153 public X.509 CA profile realism — code fix pass committed as `096d2141`, shortening Let's Encrypt R3/E1 authority-profile validity to intermediate-scale windows, replacing the exact GlobalSign Atlas 2024-01-01/2026-01-01 profile boundaries, and adding quarterly GlobalSign Atlas issuer/profile variety with regression coverage. Verification passed with focused certificate tests (`4 passed`), related certificate/Zeek tests (`101 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3421 passed, 15 skipped`). Regenerated eval passed at `96.3241/100` across `80,587` records; hard probes found `0` Let's Encrypt R3/E1 intermediate rows valid past 2030, `0` GlobalSign Atlas rows with the old exact validity boundaries, quarterly Atlas subject variety, `0` exact TLS `duration: 1.2` rows, while `120` duplicate ASA ICMP message bodies and `18` incomplete SOA rows remain. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `64`, Network `78`, Host/EDR `72` (average `71.5`). The prior X.509 CA profile finding did not recur; Network called TLS/X.509 content varied and coherent. Top Loop 154 targets are eCAR `USER_SESSION` lifecycle object reuse, random-looking successful external web client IP pools, clean public portal web noise, too-clean storyline friction, and duplicate DB root bash-history blocks.
  - [x] Loop 154 eCAR session lifecycle realism — code fix pass committed as `d48c3877`, making workstation unlock reauthentication use a deterministic child eCAR `USER_SESSION` object while linking back to the durable interactive session as `actor_id`. Verification passed with focused unlock/session tests (`3 passed`), related activity/eCAR/logoff/cross-source tests (`323 passed`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3422 passed, 15 skipped`). Regenerated eval passed at `96.9741/100` across `80,587` records; hard probes found `0` duplicate eCAR LOGIN object IDs across `688` LOGIN rows and `0` duplicate bash timestamp+command pairs, while successful inbound web/TLS public-prefix rows (`30`) and all-established DMZ SSL rows (`1,980/1,980`) remain. Blind verdicts were Threat Hunter Real confidence `62` (synthetic-confidence `38`), Detection Real confidence `64` (synthetic-confidence `36`), Network Synthetic confidence `68`, and Host/EDR Synthetic confidence `72` (average synthetic-confidence `53.5`). The prior repeated eCAR `USER_SESSION` objectID finding did not recur. Top Loop 155 targets are helper-process image/command truth for Zoom/Webex child helpers, foreground Linux shell parent/child termination ordering, Zeek DMZ SSL/file observation imperfection, exact SSL duration reuse, and clean storyline/telemetry completeness.
  - [x] Loop 155 process identity and foreground shell lifecycle realism — code fix passes committed as `f36f8000` and `b14cf417`, making benign helper child processes derive identity/PE metadata from the actual command-line executable and keeping visible eCAR parent processes alive through visible child lifetimes. Verification passed with focused helper/lifecycle tests, related catalog/spawn/systemd/eCAR suites (`115 passed` after the eCAR guard), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3426 passed, 15 skipped`). Regenerated eval passed at `96.6241/100` across `80,587` records; hard probes found `0` helper image mismatches across eCAR/Security/Sysmon, `0` Sysmon OriginalFileName mismatches, and `0` debian-sa1 parent-before-child terminations across `65` parent/child pairs, while DMZ SSL remains all-established (`1,980/1,980`) and DMZ files remain fully visible (`795/795`). Blind synthetic-confidence scores were Threat Hunter `62`, Detection `64`, Network `64`, Host/EDR Real confidence `62` (synthetic-confidence `38`), average synthetic-confidence `57.0`. The prior helper-process and foreground-shell endpoint findings did not recur; Host/EDR assessed the endpoint evidence as real. Top Loop 156 targets are missing network evidence for remote `sqlcmd.exe -S 10.0.2.50`, authored DMZ DNS resolver selection, missing explicit-proxy CONNECT/tunnel evidence for a PowerShell manifest fetch, tidy Windows companion timing/lock durations, and repeated endpoint user artifacts/RDP cadence.
  - [x] Loop 156 remote command network causality and DMZ DNS resolver realism — code fix passes committed as `dd7291c9`, `cc480317`, and `77adcba5`, adding typed-storyline and baseline database-command target parsing plus visible failed TCP/1433 evidence for unresolved remote DB targets, while making explicit proxy DNS and IDS false-positive DNS resolver choices use configured internal resolvers. Verification passed with focused SQL/proxy/baseline tests, related activity/storyline/proxy/deferred suites (`294 passed`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3438 passed, 15 skipped`). Regenerated eval passed at `96.3945/100` across `80,414` records; hard probes found `1` remote `sqlcmd.exe` process with matching Sysmon DNS, Windows 5156, eCAR FLOW, and Zeek S0 TCP/1433 evidence, `0` proxy DNS queries to unexpected resolvers, and DMZ SSL still all established (`1,962/1,962`). Blind synthetic-confidence scores were Threat Hunter Real confidence `58` (synthetic-confidence `42`), Detection Synthetic confidence `60`, Network Real confidence `61` (synthetic-confidence `39`), and Host/EDR Real confidence `64` (synthetic-confidence `36`), average synthetic-confidence `44.25`. The prior process-only SQL and authored DMZ resolver findings did not recur; reviewers explicitly called the SQL and DNS evidence convincing. Top Loop 157 targets are Detection's tight Security 4688 ↔ Sysmon Event 1 companion timing envelope and exact lock/unlock durations, followed by Host/EDR's repeated RunMRU value/RDP cadence, Network's curated perimeter scan coverage/Snort alert neatness, and Threat Hunter's still-clean attack storyline/cleanup clustering.
  - [x] Loop 157 Windows source timing and endpoint routine variance — code fix pass committed as `923aaf6f`, broadening source-native Security/Sysmon/eCAR process-create latency profiles and adding human-shaped workstation lock/unlock duration texture. Verification passed with focused timing/baseline tests, `eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3439 passed, 15 skipped`). Regenerated eval passed at `96.7687/100` across `83,341` records; hard probes found `936` matched Security 4688/Sysmon Event 1 pairs with deltas from `-0.863s` to `+2.564s` (`386` over 1s) and `0` exact-minute-ish lock/unlock durations. Blind synthetic-confidence scores were Threat Hunter Real confidence `58` (synthetic-confidence `42`), Detection Engineer Real confidence `58` (synthetic-confidence `42`), Network Forensics Real confidence `62` (synthetic-confidence `38`), and Host/EDR Real confidence `64` (synthetic-confidence `36`), average synthetic-confidence `39.5`. The prior sub-300ms process companion timing envelope did not recur; top Loop 158 targets are too-clean attacker storyline/operator friction, repeated RunMRU/user-artifact values, curated perimeter scan portfolios, and residual rounded-looking lock/unlock polish.
  - [x] Loop 158 RunMRU/user-artifact entropy — code fix pass committed as `635bdea4`, replacing the single literal RunMRU registry artifact with a data-driven command pool and materialized RunMRU value names/commands. Verification passed with EDR/config focused tests, `eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3440 passed, 15 skipped`). Regenerated eval passed at `96.6300/100` across `82,586` records; hard probes found `6` RunMRU rows, `5` unique keys, `5` unique values, `0` repeated key/value pairs, and `0` old exact `RunMRU\\a = cmd.exe /k dir\\1` rows. Blind synthetic-confidence scores were Threat Hunter Real confidence `57` (synthetic-confidence `43`), Detection Engineer Real confidence `62` (synthetic-confidence `38`), Network Forensics Real confidence `61` (synthetic-confidence `39`), and Host/EDR Real confidence `63` (synthetic-confidence `37`), average synthetic-confidence `39.25`. The prior repeated RunMRU finding did not recur; top Loop 159 targets are over-thematic Linux sudo-denial motifs, too-clean attacker/storyline naming, curated scan campaigns, and residual audit-clear/lock sequence neatness.
  - [x] Loop 159 Linux sudo-denial and admin-history texture — code fix pass committed as `ec3b409a`, making the web/proxy sudo-denial profile sparse and less over-thematic while broadening Linux sudo/admin command texture. Verification passed with focused config/syslog tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3441 passed, 15 skipped`). Regenerated eval passed at `96.6255/100` across `76,313` records; hard probes found `261` sudo rows across `46` unique commands, `0` web/proxy sudo-denial motif rows in this seed, and `0` occurrences of the old `/etc/shadow`, `169.254.169.254`, or `grep -R password /etc` motifs. Blind synthetic-confidence scores were Threat Hunter Synthetic confidence `63`, Detection Engineer Real confidence `61` (synthetic-confidence `39`), Network Forensics Real confidence `61` (synthetic-confidence `39`), and Host/EDR Real confidence `61` (synthetic-confidence `39`), average synthetic-confidence `45.0`. The prior over-thematic sudo-denial finding did not recur. Top Loop 160 targets are the same-host PowerShell/C2/log-clear timing seam, still-too-compact attacker/storyline friction, terse DB root bash history, and scan-profile similarity.
  - [x] Loop 160 same-host C2/process timing and storyline friction — code fix passes committed as `394f2c63` and `b286044f`, first clamping storyline network evidence after visible process-create timing and then preplanning source-native process-create times before threaded emitter dispatch so follow-on storyline evidence cannot race the delayed eCAR/Sysmon/Security render times. Verification passed with focused storyline/logon tests, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3444 passed, 15 skipped`). Regenerated eval passed at `96.9788/100` across `75,604` records with Timing `96.0587`; hard probes verified `0` DC-01 PowerShell/process-to-C2 ordering inversions (`latest process create 17:41:49.673Z`, proxy manifest `17:41:51Z`, Zeek manifest/CONNECT `17:41:51.243194Z`). Blind synthetic-confidence scores were Threat Hunter Synthetic confidence `68`, Detection Engineer Real confidence `63` (synthetic-confidence `37`), Network Forensics Real confidence `61` (synthetic-confidence `39`), and Host/EDR Real confidence `62` (synthetic-confidence `38`), average synthetic-confidence `45.5`. The prior same-host C2/process timing seam did not recur in hard probes; top Loop 161 targets are repeated Linux admin-command motifs across users/hosts, SCP receiver-side file artifact timing before source-side transfer process, clean attacker/storyline naming and DB root shell context, and scan-profile similarity.
  - [x] Loop 161 Linux admin-habit and SCP transfer timing realism — code fix passes committed as `7cb829d0` and `3aa98087`, reducing high-signal Linux admin-command repetition with global per-family repeat budgets and clamping SCP receiver-side file creation after the visible source-side SCP process. Verification passed with focused bash/storyline tests, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3447 passed, 15 skipped`). Regenerated eval passed at `96.4843/100` across `78,054` records; hard probes verified high-signal Linux command families all at `<=2` appearances (`systemctl_sshd` at `1`) and APP-INT-01 SCP receiver file creation at `17:15:50.720Z` after the DB-PROD-01 source SCP process at `17:15:48.115Z`. Blind synthetic-confidence scores rose to Threat Hunter `74`, Detection Engineer `74`, Network Forensics `62`, and Host/EDR `68` (average `69.5`) because reviewers surfaced new hard endpoint causality issues, not because the Linux/SCP fixes regressed. Top Loop 162 targets are endpoint command/effect timestamp causality (`Sysmon Event 10` before Event 1 for the same process, service/group/log-clear effects before causative process rows), eCAR self-sourced remote sessions, collection-imperfection modeling, and continued storyline messiness.
  - [x] Loop 162 endpoint command/effect source-timing causality — code fix committed as `0f9c292f`, clamping storyline service/group/task/log-clear/process-access/remote-thread effects after visible process-create source times, teaching Sysmon follow-on ordering about the Event 10 `SourceProcessGUID` spelling, synchronizing Sysmon `UtcTime` after final timestamp shifts, and suppressing self-sourced eCAR session source IPs. Verification passed with focused source-timing/storyline tests, focused eCAR/Sysmon suites, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3450 passed, 15 skipped`). Regenerated eval passed at `96.5264/100` across `78,061` records; hard probes verified `0` Sysmon Event 8/10-before-Event 1 cases, `0` self-sourced eCAR USER_SESSION/LOGIN rows, and DC service/group/log-clear effects all after visible command processes. Blind synthetic-confidence scores were Threat Hunter `72`, Detection Engineer `64`, Network Forensics `72`, and Host/EDR `82` (average `72.5`). Top Loop 163 targets are Linux eCAR PID morphology, local eCAR LOGOUT rows carrying orphaned `src_port`, one-shot PowerShell parent grafting, fixed web/TLS timing buckets, Linux command/syslog pool texture, and public-certificate SAN diversity.
  - [x] Loop 163 Linux endpoint source-native realism — code fix passes committed as `b3ea6ab9`, `05a1205f`, `3fae9806`, and `faf55ab7` for host-local Linux PID morphology, local eCAR LOGOUT orphaned `src_port`, and one-shot shell parent grafting. Linux PID allocation now avoids five-minute bucket sawtooths, fits out-of-order earlier parent/child allocations below already-known future PIDs when possible, and preserves lived-in ranges; eCAR additionally normalizes Linux PID rendering in source timestamp order so endpoint source latency cannot present lower PIDs after higher ones. eCAR suppresses source ports whenever a session source IP is source-native local/unknown, and short-lived PowerShell/cmd wrappers are rejected as later unrelated parents. Verification passed with focused regressions, neighboring state/source/activity/spawn-rule suites (`333 passed`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3456 passed, 15 skipped`). Regenerated eval passed at `95.8936/100` across `85,144` records; hard probes found `0` Linux eCAR PID-backward cases, `0` eCAR LOGOUT orphan source-port rows, `0` self-sourced eCAR USER_SESSION/LOGIN rows, and `0` one-shot shell parent grafts. Blind synthetic-confidence scores were Threat Hunter Real confidence `64` (synthetic-confidence `36`), Detection Engineer `82`, Network Forensics `78`, and Host/EDR `67` (average `65.75`). Top Loop 164 targets are Windows process/network lifecycle ordering (`5156`/Zeek/Sysmon network evidence before visible same-PID process creation) and Security same-channel `4689` before `4688` termination ordering.
  - [x] Loop 164 Windows process lifecycle source ordering — code fix passes committed as `6886df61` and `49bc37bb`, clamping Windows process-owned Security 5156, Sysmon Event 3, eCAR FLOW, Zeek-correlated network evidence, and process termination source timestamps after visible same-PID process creation, including repaired browser-flow attribution paths. Verification passed with focused lifecycle/source-timing regressions, related emitter/storyline/source suites (`371 passed`), Ruff checks/format checks, full normal `uv run pytest --no-cov -q` (`3460 passed, 15 skipped`), and regenerated hard probes. Regenerated eval passed at `96.0935/100` across `85,142` records; hard probes found `0` Security 4689-before-4688 rows, `0` Security 5156-before-4688 rows, `0` Sysmon Event 3-before-Event 1 rows, and `0` Zeek-before-visible-process-create WFP tuple cases. Deliberation was skipped at the user's request. Blind synthetic-confidence scores were Threat Hunter Real confidence `68` (synthetic-confidence `32`), Detection Engineer `68`, Network Forensics `72`, and Host/EDR `72` (average `61.0`). The prior Windows lifecycle blocker did not recur. Top Loop 165 targets are Zeek HTTP/files response metadata and timestamp integrity, proxy CONNECT error body modeling, Security 1102 native subject/export semantics, and interactive terminal editor lifetimes.
  - [x] Loop 165 Zeek HTTP/files and CONNECT source-native integrity — code fix pass committed as `ea6133fd`, aligning HTTP `resp_mime_types` with visible `resp_fuids`, clamping same-UID response files after the HTTP row, preserving final HTTP timestamps for files rendering, adding proxy CONNECT error bodies, and extending interactive terminal-editor lifetimes. A follow-up eval-rule fix narrowed the old CONNECT-body co-occurrence rule to successful 200 tunnels only, with focused regression coverage. Verification passed with focused Zeek/proxy/process tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3467 passed, 15 skipped`). Regenerated eval passed at `95.4935/100` across `85,142` records; hard probes found `0` HTTP MIME-without-FUID rows, `0` missing `resp_fuid` file rows, `0` response files at/before their HTTP rows, `0` successful CONNECT bodies, `0` bodyless CONNECT error rows despite response bytes, and `0` terminal-editor lifetimes under 5 seconds. Deliberation was skipped at the user's request. Blind synthetic-confidence scores were Threat Hunter `72`, Detection Engineer `72`, Network Forensics Real confidence `67` (synthetic-confidence `33`), and Host/EDR `74` (average `62.75`). Top Loop 166 targets are Linux eCAR command lifetimes compressed to milliseconds for database/service/journal/filesystem commands, process creation after visible SSH logout, NetworkManager syslog bracketed timestamp formatting, and Sysmon Event 11 `User` propagation.
  - [x] Loop 166 Linux endpoint lifecycle and source-native syslog formatting — code fix pass committed as `d1494cec`, preserving Linux eCAR process-termination lifetimes after source latency, avoiding process telemetry after expired SSH transports, broadening database/service/journal/filesystem command duration profiles, replacing bare MySQL prompt commands with defaults-file usage, rendering NetworkManager bracket timestamps in epoch style, and propagating user context to Sysmon Event 11 file-create records. Verification passed with focused source-timing/lifecycle/syslog/Sysmon/activity tests (`362 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3475 passed, 15 skipped`). Regenerated eval passed at `95.4288/100` across `77,748` records; hard probes found `0` bare `mysql -p` commands, `0` key Linux eCAR command lifetimes under 500 ms, `0` Linux same-user eCAR process creates within 2 seconds after visible logout, `0` NetworkManager timestamp mismatches, and `0` Sysmon Event 11 rows missing `User`. Deliberation was skipped at the user's request. Blind synthetic-confidence scores were Threat Hunter `82`, Detection Engineer `62`, Network Forensics `64`, and Host/EDR `82` (average `72.5`). Top Loop 167 targets are the non-native Windows Security Event 4738 `Dummy` field, Linux `sshd` PID/auth timing morphology, eCAR process-create order preservation across Security/Sysmon/eCAR, and repeated OCSP response identity.
  - [x] Loop 167 Windows Security, SSH syslog, eCAR ordering, and OCSP response identity — code fix pass committed as `9d523c3a`, removing the non-native Event 4738 `Dummy` field, widening SSH password-auth timing to human/network scale, normalizing visible `sshd` child PIDs in final syslog order, preserving eCAR canonical process-create order, and stabilizing repeated OCSP HTTP/file response profiles. Verification passed with focused Windows/syslog/SSH/eCAR/OCSP tests, related source/syslog/dispatcher suites (`246 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3478 passed, 15 skipped`). Regenerated eval passed at `96.0331/100` across `84,089` records; hard probes found `0` Event 4738 Dummy fields, `0` sshd PID regressions, SSH password auth min/median `462 ms`/`2142 ms` with `0` under 400 ms, eCAR PSEXESVC PowerShell before wevtutil restored, and `0` repeated OCSP response-size instability groups. Deliberation was skipped at the user's request. Blind synthetic-confidence scores were Threat Hunter `78`, Detection Engineer Real confidence `72` (synthetic-confidence `28`), Network Forensics `78`, and Host/EDR `86` (average `67.5`). Top Loop 168 targets are foreground Linux shell/process lifecycle serialization, eCAR create-after-Sysmon-terminate ordering, stable Core/DMZ Zeek sensor timing offsets, `debian-sa1 1 1` lifetimes, 4738 account-change schema warnings, and SMB filename entropy.
  - [x] Loop 168 Linux foreground lifecycle, eCAR process-order, and Zeek sensor timing — code fix passes committed as `a3fa233e`, `84bb4126`, `73c3252c`, and `6b95d76f`, serializing foreground shell children under the same bash actor, preventing eCAR creates from rendering after same-process termination evidence, replacing per-record Core/DMZ jitter with stable sensor/path offsets, giving `debian-sa1 1 1` realistic lifetimes, and cleaning up residual 4738 schema warnings. Verification passed with focused lifecycle/eCAR/sysstat/Zeek tests, related source/activity/eCAR suites (`269 passed`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3481 passed, 15 skipped`). Regenerated eval passed at `95.7315/100` across `78,907` records; hard probes found `0` foreground shell overlaps, `0` eCAR create-after-Sysmon-terminate cases, `0` `debian-sa1` lifetimes under 1 second, `0` Event 4738 warnings, and Core/DMZ duplicate tuple offsets constrained to `16.422-16.998 ms`. Deliberation was skipped at the user's request. Blind synthetic-confidence scores were Threat Hunter `68`, Detection Engineer `72`, Network Forensics `78`, and Host/EDR `66` (average `71.0`). Top Loop 169 targets are Kerberos KDC event versus Zeek UDP packet-count accounting, generated microsecond suffixes in scan bursts, missing Apache reverse-shell web trigger evidence, and remaining too-clean narrative/background-density tells.
  - [x] Loop 169 Kerberos packet accounting and network/storyline timing — code fix passes committed as `c613ac33`, `6e31ce16`, `75e4d5c5`, and `b2e1e628`, raising Kerberos Zeek packet/history floors for nearby KDC audit groups, preserving connection-driven KDC audit counting, preventing recent Kerberos audit port reuse, diversifying scan-burst submillisecond timing, and preserving inbound web VIPs so Apache trigger evidence renders. Verification passed with focused Kerberos/activity/source tests, `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3487 passed, 15 skipped`). Regenerated eval passed at `94.8604/100` across `84,406` records; hard probes found `0` Kerberos audit/Zeek packet cluster contradictions across `1,103` clusters, `291` unique scan-burst submillisecond suffixes across `343` rows, `1` Apache upload web-access trigger, `0` `debian-sa1` lifetimes under 1 second, and `0` Event 4738 dummy-field rows. Deliberation was skipped at the user's request. Blind synthetic-confidence scores were Threat Hunter `72`, Detection Engineer `67`, Network Forensics `73`, and Host/EDR `72` (average `71.0`). Top Loop 170 targets are Linux `systemd-logind` session records lacking concrete PAM/session initiators, DNS C2 response labels that encode TTL/generator semantics, quantized dual-Zeek sensor offsets, repeated bash-history command pools, Windows Security 4624 native manifest field order, and eCAR FLOW port typing.
  - [x] Loop 170 Linux session source modeling and DNS/network artifact realism — code fix passes committed as `cb920b33`, `0eddcdd0`, and `74ae3c85`, removing DNS TTL/generator-like answer labels, diversifying dual-Zeek sensor offsets, preserving baseline PAM/logind opener ordering, and backfilling native PAM session open rows for otherwise-orphaned `systemd-logind` sessions. Verification passed with focused DNS/Zeek/syslog regressions, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3492 passed, 15 skipped`). Regenerated eval passed at `95.6112/100` across `78,398` records; hard probes found `0` DNS TTL-label matches, `0` orphaned `systemd-logind` new-session rows, `0` non-auth logind priorities, and diverse Core/DMZ offsets. Deliberation was skipped at the user's request. Blind synthetic-confidence scores were Threat Hunter `64`, Detection Engineer `68`, Network Forensics `63`, and Host/EDR `74` (average `67.25`). The prior PAM/logind, DNS TTL-label, and quantized sensor-offset tells did not recur. The refreshed attacker-messiness prompt had partial effect: the scenario now contains more fumbles and false starts, but reviewers still saw a compact, tidy, training-shaped story. Top Loop 171 targets are Linux per-host interface identity drift (`ens160`/`eth0`), absent AD SRV DNS discovery around Kerberos/LDAP, DNS AAAA/CNAME morphology, class-sensitive process lifetimes, repeated registry value vocabulary, and endpoint software-stack uniformity.
  - [x] Loop 171 Linux interface identity and AD/DNS realism — code fix pass committed as `8e65458d`, adding deterministic per-host Linux interface selection, applying it to DHCP/UFW/NetworkManager/systemd-resolved syslog, and adding cached AD SRV discovery before internal forced A/AAAA prerequisites around Kerberos/LDAP. Verification passed with `uv run eforge validate-config`, focused DNS/syslog/baseline tests, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3495 passed, 15 skipped`). Regenerated eval passed at `95.4399/100` across `84,033` records; hard probes found `0` mixed-interface Linux hosts, `113` AD SRV NOERROR answers from `14` sources, `0` DNS TTL-label matches, `0` orphaned `systemd-logind` new-session rows, and diverse Core/DMZ offsets, while flagging `187/189` non-canonical AAAA answer rows as a remaining DNS morphology issue. Deliberation was skipped at the user's request. Blind synthetic-confidence scores were Threat Hunter `82`, Detection Engineer `72`, Network Forensics `78`, and Host/EDR `86` (average `79.5`). The prior Linux interface drift and missing AD SRV discovery did not recur, but reviewers surfaced new hard source-native tells: Security 4688 before Sysmon Event 1 / ProcessGuid start for same PID/command, Linux cron/sudo PROCID lifecycle semantics, one-off public TLS clients marked resumed, smooth clock-derived Windows LogonID slopes, proxy User-Agent/domain vendor mismatches, Windows 4698 versioning on modern hosts, public PTR mirroring, and repeated bash command/typo pools.
  - [x] Loop 172 source-native timing, Linux syslog PID semantics, and TLS/proxy statefulness — code fix passes committed as `fad4bc09` and `c1bc96be`, aligning Security/Sysmon/eCAR process-create timing around canonical process start, normalizing source-native sudo PAM/COMMAND/close ordering, using transient cron/sudo PIDs, requiring prior visible TLS client/server state for resumption, and binding proxy vendor User-Agents to compatible domains. Verification passed with `uv run eforge validate-config`, focused timing/syslog/baseline/storyline tests (`113 passed, 1 skipped`), Ruff checks/format checks, full normal `uv run pytest --no-cov -q` (`3500 passed, 15 skipped`), regenerated data, and hard probes. Regenerated eval passed at `94.9540/100` across `86,386` records; hard probes found `0` Security 4688-before-Sysmon Event 1 rows, `0` sudo COMMAND rows missing same-PID PAM open-before or close-after, `0` multi-user cron PIDs, `0` proxy vendor UA/domain mismatches, and `0` public inbound one-off/single-source TLS resumed sessions, while DNS AAAA non-canonical rows remain (`200/267`). Deliberation was skipped at the user's request. Blind synthetic-confidence scores were Threat Hunter `78`, Detection Engineer `64`, Network Forensics `68`, and Host/EDR `84` (average `73.5`). The prior cron/sudo PID, Security-before-Sysmon, proxy vendor mismatch, and one-off TLS resumption tells did not recur, but reviewers surfaced new high-leverage issues: same-channel Windows account-management events before causative 4688 process creates, eCAR SSH success before syslog Accepted, the fixed `250ms` Security-after-Sysmon gap becoming a timing fingerprint, dual-Zeek sensor timing/byte symmetry, repeated exact TLS `1.2` second durations, repeated bash-history command pools, repeated `Microsoft Update Health Tools` registry values, and DNS tunnel response grammar regularity.
  - [x] Loop 173 Windows account/SSH source ordering and timing fingerprint cleanup — code fix pass committed as `3de3d26c`, adding data-driven source timing for Windows account-management side effects, jittered Security-after-Sysmon process-create gaps, and eCAR SSH session timing that follows visible syslog Accepted/PAM evidence even under observation delay. Verification passed with `uv run eforge validate-config`, focused timing/SSH regressions, Ruff checks/format checks, full normal `uv run pytest --no-cov -q` (`3502 passed, 15 skipped`), regenerated data, and hard probes. Regenerated eval passed at `94.9543/100` across `86,395` records; hard probes found `0` account-management-before-nearest-`net user` 4688 rows, `0` Security 4688-before-Sysmon Event 1 rows, `0` eCAR SSH LOGIN-before-syslog Accepted rows, `0` sudo same-PID open/close gaps, `0` cron multi-user PIDs, and `0` proxy vendor UA/domain mismatches. Deliberation was skipped at the user's request. Blind verdicts/synthetic-confidence scores were Threat Hunter `Real` at 68 confidence (synthetic-confidence `32`), Detection Engineer `78`, Network Forensics `72`, and Host/EDR `78` (average synthetic-confidence `65.0`). The prompt/storyline messiness changes appear to have had the intended partial effect: the threat hunter called the dataset real and praised the huntable cross-source story, while remaining reviewers shifted to lower-level source-native tells rather than the prior "too clean attacker story" as the decisive issue. Detailed loop artifacts were saved under `scenarios/iteration-test/blind-test/loop-173/`. Top next targets are TLS certificate-chain issuer/subject validity (`19` confirmed mismatches), DNS TTL/cache semantics (`63` large-variance RRsets confirmed), Sysmon ProcessGuid/LogonGuid morphology, more bidirectional provider timing variance without causal inversions, focused eCAR flow ownership/source-port reuse investigation for local Docker-like commands, and continued reduction of narratively tidy attacker artifact names.
- [x] Up-to-10 current-dev assessment continuation — completed 10 iterative EvidenceForge realism loops from the latest calibrated iteration-test state, fixed the highest-leverage verified findings, committed each completed fix pass, regenerated/evaluated, and preserved loop artifacts.
  - Loop 1 baseline eval completed at `93.89` across `107,377` records; blind synthetic-confidence scores were Threat Hunter `76`, Detection `82`, Network `82`, Host/EDR `86`.
  - Loop 1 fix pass completed and verified: fixed external CIDR-only segment scan target resolution, coherent SSH syslog and Zeek UID observation decisions, OS-aware TLS destination filtering for Windows update/trust-list domains, and Let's Encrypt RSA/ECDSA chain templates. Verification passed with `uv run eforge validate-config`, focused regressions (`11 passed` plus the adjusted certificate regression), `uv run ruff check .`, `uv run ruff format --check .`, and full normal `uv run pytest --no-cov -v` (`3057 passed, 37 skipped`).
  - Loop 2 regeneration/eval completed at `93.80` JSON overall (`94/100` human-readable) across `116,087` records. Hard probes found zero SSH ordering violations, zero Zeek UID gaps, zero Let's Encrypt R3/X2 mismatches, and zero non-Windows `windowsupdate.com` proxy rows. Blind synthetic-confidence scores were Threat Hunter `76`, Detection `78`, Network `68`, Host/EDR `76` (average `74.5`).
  - Loop 2 fix pass completed and verified: stabilized per-host deployed web asset cache-buster tokens, made health/status endpoint response sizes small and stable, scoped health-check visitor profiles to server/domain-controller sources, reduced machine-account DC Kerberos volume, skewed service class selection, and fixed member-server SPN targeting for source-native `SMB`/`file_server` names. Verification passed with `uv run eforge validate-config`, focused web/Kerberos regressions, `uv run ruff check .`, `uv run ruff format --check .`, and full normal `uv run pytest --no-cov -q` (`3064 passed, 37 skipped`). Regenerated eval passed at `95.19` JSON overall across `92,476` records; probes found max static asset variants per host `2`, zero workstation-sourced health-check rows, max health response size `589` bytes, and DC-local Kerberos 4769 rows reduced to `68/491`.
  - Loop 3 blind review completed against `/private/tmp/eforge-assess-loop3-neutral-b476c16/data`. Blind synthetic-confidence scores were Threat Hunter `62`, Detection `68`, Network `72`, Host/EDR `72` (average `68.5`). Top verified fix targets are the DC service-logon + `explorer.exe` parent contradiction, Zeek HTTP `trans_depth > 1` on one-request unique-UID connections, repeated Linux syslog daemon lines, and role-drifted bash command pools.
  - Loop 3 fix pass completed and verified: service-logon sessions now stay service-shaped instead of spawning Explorer children, process telemetry carries LogonType 5 token/session semantics, one-request HTTP connections clamp Zeek `trans_depth` to 1, Linux eCAR command rendering no longer quotes expandable glob tokens, DB host shell noise avoids web-admin command pools, and high-repeat syslog daemon messages have reduced weights. Verification passed with `uv run eforge validate-config`, focused activity/spawn/config regressions (`269 passed, 1 skipped` plus focused new tests), `uv run ruff check .`, `uv run ruff format --check .`, and full normal `uv run pytest --no-cov -q` (`3069 passed, 37 skipped`).
  - Loop 4 regeneration/eval hard-probe follow-up completed and verified: the first post-`7e6c7ea` regeneration passed automated eval at `95.30` across `86,698` records and fixed the service-logon/Zeek-depth/quoted-glob probes, but hard probing still found DB-host `apache2` bash/eCAR commands caused by username-only bash tool-affinity caching. Fixed the cache to scope affinity by user plus command pool so web-admin tool preferences cannot leak into DB sessions. Verification passed with focused regressions, `uv run eforge validate-config`, related activity/spawn/expert/baseline tests (`246 passed, 1 skipped`), Ruff checks, and full normal `uv run pytest --no-cov -q` (`3070 passed, 37 skipped`).
  - Loop 4 regeneration/eval second hard-probe follow-up completed and verified: post-`02bd4bd` regeneration passed automated eval at `95.71` across `87,420` records and kept the service-logon/Zeek-depth/quoted-glob probes clean, but two DB-host `apache2` commands remained via global `{service}` template substitution. Fixed service placeholders to prefer the current host's service list before falling back to global service names. Verification passed with focused regressions, `uv run eforge validate-config`, related activity/spawn/expert/baseline tests (`247 passed, 1 skipped`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3071 passed, 37 skipped`).
  - Loop 4 final regeneration/eval completed from commit `67ac00d`: regenerated unchanged `scenarios/iteration-test/scenario.yaml`, quantitative eval passed at `95.94` JSON overall (`96/100` human-readable) across `89,334` records, and hard probes found zero HTTP `trans_depth > 1` rows, zero service-logon Explorer/medium-integrity/nonzero-terminal contradictions, zero quoted-glob commands, and zero DB-host `apache2`/`certbot`/`nginx reload`/`ab` web-admin command hits.
  - Loop 4 blind review completed against `/private/tmp/eforge-assess-loop4-neutral-67ac00d/data`. Blind synthetic-confidence scores were Threat Hunter `78`, Detection `82`, Network `82`, Host/EDR `78` (average `80.0`). Top next fix targets are canonical process-to-network HTTP client mismatch (`api.example.com/status` command lines paired with unrelated proxy/Zeek hosts and user agents), repeated Linux SSH/logind ordering and exact password-auth timing artifacts, Windows eCAR thread-ID morphology, and Zeek protocol timing/lifecycle bounds.
  - Loop 4 blind-review follow-up fix pass completed and verified: command-line HTTP clients now drive canonical HTTP/proxy/TLS destination metadata so curl/wget-style eCAR process rows do not point at unrelated proxy/Zeek hosts or user agents, including the post-regeneration hard-probe catch where stale effect context retargeted a rendered curl command to a wget-style proxy destination; Linux SSH compound syslog timing uses source-native subsecond connection/PAM/logind spacing instead of exact one-second triads; Windows eCAR process-owned TIDs align to Windows allocation morphology; Zeek certificate files/x509 analyzer rows are bounded inside the owning connection lifetime; sudo baseline noise uses auth/authpriv-style facility semantics. Verification passed with focused regressions, `uv run eforge validate-config`, related unit files (`339 passed, 1 skipped`), Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3076 passed, 37 skipped`).
  - Loop 5 regeneration/eval hard-probe follow-up fix pass completed and verified: regeneration from `508501c` passed quantitative eval at `95.70` JSON overall (`96/100` human-readable) across `83,915` records, and hard probes kept Zeek `trans_depth`, service-logon parentage, DB role drift, Windows eCAR TID shape, Zeek analyzer timing bounds, and SSH ordering clean. The same probes caught remaining curl/proxy host mismatches caused by profile-driven traffic being attributed to a still-running one-shot CLI process; fixed canonical connection generation to derive HTTP/proxy destination metadata from the owning process command when an attributed PID has a concrete HTTP URL. Verification passed with focused CLI/proxy tests, related activity/source tests (`340 passed, 1 skipped`), `uv run eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3077 passed, 37 skipped`).
  - Loop 5 final regeneration/eval completed from commit `811b2f1`: regenerated `scenarios/iteration-test/scenario.yaml`, quantitative eval passed at `95.26` JSON overall (`95/100` human-readable) across `93,567` records, and hard probes found zero CLI/proxy host mismatches, zero `api.example.com` remnants, zero HTTP `trans_depth > 1` rows, zero service-logon Explorer/medium-integrity/nonzero-terminal contradictions, zero DB-host web-admin command hits, zero expandable Linux quoted-glob commands, zero Windows eCAR TID morphology violations, zero Zeek file/x509 connection-bound violations, and zero SSH exact-one-second/order violations.
  - Loop 5 blind review completed against `/private/tmp/eforge-assess-loop5-neutral-811b2f1-20260515T2109/data`. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `72`, Network `78`, Host/EDR `78` (average `75.0`). Top next fix targets are duplicate Linux `systemd-logind` session removals/SSH lifecycle state, web asset-before-document ordering plus cache behavior, Windows WinSxS component path/build identity, PsExec service-process lineage/privilege semantics, and lower-priority endpoint observation smoothness/command-pool variance.
  - Loop 6 fix pass completed and verified: duplicate pre-window `systemd-logind`
    removals now receive unique rendered session IDs, web page-asset fanout timing
    stays beyond source-observation delay windows, repeated fingerprinted web assets
    are served from browser cache instead of repeatedly hitting the server, standalone
    proxy static-asset requests no longer claim unseen same-origin page referrers,
    and TiWorker WinSxS component paths/metadata resolve by host Windows build.
    Verification passed with `uv run eforge validate-config`, focused
    syslog/web/proxy/TiWorker regressions, `uv run ruff check .`,
    `uv run ruff format --check .`, and full normal `uv run pytest --no-cov -q`
    (`3082 passed, 37 skipped`).
  - [x] Loop 6 hard-probe follow-up: one residual rendered Zeek DMZ static asset
    still appeared before its same-origin page after source-native conn/http offsets,
    so widened the data-driven web asset fanout windows beyond the combined Zeek
    observation offsets. Verification passed with focused timing/cache regressions,
    `uv run eforge validate-config`, Ruff checks/format checks, and full normal
    `uv run pytest --no-cov -q` (`3082 passed, 37 skipped`).
  - Loop 6 final regeneration and assessment completed from commit `ac30094`:
    regenerated `scenarios/iteration-test/scenario.yaml`, quantitative eval passed at
    `96/100` across `76,801` records, and hard probes found zero duplicate logind
    removals, zero page-asset ordering inversions, zero repeated full static 200s,
    and zero TiWorker component/version mismatches. Blind synthetic-confidence scores
    were Threat Hunter `74`, Detection `74`, Network `72`, Host/EDR `82`
    (average `75.5`). Top Loop 7 targets are PsExec/service-control process lineage
    and token semantics, one-shot CLI/eCAR process lifetimes, proxy cache behavior
    for immutable third-party static assets, stale Let's Encrypt OCSP responder
    mapping, and repeated Linux daemon/syslog texture.
  - [x] Loop 7 fix pass: routed recent PsExec/HealthMonitor storyline follow-on
    command utilities through the installed service wrapper with SYSTEM/0x3e7
    identity, normalized `%SystemRoot%` service images for wrapper reuse, and
    shortened one-shot curl/wget/cmd `/c`/PowerShell command lifetimes while
    leaving interactive shells unbounded. Hard-probe follow-ups also reject expired
    Linux one-shot process owners for later network/proxy attribution and start
    explicit-proxy one-shot clients near the request time, then terminate bounded
    foreground process owners after observed network activity; this prevents stale
    `curl` PIDs from stretching across unrelated requests or remaining open.
    Verified with focused regressions, related activity/storyline/spawn tests
    (`213 passed`), config validation, Ruff, format check, and full normal
    `uv run pytest --no-cov -q` (`3092 passed, 37 skipped`). Regeneration and
    blind review follow.
  - [x] Loop 7 hard-probe follow-up: one late Linux bash/eCAR
    `curl -sI https://localhost` process remained open despite being a bounded
    foreground command. Added a finalization backstop for tracked one-shot shell
    process telemetry before emitters close while preserving commands whose
    expected termination falls beyond the visible window. Verification passed
    with focused lifetime/storyline/proxy regressions, config validation, Ruff,
    format check, and full normal `uv run pytest --no-cov -q`
    (`3094 passed, 37 skipped`). Regeneration and blind review follow.
  - Loop 7 final regeneration and assessment completed from commit `6374303`:
    regenerated `scenarios/iteration-test/scenario.yaml`, quantitative eval passed
    at `96/100` across `76,838` records, and hard probes found zero PsExec service
    semantic violations, zero one-shot CLI lifetimes over 120 seconds, zero
    unexplained one-shot terminate gaps after accounting for `enterprise_standard`
    eCAR observation drops, zero duplicate or regressing `systemd-logind` removals,
    zero web asset-before-referrer inversions, and zero repeated full static 200s.
    Blind synthetic-confidence scores were Threat Hunter `72`, Detection `68`,
    Network `74`, Host/EDR `76` (average `72.5`). Top Loop 8 targets are
    PsExec/security service-install source-native fields (`ServiceStartType`),
    HTTP/proxy status-outcome texture, Linux bash/sudo command-pool realism, DNS
    tunnel response grammar/TTL diversity, and attack-phase pivot continuity.
  - [x] Loop 8 fix pass: corrected PsExec/service-control Windows 4697
    service-install source-native fields by defaulting generated service
    installs to demand/manual `ServiceStartType=3`, preserving explicit
    `sc create ... start= auto`, and validating with focused service tests,
    config validation, Ruff, format check, and full normal
    `uv run pytest --no-cov -q` (`3095 passed, 37 skipped`). Regeneration and
    blind review follow.
  - [x] Loop 8 regeneration, hard probes, quantitative eval, and blind review
    completed from commit `e768f4c`: automated eval passed at `96/100` across
    `76,837` records, hard probes found zero PSEXESVC service-start or
    Security/Sysmon lineage violations (`ServiceStartType=3`, SYSTEM children
    under `PSEXESVC.exe`), and blind synthetic-confidence scores were Threat
    Hunter `72`, Detection `64`, Network `78`, Host/EDR `80` (average `73.5`).
    Top Loop 9 targets are HTTP/proxy status-outcome cleanliness, Linux
    bash/syslog command-pool repetition, and rare Windows admin-tool overuse.
  - [x] Loop 9 fix pass: added realistic HTTP/proxy status-outcome texture by
    carrying sampled browser request statuses through `BrowsingRequest` into
    generated `HttpContext`, setting status-aware response sizes/MIME fan-out,
    and raising proxy CONNECT terminal-failure variance. Isolated browsing
    sessions now sample roughly 75% `200` with `304`, `206`, `404`, `403`, and
    5xx outcomes in the tail. Verified with config validation, focused
    web/proxy tests, Ruff, format check, and full normal
    `uv run pytest --no-cov -q` (`3097 passed, 37 skipped`). Regeneration and
    blind review follow.
  - [x] Loop 9 regeneration, HTTP/proxy hard probes, quantitative eval, and
    blind review completed from commit `46bd9d2`: automated eval passed at
    `95.96` JSON overall (`96/100` human-readable) across `78,166` records,
    hard probes confirmed the status-texture gate now passes with proxy `200`
    ratio `78.3%` and Zeek HTTP `200` ratio `93.0%`, and blind
    synthetic-confidence scores were Threat Hunter `70`, Detection `66`,
    Network `68`, Host/EDR `82` (average `71.5`). Top Loop 10 target is
    rare Windows admin-tool overuse: generated eCAR still contains 21 bare
    `ntdsutil.exe` launches on DC-01 under `services.exe` and 65
    `dsquery.exe` process creates across Windows hosts; Linux bash/syslog
    command-pool repetition remains the next broad target.
  - [x] Loop 10 fix pass completed and verified: removed `ntdsutil.exe` from
    generic domain-controller service-process texture, added data-driven
    application selection weights, restricted `dsquery.exe` to DC generic
    selection, and enforced catalog `system_types` in the WorldPlanner
    process-owner path so workstation/file-server LDAP-like traffic cannot
    spawn forbidden admin tools. Verification passed with config validation,
    focused app/catalog/world-model regressions, Ruff, format check, and full
    normal `uv run pytest --no-cov -q` (`3100 passed, 37 skipped`).
    Regeneration and final assessment from commit `f0f5c3d` passed automated
    eval at `96/100` across `80,616` records; hard probes found zero eCAR
    process-create rows for `ntdsutil.exe`, `dsquery.exe`, `repadmin.exe`, or
    `nltest.exe`, preserved proxy status texture (`200` ratio `80.6%`) and
    Zeek HTTP status texture (`200` ratio `91.6%`), and blind
    synthetic-confidence scores were Threat Hunter `68`, Detection `62`,
    Network `66`, Host/EDR `74` (average `67.5`). Best next target for a
    future batch is Linux bash/syslog command-pool repetition, followed by
    Zeek HTTP `CONNECT` source-visibility semantics and richer proxy cache
    behavior.
- [x] Current-dev assessment continuation loops 11-20 — continued the iterative EvidenceForge realism loop from Loop 10 through Loop 20, fixing the highest-leverage verified blind-review findings each pass and committing each completed fix before regeneration/review.
  - [x] Loop 11 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `e9ff69c`: diversified Linux bash/syslog command texture with per-generation bash command memory, expanded common command tails, and data-driven sudo syslog placeholder pools. Verification passed with focused regressions, `eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3102 passed, 37 skipped`). Regenerated eval passed at `95.83/100` across `78,559` records; hard probes showed max bash exact repeat dropped from `21` to `8` and max sudo exact repeat from `28` to `2`. Blind synthetic-confidence scores were Threat Hunter `82`, Detection `84`, Network `74`, Host/EDR `88` (average `82.0`), indicating deeper concrete defects surfaced after the command-pool tell was reduced. Top Loop 12 targets are eCAR read-only command file-create artifacts, DHCP syslog ordering, Linux systemd parent PID ownership, Windows `root` identity bleed/4624 caller-process semantics, web static response byte/MIME state, and ICMP echo byte symmetry.
  - [x] Loop 12 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `b7c8a70`: repaired source-native host contradictions from the loop-11 panel, including read-only command output parsing, Linux PID 1 systemd ownership, DHCP client lifecycle ordering, Windows explicit-credential subject coercion, and 4624 caller-process semantics. Verification passed with focused regressions (`318 passed, 1 skipped`), `eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3110 passed, 37 skipped`). Regenerated eval passed at `96.17/100` across `79,317` records; hard probes showed zero dash-prefixed read-only file artifacts, zero Linux systemd parent PID violations, zero Windows `root` identity mentions, zero 4624 caller-process mismatches, and zero DHCP ACK-before-REQUEST ordering failures. Blind synthetic-confidence scores were Threat Hunter `76`, Detection `76`, Network `68`, Host/EDR `74` (average `73.5`), with top Loop 13 targets now static web/Zeek HTTP response semantics, native Sysmon Event 1/GUID fidelity, dual-Zeek sensor observation determinism, DC 4776 workstation attribution, and pooled host command/daemon texture.
  - [x] Loop 13 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `93463e4`: repaired static web/Zeek HTTP response semantics so cacheable hashed/static assets keep stable source-native content lengths and zero-body responses no longer render as successful `200` objects with concrete MIME bodies. Verification passed with focused HTTP/browsing/web tests (`69 passed, 1 skipped`), the broader HTTP/proxy/emitter slice (`286 passed, 1 skipped`), `eforge validate-config`, Ruff checks/format checks, and full normal `uv run pytest --no-cov -q` (`3117 passed, 37 skipped`). Regenerated eval passed at `96.64/100` across `82,065` records; hard probes showed zero static `200` zero-body rows, zero bad `304` body/MIME rows, zero zero-body `206` rows, zero refined web static `200` unstable-size groups, and zero Zeek static `200` unstable-size groups. Blind synthetic-confidence scores were Threat Hunter `76`, Detection `68`, Network `72`, Host/EDR `82` (average `74.5`), and the prior web/static-body issue disappeared from reviewer findings. Top Loop 14 targets are SSH command target/network-destination contradictions, Zeek UDP/53 DNS-service zero-payload rows, native Kerberos 4624 `WorkstationName` semantics, Sysmon provider thread-ID distribution, and developer-tool current-directory realism.
  - [x] Loop 14 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `7a82449`: repaired SSH command-to-network destination ownership, preserved request payload for Zeek UDP/53 DNS-service failures, added native variance to Kerberos type 3 4624 `WorkstationName`, made Sysmon provider ThreadID selection less round-robin, and moved developer-tool `CurrentDirectory` values into user project directories. Verification passed with focused tests (`6 passed`), broader related slice (`350 passed`), `eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3122 passed, 37 skipped`). Regenerated eval passed at `96.60/100` across `81,716` records; hard probes found zero UDP/53 DNS zero/small request-payload rows, zero SSH command/network mismatches, `26.8%` blank/dash Kerberos type 3 `WorkstationName`, zero perfectly balanced Sysmon ThreadID hosts, and zero developer-tool install/system current directories. Blind synthetic-confidence scores were Threat Hunter `76`, Detection `78`, Network `84`, Host/EDR `78` (average `79.0`). Top Loop 15 targets are Windows account-state contradictions after lockout/disabled failures, ICMP and ASA/Zeek source-native network contradictions, user-session process ownership for `sihost.exe`, high-volume Linux daemon boilerplate, bash cadence/typo texture, proxy HTTPS semantics, and OneDrive Sysmon Event 7 version metadata.
  - [x] Loop 15 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `9c9dcef`: enforced source-native auth state semantics so enabled known accounts fail as bad passwords, disabled synthetic accounts fail as disabled, and unknown targets fail as no-such-user; preserved ICMP echo payload/IP-byte accounting across Zeek sensors; and made ASA successful TCP teardown reasons agree with Zeek `SF`/FIN semantics. Verification passed with focused auth/Zeek/ASA tests (`90 passed`), broader related slice (`302 passed`), `eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3127 passed, 37 skipped`). Regenerated eval passed at `95.52/100` across `83,251` records; hard probes found zero locked/disabled-before-success auth violations, zero bad ICMP echo accounting rows, and zero ASA reset-to-Zeek-`SF` mismatches. Blind synthetic-confidence scores were Threat Hunter `74`, Detection `68`, Network `72`, Host/EDR `64` (average `69.5`). Top Loop 16 targets are Windows `.cmd`/`.bat` process-image contradictions, Zeek interval float precision, Linux journald/syslog boilerplate volume, Sysmon ProcessGuid embedded-time clustering, active TCP source-port reuse, Mimikatz LSASS remote-thread over-modeling, Sysmon Event 3/Event 10 texture, and bash command/cadence repetition.
  - [x] Loop 16 fix pass completed and verified: Windows `.cmd`/`.bat` process launches now canonicalize to the real `cmd.exe` host before state/event creation, shared Zeek JSON rendering rounds float values to microsecond precision, and Linux `systemd-journald` storage-accounting boilerplate takes a smaller share of ambient syslog noise. Verification passed with focused regressions, broader process/catalog/Zeek/syslog slices, `eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3128 passed, 37 skipped`). Regeneration and blind review follow.
  - [x] Loop 16 regeneration, hard probes, quantitative eval, and blind review completed from commit `21f3a79`: regenerated output passed automated eval at exact `96.09/100` across `81,173` records; hard probes confirmed zero Windows/eCAR `.cmd`/`.bat` process-image rows, zero Zeek interval precision violations with max six decimal places, journald at `419/9,101` syslog rows (`4.6%`), and no active TCP source-port overlaps in this regenerated dataset. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `76`, Network `82`, Host/EDR `78` (average `77.0`). Top Loop 17 targets are Zeek UDP/IP byte arithmetic and paired DNS payload contradictions, Windows lock/unlock binding to a Type 3 network-logon LUID, and Linux SSH/syslog microsecond suffix preservation.
  - [x] Loop 17 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `eaf090a`: repaired Zeek UDP/DNS packet accounting and cross-sensor DNS payload consistency, blocked workstation lock/unlock/Type 7 unlock generation from Type 3/5 sessions, selected interactive/RDP sessions for storyline lock/unlock events, and added source-local sub-millisecond jitter for SSH syslog lifecycles across baseline, explicit SSH, and SCP receiver artifacts. Verification passed with focused regressions, broader related slices (`406 passed, 13 skipped`), `eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3131 passed, 37 skipped`). Regenerated eval passed at exact `96.09/100` across `81,173` records; hard probes found zero UDP/DNS IP-byte violations, zero cross-sensor DNS payload mismatches, zero Type 3/5 LUID lock/unlock violations, and zero repeated low-microsecond SSH lifecycle suffix groups. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `74`, Network `72`, Host/EDR `68` (average `71.5`). Top Loop 18 targets are exact `0.8` Zeek TLS duration clustering, proxy HTTPS source-mode semantics, Sysmon/Security LogonGuid fidelity, SCP receiver-side eCAR process attribution, bash-history authoredness, and SMB/REJ statistical polish.
  - [x] Loop 18 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `bc738f2`: repaired the high-volume exact `0.8` Zeek TLS/web duration floor by adding deterministic post-floor texture in both generator-owned TLS durations and Zeek render-time fallback floors. Verification passed with focused TLS/activity tests, broader Zeek/activity/timing slices (`289 passed, 13 skipped`), `eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3131 passed, 37 skipped`). Regenerated eval passed at exact `96.09/100` across `81,173` records; hard probes found zero exact `0.8` TLS rows, zero rows in the `0.800`-`0.801` band, max repeated TLS duration bucket of `2`, and preserved prior UDP/DNS, cross-sensor DNS, Windows LUID, and SSH syslog gates. Blind synthetic-confidence scores were Threat Hunter `76`, Detection `72`, Network `74`, Host/EDR `64` (average `71.5`). Top Loop 19 targets are HTTP/proxy source-native response semantics, cross-sensor Zeek timing-band regularity, public TLS/web long-tail texture, same-LUID Security/Sysmon LogonGuid consistency, and bash/host authoredness.
  - [x] Loop 19 fix, regeneration, hard probes, quantitative eval, and blind review completed from commits `dc4616c` and `b4c99b1`: repaired HTTP/proxy source-native response semantics by blocking Windows Update Agent from Linux package paths, OS-gating `packages.microsoft.com` URI/UA selection, normalizing redirect/error HTTP MIME to source-native HTML bodies, and preventing HTTP file-transfer fan-out from reintroducing asset MIME on redirects/errors. Verification passed with focused HTTP/proxy/Zeek tests, broader related slices (`269 passed`), `eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3134 passed, 37 skipped`). Regenerated eval passed at exact `96.23/100` across `90,613` records; hard probes found zero Windows Update Agent + Ubuntu package path violations, zero redirect/error asset-MIME violations, zero exact `0.8` TLS rows, and zero rows in the `0.800`-`0.801` TLS duration band. Blind synthetic-confidence scores were Threat Hunter `76`, Detection `76`, Network `76`, Host/EDR `82` (average `77.5`). Top Loop 20 targets are user-shell/UWP processes incorrectly modeled as SYSTEM/session 0, duplicate `explorer.exe` shells from one `userinit.exe`, Zeek HTTP connection reuse, Snort TLS-failure vs Zeek established-TLS conflicts, and Linux `scp` eCAR lifecycle coverage.
  - [x] Loop 20 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `76bc107`: repaired user-shell/UWP process ownership by removing desktop helpers from system-service pools, rerouting any remaining `sihost.exe`, `RuntimeBroker.exe`, `backgroundTaskHost.exe`, and `SearchHost.exe` system-process selections into the active interactive session, and reusing the per-session `explorer.exe` shell instead of emitting duplicate primary shell creates. Verification passed with focused regressions, related activity/system-process/spawn-rule tests (`236 passed`), `eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3138 passed, 37 skipped`). Regenerated eval passed at exact `96.25/100` across `79,758` records; hard probes found zero shell/UWP SYSTEM/session-0 violations across Security, Sysmon, and eCAR, zero duplicate primary `explorer.exe` create clusters, and preserved prior Windows Update/Ubuntu, redirect/error MIME, and TLS duration gates. Blind synthetic-confidence scores were Threat Hunter `72`, Detection `74`, Network `82`, Host/EDR `70` (average `74.5`). Top Loop 21 target is the concrete Windows 5156/eCAR browser-flow attribution defect where browser-like Zeek HTTP rows join to host sockets/processes attributed to `svchost.exe`; next targets are Linux syslog/bash cadence, public DNS/X.509 corpus realism, Zeek HTTP connection reuse, and cross-sensor timing texture.
- [x] Current-dev assessment continuation loops 21-30 — completed the requested next 10 EvidenceForge realism loops from Loop 20, targeting the highest-leverage verified findings from each panel while keeping the draft PR open.
  - [x] Loop 21 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `c9a7b72`: repaired browser-like HTTP process/network ownership by resolving Windows browser User-Agents to active interactive browser processes when possible, clearing misleading service-process attribution when no user session exists, and scoping internal human-browser web visitors to workstation clients. Verification passed with focused regressions, related eCAR/proxy/web/baseline tests (`107 passed, 1 skipped`), `eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3141 passed, 37 skipped`). Regenerated eval passed at exact `96.40/100` across `80,222` records; hard probes found zero browser-like Zeek HTTP rows joined to Windows 5156 or eCAR `svchost.exe`, while all `497` matched WFP browser rows were browser-owned and service/tool HTTP retained service ownership where appropriate. Blind synthetic-confidence scores were Threat Hunter `78`, Detection `68`, Network `84`, Host/EDR `72` (average `75.5`). Top Loop 22 target is Linux endpoint realism: bash command cadence and fleet-wide repeated syslog daemon pools, followed by public DNS/X.509 metadata templates, Zeek multi-sensor timing texture, HTTP connection reuse, and SYSTEM Sysmon `LogonGuid` morphology.
  - [x] Loop 22 fix pass — repaired the multi-reviewer Linux endpoint realism finding at the generator/config layers: bash histories now use per-host/user session pacing with bounded quick-command streaks, command pools suppress high exact-repeat counts, and extra syslog daemon pools are scoped by system type/role with lower weights and expanded templates. Verification before regeneration passed with focused regressions (`46 passed`), related activity/config tests (`212 passed`), `eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3144 passed, 37 skipped`).
  - [x] Loop 22 regeneration, hard probes, quantitative eval, and blind review completed from commit `ecc45ef`: repaired Linux endpoint cadence fingerprints by adding session-aware bash pacing, tighter exact-command repeat suppression, and system-type/role-scoped extra syslog pools. Regenerated eval passed at exact `96.14/100` across `73,808` records; hard probes found zero server-side desktop-daemon syslog rows, zero non-database `multipathd` rows, bash `<=10s` gaps at `47/726` (`6.47%`), median bash delta `85s`, and max per-file exact-command repeat `4`. Blind synthetic-confidence scores were Threat Hunter `78`, Detection `66`, Network `84`, Host/EDR `42` (Host/EDR verdict Real at confidence `58`; average synthetic-confidence `67.5`). Top Loop 23 target is the hard Zeek multi-sensor timing fingerprint where mirrored DMZ records always trail core records by a tiny positive offset; next targets are public DNS/X.509 corpus templates, remaining Linux daemon-message repetition, DNS TXT tunnel vocabulary/cadence, and HTTP connection reuse.
  - [x] Loop 23 fix pass — repaired the Zeek multi-sensor timing texture at the shared Zeek multiplex layer by applying independent clock skew/drift plus per-flow capture delay to every observing sensor, not only secondary sensors, so mirrored rows can have positive or negative cross-sensor deltas while remaining within a well-synced low-millisecond envelope. Verification before regeneration passed with focused timing tests (`3 passed`), related Zeek fanout/multiplex/HTTP/SSL/files/activity tests (`116 passed`), `eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3144 passed, 37 skipped`).
  - [x] Loop 23 regeneration, hard probes, quantitative eval, and blind review completed from commit `91546d7`: repaired the hard one-way Zeek multi-sensor timing fingerprint. Regenerated eval passed at exact `96.14/100` across `73,808` records; hard probes matched `2,782` core/DMZ Zeek rows across conn/http/dns/ssl, confirmed every checked format now has both positive and negative offsets, and found zero always-positive formats. Blind synthetic-confidence scores were Threat Hunter `74`, Detection `66`, Network `84`, Host/EDR `62` (average `71.5`). Top Loop 24 target is public DNS/X.509 corpus realism because the network specialist continues to flag templated NS/MX/SOA answers, exact host-plus-wildcard SANs, and clustered certificate validity periods at high confidence; next targets are remaining Linux daemon-message repetition, DNS TXT tunnel grammar/cadence, HTTP connection reuse, and Sysmon SYSTEM `LogonGuid` morphology.
  - [x] Loop 24 fix pass — repaired public DNS and X.509 corpus realism by replacing provider-agnostic NS/MX/SOA templates with data-driven provider/domain-class NS, MX, and SOA answer profiles; making public certificate SANs weighted instead of exact host-plus-wildcard by default; and widening/reweighting short-lived public CA validity profiles. Verification passed with focused DNS/TLS/config tests, `uv run eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3148 passed, 37 skipped`).
  - [x] Loop 24 regeneration, hard probes, quantitative eval, and blind review completed from commit `999a20e`: regenerated eval passed at exact `96.23/100` across `78,991` records; hard probes found zero old public DNS `ns1/ns2`, `mail.<domain>`, or `ns1/hostmaster` companion templates, public host-cert exact host-plus-wildcard SANs at `35/431` (`8.12%`), and `69` distinct certificate validity day counts. Blind synthetic-confidence scores were Threat Hunter `76`, Detection `69`, Network `68`, Host/EDR `64` (average `69.25`). The Network reviewer explicitly called DNS a realism strength after the fix. Top Loop 25 target is Zeek HTTP connection reuse because all `3,373` HTTP rows still have `trans_depth=1` with zero repeated HTTP UIDs; next targets are Linux syslog daemon-message statefulness, DNS TXT tunnel cadence/grammar, proxy/C2 browser artifacts, and larger/messier X.509 SAN sets.
  - [x] Loop 25 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `dd56f08`: introduced source-native persistent HTTP/1.1 transaction reuse for browser-like HTTP asset fetches and internal web sessions, suppressing duplicate connection-level rows for application-layer-only follow-on requests while preserving `zeek_http` rows with repeated UIDs and `trans_depth > 1`. Verification passed with focused regressions, related ActivityGenerator/baseline/Zeek/proxy tests (`284 passed, 1 skipped`), `uv run eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3151 passed, 37 skipped`). Regenerated eval passed at exact `95.99/100` across `79,163` records; hard probes found `277` HTTP rows with `trans_depth > 1`, `106` repeated HTTP UID groups, zero duplicate conn UID groups, and every reused HTTP UID backed by exactly one conn row. Blind synthetic-confidence scores were Threat Hunter `78`, Detection `67`, Network `92`, Host/EDR `64` (average `75.25`). The old all-`trans_depth=1` tell is fixed, but Network found a new harder source-native HTTP/conn contradiction: some same-UID HTTP transactions occur after parent connection close, have non-monotonic `trans_depth`, or exceed parent conn byte counters.
  - [x] Loop 26 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `4f92a11`: repaired persistent Zeek HTTP transaction sequencing and parent `conn.log` accounting by aggregating parent flow bytes/duration across planned HTTP request groups, guarding reuse near parent close, constraining reuse to parent byte budgets, and preserving same-UID Zeek HTTP timestamp order after analyzer jitter. Verification passed with focused regressions, related ActivityGenerator/baseline/Zeek/proxy tests (`266 passed, 1 skipped`), `uv run eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3153 passed, 37 skipped`). Regenerated eval passed at exact `95.25/100` across `76,677` records; hard probes found `189` HTTP rows with `trans_depth > 1`, `82` repeated HTTP UID groups, zero duplicate conn UID groups, zero reused groups without exactly one parent conn, zero after-parent-close rows, zero non-monotonic `trans_depth` groups, and zero HTTP body sums exceeding parent conn byte counters. Blind synthetic-confidence scores were Threat Hunter `82`, Detection `66`, Network `62`, Host/EDR `66` (average `69.0`). Top Loop 27 target is Linux syslog daemon-message statefulness because Host/EDR and Threat Hunter both flagged high-volume exact daemon phrase reuse across dissimilar hosts; next targets are DNS TXT tunnel grammar/cadence, public X.509 SAN diversity, C2/proxy HTTP shape variation, eCAR FLOW principal context, and Sysmon SYSTEM `LogonGuid` morphology.
  - [x] Loop 27 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `38e431d`: reduced cross-host exact Linux syslog daemon-message repetition by expanding/parameterizing high-volume daemon pools, lowering stale direct snapd/syslog boilerplate, and adding validator coverage for the former exact phrases. Verification passed with focused config validation tests, related baseline/syslog slices, `uv run eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3154 passed, 37 skipped`). Regenerated eval passed at exact `95.92/100` across `76,542` records; hard probes found zero old exact flagged daemon phrases, zero daemon messages repeated `25+` times, zero messages repeated `100+` times, and top exact repeats moved to SSH PAM session lifecycles. Blind synthetic-confidence scores were Threat Hunter `82`, Detection `66`, Network `64`, Host/EDR `58` (average `67.5`). Top Loop 28 target is DNS TXT tunnel grammar and C2/proxy cadence because Threat Hunter and Detection both flagged tight, storyline-bound behavior; next targets are scenario-authored attack-name legibility (defer unless scenario edits are authorized), Linux `phpsessionclean`/`irqbalance` texture, X.509 SAN and AD SRV realism, eCAR FLOW principal context, and Sysmon SYSTEM `LogonGuid` morphology.
  - [x] Loop 28 fix, regeneration, hard probes, quantitative eval, and blind review completed from commit `350b0f5`: loosened DNS TXT tunnel and C2/proxy status behavior by recognizing `/v2/*` and beacon activity as C2 HTTP, varying C2 response body sizes per beacon tick, widening DNS tunnel pacing/label grammar, and adding benign TXT collisions from other hosts around the tunnel window without changing scenario-authored names. Verification passed with focused regressions, related DNS/storyline/proxy tests (`198 passed, 1 skipped`), `uv run eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3158 passed, 37 skipped`). Regenerated eval passed at exact `96.00/100` across `74,504` records; hard probes found `226` TXT records across `12` source IPs, `34` benign TXT records from `11` non-tunnel sources, westbridge TXT median gap `2.15s`/p95 `26.26s`, and `/v2/status` proxy response sizes ranging `699`-`67,971` bytes across `8` thousand-byte buckets. Blind synthetic-confidence scores were Threat Hunter `78`, Detection `62`, Network `61`, Host/EDR `58` (average `64.75`). Top Loop 29 target is Linux syslog timer/daemon texture (`phpsessionclean`, `irqbalance`, and trust-anchor repetition); next targets are eCAR FLOW principal context, X.509 SAN/AD SRV realism, and scenario-authored name legibility if edits are later authorized.
  - [x] Loop 29 fix pass — repaired Linux syslog timer and daemon texture by adding data-driven systemd schedule filters for role, excluded role, service/package state, per-host probability, slot skip probability, and slot jitter; scoped `phpsessionclean` to PHP-backed web hosts instead of generic web/proxy hosts; and replaced formulaic `irqbalance`/systemd-resolved trust-anchor messages with source-native bounded ranges. Verification passed with focused config/syslog tests (`44 passed`), `uv run eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3159 passed, 37 skipped`).
  - [x] Loop 29 regeneration, hard probes, quantitative eval, deliberation, and blind review completed from commit `e37a5f3`: regenerated eval passed at exact `95.99/100` across `76,333` records; hard probes verified `phpsessionclean` only on `WEB-EXT-01`, zero non-PHP `phpsessionclean` hosts, zero `Positive Trust Anchors` lines, zero old `irqbalance` sample/pass lines, zero large IRQ counters, zero self-move IRQ lines, and zero `systemd-resolved` self-contradictions. Blind synthetic-confidence scores were Threat Hunter `82`, Detection `64`, Network `60`, Host/EDR `39` (Host/EDR verdict Real at confidence `61`; average `61.25`). Top Loop 30 generator-owned target is eCAR FLOW principal attribution because Detection found dataset-wide blank FLOW principals even when process/user context exists; next targets are TLS/X.509 SAN diversity plus AD SRV responses, DB bash/eCAR command timing, and additional DNS/C2 texture. Scenario-authored name legibility remains the highest broad tell but is deferred unless scenario edits are authorized.
  - [x] Loop 30 fix pass — added data-driven mixed eCAR FLOW principal attribution so user-owned process flows usually carry process/user identity, service/root flows carry it less often, inbound listener flows can occasionally expose local service principals, and unknown/rejected flows remain source-native gaps. Verification passed with focused eCAR/config tests (`93 passed`), broader eCAR tests (`88 passed`), `uv run eforge validate-config`, Ruff checks/format checks, `git diff --check`, and full normal `uv run pytest --no-cov -q` (`3162 passed, 37 skipped`).
  - [x] Loop 30 regeneration, hard probes, quantitative eval, deliberation, and blind review completed from commit `bc3772d`: regenerated eval passed at exact `95.99/100` across `76,333` records; hard probes found `4,579/13,240` eCAR FLOW records now carry mixed principals (`34.58%` overall, `52.31%` outbound, `15.30%` inbound), with zero `pid=-1` principal leaks and zero failed-flow principal claims. Blind synthetic-confidence scores were Threat Hunter `84`, Detection `62`, Network `68`, Host/EDR `39` (Host/EDR verdict Real at confidence `61`; average `63.25`). The eCAR all-blank FLOW principal tell is fixed, but the panel surfaced a verified higher-leverage contradiction: DB bash history records the `scp /tmp/mhs-archive.sql.gz` command at `17:22:09Z` while eCAR records the same process/flow at `17:15:38-17:15:39Z`. Next generator-owned targets are bash-history/process timing alignment, web/proxy path-template diversity, and richer TLS/X.509 SAN distributions; scenario-authored name legibility remains deferred unless scenario edits are authorized.
- [x] Full slow-suite regression cleanup after loop-65 merge — explicit-proxy storyline beacons now preserve authored hostname+destination IP pairs only when the storyline marks that pair as intentional, normal proxy-origin DNS resolution remains intact, and the parallel-generation LogonID assertion treats Type 7 unlock reuse as valid slice-of-time Windows behavior. Verified with targeted proxy/parallel tests, `uv run ruff check .`, `uv run ruff format --check .`, and `uv run pytest -v --include-slow` (`2875 passed, 23 skipped`).
  Detection Engineer blind review completed for the regenerated Loop 61 dataset at `scenarios/iteration-test/data`; reviewer verdict: Synthetic, 63/100 confidence. Main findings: one PROXY-01 sshd accepted-login lifecycle gap/self-source artifact and Windows 4648 explicit-credential caller PID/image provenance ambiguity around `WS-MCHEN-01`.

### P2 Windows Event XML Shared Helpers

- [ ] **P2** Review shared Windows Event XML helper opportunities across Security and Sysmon emitters — timestamps now share EVTX-like 100ns formatting, but Security and Sysmon also share the same high-level Event XML envelope. Inventory which pieces can safely move to shared helpers (timestamp formatting, XML escaping, EventRecordID mechanics, common System envelope fields) without hiding provider-specific Security/Sysmon field semantics.

### World Model Refactor

- [x] Open the draft PR from `world-model` into `dev`
Compiled `WorldModel` / `WorldPlanner` behavior is now the shared planning layer for persona placement, host capabilities, proxy/infrastructure routing, and interactive/network/SSH/RDP session bootstrap across baseline and storyline generation.
Runtime ownership state was expanded to track exact session/process/connection provenance, the remaining `hash()`-based realism-critical derivations were replaced with `_stable_seed(...)`, and the contributor/docs/skill guidance was updated to match the new contract.
Verification is complete: dedicated `tests/unit/test_world_model.py` coverage was added and `uv run pytest -v --include-slow` passed (`1483 passed`).

### Recently Resolved

- [x] Blind Windows process/Sysmon/eCAR realism evaluation — separate evaluator scored the regenerated focused dataset 90% synthetic. Highest-impact findings: eCAR THREAD/REMOTE_CREATE disagrees with Sysmon Event 8 on thread IDs/start addresses, process create user/logon identity can disagree across Security/Sysmon/eCAR, and process-create timestamps are too exact across sources. Medium findings covered unmatched terminations, templated process distributions, and pre-seeded process GUID references.

- [x] Pytest stabilization after Windows process/Sysmon/eCAR review — fixed the explicit-proxy storyline integration failure by preventing dynamic HTTPS API-style proxy requests from being modeled as cache HITs, then reran `uv run pytest --include-slow --durations=50 --durations-min=1.0` successfully (`2329 passed, 1 skipped` in 737.74s). Duration data shows runtime is dominated by the medium dataset memory test (410.68s), explicit proxy storyline fixture (139.43s), and medium dataset generation setup (107.60s); the medium/parallel tests were already marked slow, and the explicit proxy correlation fixture is now marked slow too.

- [x] Windows process/Sysmon/eCAR source review — generated a focused Windows-heavy dataset with Windows Security, Sysmon, eCAR, and Zeek. Fixed Sysmon Event 5 `ProcessGuid` mismatches by carrying process start time on termination events, and fixed Windows Security/Sysmon same-second timestamp jitter so rendered XML stays chronological per host. Focused tests and regenerated-output probes passed.

- [x] Windows process/Sysmon/eCAR blind-review follow-up — implemented shared canonical `RemoteThreadContext` evidence for Sysmon Event 8 and eCAR `THREAD/REMOTE_CREATE`, moved user-session process identity correction into generation before Windows Security/Sysmon/eCAR rendering, and switched eCAR module-load generation to the same process-aware DLL profile data used by Sysmon ImageLoaded events. Remote-thread start locations are now data-driven/overlay-aware and covered by `eforge validate-config`. Existing Windows/Sysmon render-time timestamp normalization already prevents exact XML timestamp ties, so no additional timestamp offset was needed in this pass. Verification: focused tests passed, full unit suite passed (`2278 passed, 1 skipped`), Ruff passed, and `uv run eforge validate-config` passed.

- [x] Windows process/Sysmon/eCAR blind-eval cleanup — fixed approved follow-up findings from the 82% synthetic blind eval: eCAR remote-thread `tgt_tid` now matches Sysmon Event 8 `NewThreadId`, Security 4689 avoids blank `SubjectLogonId` for system-owned process exits, process-create render timestamps have deterministic source offsets across Security/Sysmon/eCAR, eCAR `PROCESS/OPEN` uses explicit target fields instead of overloading `command_line`, eCAR module-load timing no longer exactly ties process creation, and failed logons carry explicit eCAR failure outcome/status fields. Focused tests, full unit tests, full non-slow tests, Ruff, and `eforge validate-config` passed.

- [x] **SUPERSEDED** Windows process/Sysmon/eCAR blind-eval follow-up from 88% synthetic review — superseded by the broader source-specific process lifecycle completeness modeling TODO and the newer eCAR baseline variance TODO. Concrete field-provenance/process-path fixes landed during the canonical emitter provenance and process lifecycle work.

- [x] Canonical emitter field provenance fixes — implemented the approved emitter audit fixes: Windows 5156 process attribution resolves from canonical process state, Sysmon/eCAR share canonical image-load data, Sysmon Event 10 and eCAR `PROCESS/OPEN` use `ProcessAccessContext`, Sysmon parent GUIDs use parent process start time, user process parentage no longer falls back to PID 4, Zeek dhcp.log receives DHCP option-domain data when available, bash history no longer carries non-native `exit_code`, ASA/proxy context-owned fields are honored, and deferred source-specific process lifecycle completeness modeling is documented below.

- [x] Canonical emitter field provenance generated-output evaluation — generated a targeted multi-source dataset under `/private/tmp/eforge-provenance-output` and ran deterministic rendered-output probes for: 5156 PID/image attribution from process state, 4688 parent PID sanity, Sysmon/eCAR image-load agreement, Sysmon/eCAR process-access target/thread provenance, Zeek DHCP option-domain rendering, bash history without `exit_code`, ASA context-owned fields, and proxy CONNECT tunnel scoping. The first probe caught a real WFP process-image propagation bug on explicit proxy client legs; fixed it and reran the probes successfully. Next: targeted blind provenance review, then broad all-data blind review.

- [x] Canonical emitter field provenance blind-review follow-up — targeted blind review scored the focused dataset 88% synthetic. Fixed confirmed actionable findings: Windows 5156 no longer inherits a storyline process from the wrong host/OS, unresolved non-system WFP process images are suppressed instead of rendering `-`, PID 4 WFP fallback renders as `System`, internal DNS preserves scenario IP→FQDN registrations before generated aliases, and `_ldap._tcp...` NXDOMAIN companion probes use SRV. Regenerated-output probes passed. The proxy CONNECT+GET finding was a prompt artifact because the blind prompt omitted the current TLS-inspection assumption; rerun the blind review with that assumption stated.

- [x] **SUPERSEDED** Canonical emitter field provenance blind-review remaining findings from 78% synthetic review — superseded by later full-path storyline normalization, bash typo/path cleanup, proxy domain-class path/content profiles, and Sysmon follow-on ordering fixes. The still-current related work is now represented by web/session realism, imperfect observation/source coverage, and process lifecycle modeling TODOs.

- [x] **SUPERSEDED** Source-specific process lifecycle completeness modeling — the broad requirement is now covered by named observation profiles plus the host/activity profile layer. Observation profiles model deterministic source-family missingness/delay/coverage semantics for Security/Sysmon/eCAR and other sources, while host activity profiles add endpoint/source volume variance; the remaining narrower deployment-topology gap is tracked as configurable per-host/source log deployment coverage.

- [x] Open PR consolidation into `dev` — re-applied the storyline typing-cadence monotonicity fix from PR #81, folded Dependabot pytest/Pygments updates into the dev workflow, and added Dependabot configuration so future dependency PRs target `dev`.

- [x] **SUPERSEDED** Windows Security/authentication source review — superseded by the focused Windows auth timing/source semantics fixes and tests completed during the timing and source-review work. No separate active review thread remains.

- [x] TODO.md reality audit — verified high-signal open realism/code-cleanup findings against the current codebase, marked stale items, and identified the generated-output validation pass needed before deeper realism work.
  Targeted verification: `uv run pytest tests/unit/test_network_realism.py tests/unit/test_activity_helpers.py tests/unit/test_dc_kerberos_logon.py -q --no-cov` passed (25 tests).

- [x] Generated-output TODO validation — generated two temporary 12-hour audit datasets under `/tmp` from the VDF scenario, including a network/IDS sensor variant. Evaluator parsed 1,057,006 records from 11 sources with 99.998% parsability and overall score 86.64. Output metrics confirmed several stale TODOs and several still-real Sysmon/DNS/ASA findings.

- [x] Security: cap `baseline_activity.traffic_rates` override values (max 50,000) to prevent scenario-driven resource exhaustion DoS.
- [x] Security: cap `dns_tunnel` payload/payload_size to 1 MiB to prevent memory exhaustion from untrusted scenarios
- [x] Security: guard web_scan preset overlay merge against non-dict `presets` payloads to prevent malformed overlay crash/DoS
- [x] Fix `_find_user_session` mixed tz-aware/naive `start_time` comparison crash (Aardvark finding)
- [x] Fix RDP remote-logon network evidence duplication/coherence (avoid duplicate type-10 helper connection; preserve shared source-port correlation)
- [x] Baseline inbound profile traffic no longer depends on outbound role traffic for business-hour gating (fixed UnboundLocalError when outbound profile is empty).
- [x] Security: validate blocked_c2 interval/duration are > 0 to prevent zero-interval infinite loop DoS
- [x] Harden temporal evaluator `exclude_ports` parsing against malformed `zeek_conn.id.resp_p` values (prevent eval crash on non-numeric ports)
- [x] Evaluator grace period for causal ordering (logon→process rule skips events within logon_grace_period from scenario start)
- [x] Evaluator event type detection from typed EventSpec fields (replaces fragile keyword matching) + 9 new record matchers
- [x] Evaluator per-sub-event indicator accuracy (fixes last-writer-wins IP merge for compound storyline steps) + tighter eCAR FLOW matching
- [x] Evaluator format group trace coverage (host-local vs network groups instead of checking all formats)
- [x] Evaluator anomaly rate: red herring events count as anomalies + 2 new suspicious patterns (temp_dir_execution, unusual_powershell) + doubled noise intensity
- [x] Evaluator burstiness: raised minimum event threshold to 30 for reliable CV estimates + tuned Hawkes alpha/beta ratios
- [x] Evaluator causal pair tolerance field (DNS→TCP allows 3% direct-IP connections) + expanded eCAR exclude_accounts for Linux daemons
- [x] ZeekDhcpEmitter missing can_handle() — DHCP events never reached emitter
- [x] Windows emitter cross-host OS filtering — can_handle() now uses _get_host() for consistent host selection
- [x] Per-system session check for baseline + suspicious noise — logon emitted on target system, not reused from wrong system
- [x] Context-aware logon types — interactive (type 2) for workstations, network/RDP (type 3/10) for servers
- [x] DNS before baseline system traffic — SMB/Kerberos/LDAP/DB connections emit DNS via causal expansion with 2% direct-IP skip
- [x] System IP→FQDN registration — scenario system hostnames registered in REVERSE_DNS at setup time
- [x] Red herring typing cadence — compound red herring steps now use typing cadence like storyline events
- [x] primary_system required for all users — scenario skill, reference, and validation updated; coverage test prompt updated
- [x] SubjectLogonId hardcoded to SYSTEM (0x3e7) on 4720/4728/4697/4698/1102
- [x] 4728 MemberSid doesn't match 4720 TargetSid across storyline events
- [x] 4648 SubjectLogonId is SYSTEM (0x3e7) for domain user
- [x] Missing Snort IDS baseline alerts for single-system segments
- [x] Sysmon 8 TargetProcessId hardcoded to 4 (System kernel PID)
- [x] Network logon (type 3) processes parented by explorer.exe instead of services/svchost
- [x] 4698 TaskContent empty when not specified in scenario
- [x] System Process Regularity eval penalized realistic variance (CV scoring)
- [x] Volume Adequacy eval targets miscalibrated for storyline-based signal counting
- [x] Slow tests (medium dataset, parallel generation) excluded from default `uv run pytest`; run explicitly with `-m slow`

### Tier 0: Infrastructure

- [x] Security: validate `environment.timezone.systems` overrides at schema load to prevent runtime `UnknownTimeZoneError` crashes during timezone conversion.
- [x] Security: threaded emitter worker exceptions can deadlock `barrier_flush()` (`Queue.join()` wait forever when worker dies).
- [x] Security: blocked symlinked `eforge` install target in `install-skills` to prevent arbitrary overwrite/deletion and stale-file cleanup outside target.
- [x] Security: cap firewall deny baseline amplification (`deny_ratio`/hourly deny volume) to prevent scenario-driven local DoS — `NetworkSensor.deny_ratio` now enforces `<= 50.0`.
- [x] Security: prevent IPv6 scenario DoS in DNS AAAA fallback (`_ipv4_to_fake_ipv6` no longer evaluates for IPv6 destination IPs; AAAA uses mapped IPv6 or preserves IPv6 literal).
- [x] Security: bounded/pruned ActivityGenerator DNS cache (60s prune cadence, 600s TTL-horizon eviction, 50k hard cap) to prevent unbounded memory growth from unique `(src_ip, hostname)` keys.
- [x] `eforge generate --force` overwrite can fail for scenarios that do not emit `GROUND_TRUTH.md` — fixed the root contract so every successful generation emits a matched `data/`, `GROUND_TRUTH.md`, and `OBSERVATION_MANIFEST.json` sidecar set, including baseline-only scenarios. The CLI swap stays strict and now requires staged data, ground truth, and observation manifest before replacing old output. Verification passed with focused engine/CLI/ground-truth/manifest tests, `eforge validate-config`, Ruff checks, and full normal `uv run pytest -v` (`3051 passed, 15 skipped`).

- [x] **`uv.lock` not committed** — gitignored, so CI `setup-uv@v4` cache fails. Remove from `.gitignore` and commit.
- [x] **`eforge validate` can't find personas in dev mode** — works when installed (`eforge validate`) but not via `uv run eforge validate`. Blocks dev workflow.
- [x] **511 ruff lint errors + 102 formatting issues** — CI lint job fails immediately. Auto-fix + suppress false positives (B008 for Typer, N806 for lookup tables, B904 for typer.Exit).
- [x] **Coverage threshold mismatch (local 70% vs CI 90%)** — pyproject.toml `fail_under` doesn't match CI's `--cov-fail-under=90`. Devs pass locally, fail in CI.
- [x] **CI runs tests 3 times** — 3 separate pytest invocations (unit, integration, both again for coverage). Consolidate to single run.
- [x] **No pre-commit hooks** — ruff issues only caught in CI. Add pre-commit framework with ruff check + format hooks.
- [x] Security: sandboxed Jinja template rendering for YAML-defined format templates (SandboxedEnvironment + StrictUndefined) to block SSTI/code execution while preserving safe field interpolation.
- [x] Security: bound baseline failed-logon synthetic service account selection loops to prevent scenario-controlled infinite loops/DoS.
- [x] Security: guard persona `activity_intensity` normalization against all-zero values to prevent divide-by-zero DoS during generation (all-zero overrides now safely map to floor probability instead of crashing).
- [x] Security: prevent nmap CIDR fallback host expansion from materializing full `network.hosts()` lists (large attacker-controlled CIDRs can trigger memory/CPU DoS).
- [x] Security: bound cross-source field-agreement joins in evaluation to prevent attacker-controlled pivot buckets from causing quadratic CPU exhaustion (per-pivot B buckets and total pair comparisons are capped, with regression coverage).
- [x] **Re-generation appends to existing output** — CLI now checks for existing `data/`, `GROUND_TRUTH.md`, and `ENVIRONMENT.md` before generation. Prompts user to confirm overwrite or abort. `--force` / `-f` flag skips prompt for automation/AI use.

### Tier 1: Foundational Correctness

Data is *wrong* — a hunter hits dead ends. Fix these first; several unblock Tier 2 work.

- [x] Stale account usernames with `$` no longer crash baseline generation when converted to `User` objects (aligned `User` username/email validation with stale account pattern)
- [x] Harden provider-aware DNS hostname generation against invalid/IPv6 storyline `dst_ip` values to prevent generation-time crashes (invalid/non-IPv4 inputs now fall back to `generic` provider safely).
- [x] **LogonIDs leak across hosts** — remote processes on DC/file server use the originating-host LogonID instead of the destination host's 4624 TargetLogonId. Breaks every pivot-based hunting workflow.
- [x] **services.exe PID changes within single boot session** — process tree references a parent PID that was replaced mid-scenario. Child processes become orphaned.
- [x] **Extend canonical event model to baseline activity** — added SyslogContext, WeirdContext, extended DhcpContext. Syslog emitter renders from SyslogContext exclusively. All internal `generate_raw()` calls eliminated (was 12, now 0). `generate_raw()` exists solely for user-facing `raw` event type in scenario YAML.
- [x] **Migrate eCAR FLOW to SecurityEvent dispatch** — already complete: `"connection"` in `_supported_types`, `_render_connection()` implemented, all connections dispatch through SecurityEvent. `pid:-1` for system traffic is correct behavior.
- [x] **No 4625 on DC for password spray** — sprays against domain accounts should produce 4625/4776 on the DC, not just the originating workstation. DC-focused Sigma/Splunk rules won't fire.
- [x] **Ground truth Zeek UIDs missing from logs** — UIDs listed in GROUND_TRUTH.md IOC section don't exist in any sensor's conn.json. Answer key references evidence that isn't there.
- [x] Raw storyline events path traversal hardening — sanitized host routing keys for host-multiplexed emitters and Windows per-host writers so raw event fields cannot escape output directory; unsafe keys now fall back to flat-file output.

### Tier 2: Huntability & Detection

Data is structurally correct but the hunt doesn't work — key attack steps are undetectable or trivially obvious.

- [x] **RDP lateral movement invisible + zero RDP noise** — added background IT admin RDP connections (1-3/hour) to Windows servers/DCs in baseline. Storyline RDP sessions already produce Zeek conn records via generate_rdp_session().
- [x] **No DC Kerberos events for compromised user** — generate_logon() now emits 4768 (TGT) + 4769 (service ticket) on the DC for Kerberos-authenticated domain logons, with realistic timing offsets.
- [x] **No LSASS access events (Sysmon 10)** — added Sysmon Event 10 (ProcessAccess) emitter, format template, and generate_process_access() method. Auto-emits alongside create_remote_thread when target is lsass.exe.
- [x] **4672 (Special Privileges) target-host semantics** — elevated-session 4672 is auto-emitted alongside the target-host 4624; Kerberos causal expansion emits DC 4768/4769 only.
- [x] **Storyline events too perfect** — /eforge scenario skill now interviews about attacker sophistication and generates fumbles (mistakes) and dead ends (abandoned paths) appropriate to the chosen level.
- [x] **C2/exfiltration SNI values are auto-generated CDN names** — replaced `host-x-x-x-x.cdn-provider.net` fallback with 30 plausible SaaS/analytics/CDN domains.
- [x] **Proxy log issues** — CONNECT entries now use domain names from REVERSE_DNS or plausible random hostnames instead of raw IPs.
- [x] **Zeek http.log doesn't reflect storyline HTTP activity** — storyline HttpContext host field now uses domain names; skill guidance added to always specify method/uri for HTTP exfiltration events.
- [x] **Vastly expand canned data** — syslog: 6→20 programs (added NetworkManager, dbus-daemon, rsyslogd, sudo, dhclient, polkitd, etc.); User-Agents: 5→15 (added Opera, IE11, curl, wget, older versions); Snort SIDs: 8→30 (added scanning, web attacks, protocol anomalies, policy violations).
- [x] **Baseline connections missing initiating process PIDs** — all `generate_connection()` calls in baseline default to `pid=-1`, so eCAR FLOW records can't be correlated to the originating system process. Pass realistic PIDs from `_system_pids` (e.g., svchost for DNS/NTP/SMB, lsass for Kerberos/LDAP, sshd for SSH). Add `systemd-resolved` to Linux process tree in `emitter_setup.py`. Pass `_last_storyline_pid` in storyline connection handler. Update scenario-reference.md and EVIDENCE_FORMATS.md to document FLOW pid behavior. Update `/eforge generate` and `/eforge scenario` skill references to reflect pid correlation. Add tests for baseline PID propagation and storyline PID passthrough.

### Dual src/dst HostContext (Phase 8.5)

- [x] **Replace SecurityEvent.host with src_host/dst_host** — Network events now carry both source and destination host contexts. Single-host events use src_host (process, file, shell) or dst_host (logon, account mgmt, Kerberos). Connection events populate both when both endpoints are internal. eCAR emitter emits OUTBOUND FLOW on src_host and INBOUND FLOW on dst_host. All emitters, StateManager, ActivityGenerator (46 constructors), and 10 test files updated. No backward-compat property — clean cut.
- [x] **Fix phantom OUTBOUND eCAR records** — Pre-existing bug in baseline.py: SSH inbound connections, UFW blocks, and ICMP pings passed `source_system=system` (the destination) to `generate_connection()`. With dual-host, this created ~1,660 phantom OUTBOUND records on destination hosts. Removed incorrect `source_system` from 4 calls.
- [x] **Fix storyline connection source_system** — storyline.py passed `source_system=system` (the storyline target) instead of resolving from `source_ip`. Now resolves via `_ip_to_system`.
- [x] **Fix baseline SSH syslog source_ip** — Interactive logons on Linux defaulted to own IP or 127.0.0.1. Now picks realistic remote IP from environment for SSH-style logon types.
- [x] **Add direction field to eCAR format definition** — FLOW/CONNECT records now carry INBOUND/OUTBOUND direction in properties.

### Tier 3: Realism Polish

Data works but experienced analysts spot tells. Grouped by format for efficient fix passes. Items marked with ✓ were fixed in the blind expert panel improvement loop (2026-03-27). Items marked with ✓² were fixed in the improve/apt-healthcare-coverage loop (2026-04-02, 5 iterations, 4-expert blind panel: network forensics, host/EDR, detection engineer, threat hunter; all at 92% SYNTHETIC confidence).

- [x] Blind HTTP/proxy/network telemetry realism evaluation of `/private/tmp/eforge-http-proxy-review-output/data` — scored highly synthetic due to concrete ASA/Zeek DNS and proxy/client-tap contradictions, with ASA connection-ID artifacts and DNS consistency issues as supporting tells.
- [x] Fresh blind HTTP/proxy/network telemetry realism evaluation of regenerated `/private/tmp/eforge-http-proxy-review-output/data` — TLS-intercept proxy pattern accepted; remaining high-signal findings were plain HTTP proxy/client status contradiction, OCSP responder/issuer mismatch, and generated-looking Zeek certificate FUID artifact.
- [x] Fresh blind HTTP/proxy/network telemetry realism evaluation after latest fixes for `/private/tmp/eforge-http-proxy-review-output/data` — prior critical proxy/client and OCSP/public-responder issues were fixed; remaining evidence is medium/high realism polish around Zeek HTTP body length semantics, Let's Encrypt OCSP responder choice, and ASA connection-ID spacing artifacts.
- [x] Fresh blind Windows authentication/security telemetry realism evaluation of `/private/tmp/eforge-windows-auth-baseline-output/data` completed; assessed high synthetic likelihood from impossible 4672 logon semantics, documentation IP usage, and malformed sparse Windows events.
- [x] Fresh blind Windows authentication/security telemetry realism evaluation of regenerated `/private/tmp/eforge-windows-auth-baseline-output/data` completed; TEST-NET artifact no longer present, with remaining synthetic evidence centered on 4672 logon semantics and empty EventData for 1102/4800/4801.
- [x] Windows 4672 session semantics — removed duplicate DC-side standalone 4672 from Kerberos causal expansion; elevated 4672 now stays tied to the target-host 4624 logon session, and non-service audit fallback logon IDs no longer use SYSTEM's `0x3e7`.
- [x] Windows 4800/4801 rendering — populated workstation lock/unlock EventData fields in the Windows Security XML template.
- [x] Blind Windows authentication/security realism evaluation of current `/private/tmp/eforge-windows-auth-baseline-output/data` completed; prior 4672 issues are fixed, 4800/4801 fields are populated, with remaining synthetic indicators in DC 4625/4776 status contradictions and lock/unlock SessionId mismatches.
- [x] Windows failed-auth DC consistency — failed NTLM validation 4776 now carries the matching failure status instead of success.
- [x] Windows lock/unlock session consistency — 4800/4801 derive a stable SessionId from TargetLogonId so lock/unlock pairs do not change terminal sessions.
- [x] Blind Windows authentication/security realism evaluation of latest regenerated `/private/tmp/eforge-windows-auth-baseline-output/data` completed; prior 4672, 4625/4776, and 4800/4801 findings are fixed, with remaining realism concerns around Kerberos 4768 PreAuthType distribution and missing Zeek visibility for external failed-auth source.
- [x] Windows Kerberos 4768 pre-auth realism — moved TGT PreAuthType/ticket/encryption distributions into `kerberos_realism.yaml`, added overlay-aware loader and `eforge validate-config` schema/coherence checks, and verified generation now produces mostly encrypted timestamp pre-auth with rare populated PKINIT certificate fields.
- [x] Blind Windows authentication/security realism evaluation of latest regenerated `/private/tmp/eforge-windows-auth-baseline-output/data` completed; Kerberos PreAuthType=15 empty-cert issue is fixed, with remaining lower-confidence tells in one repeated 4672 on unlock and absent Zeek visibility for external failed auth.

**Snort/IDS:**
- [x] ✓ Snort protocol field randomly assigned (no binding to SID/rule) — restructured `_FP_SIGS` to protocol-keyed dict with per-signature port and direction
- [x] ✓ Snort flow directions inverted for outbound rules — signatures tagged "in"/"out", src/dst swapped for outbound alerts
- [x] ✓ ICMP connections carry TCP/UDP ports — force src_port=0, dst_port=0 for ICMP in generate_connection()
- [x] ✓² Snort baseline volume too low (1-3/hour) — increased to 5-15/hour per sensor; experts still consider 73/day low vs thousands in real environments
- [x] ✓² Snort alert timestamps not chronologically sorted — enabled _sort_before_flush on SnortEmitter
- [x] Snort SID revisions all `:1:1` — stale audit finding: generated IDS output used varied SIDs and revisions (e.g., `[2012887:1:2]`, `[2000575:1:8]`, `[2009714:1:9]`).
- [x] ~~Snort baseline scan IPs absent from Zeek conn~~ — no longer reproduces; prior visibility fixes (denied traffic visibility, external deny scoping) resolved this
- [x] Snort alert volume still 10-100x too low for real perimeter IDS (experts expect thousands/day) — stale audit finding: generated IDS sensor produced 4,065 alerts in 12h (~8,130/day) on the audit scenario.
- [x] No ET POLICY, ET INFO, ET DNS categories in baseline — added ET POLICY (curl UA, Basic Auth, SSLv3, APT, PE download), ET INFO (Let's Encrypt, Discord, Telegram, IP lookup, TLS failure, STUN), ET DNS (.top/.cloud TLDs) in baseline.py

**Sysmon:**
- [x] ✓ Sysmon Execution ProcessID rotates every event — stable per-host PID via hostname hash
- [x] ✓ PE metadata fields (FileVersion/Company/etc.) always "-" — lookup table of 17 common Windows binaries; fixed Windows path parsing on non-Windows OS
- [x] ✓ Sysmon TargetImage bare "lsass.exe" — resolve bare filenames to full System32 paths in Events 8/10
- [x] ✓ Sysmon ProcessGuid inconsistent for same PID — truncate timestamp to second precision for stable GUIDs
- [x] ✓ Sysmon Event 5 (ProcessTerminate), Event 8 baseline noise, Event 10 baseline noise — added to baseline + eCAR THREAD/REMOTE_CREATE and PROCESS/OPEN correlation
- [x] ✓² Lsass ProcessAccess GrantedAccess hardcoded to 0x1010 for Mimikatz — changed to 0x1FFFFF (PROCESS_ALL_ACCESS) in causal expansion rule
- [x] ✓² Benign lsass accessors limited to MsMpEng + svchost — added csrss.exe, svchost (netsvcs), services.exe as additional baseline lsass accessors
- [x] **P1** Sysmon Event 3 (NetworkConnect), 7 (ImageLoaded), 11 (FileCreate), 12/13 (Registry), 22 (DNSQuery) — implemented with data-driven filtering via sysmon_filters.yaml (SwiftOnSecurity/Olaf Hartong style). Event 3 include-filters LOLBins + suspicious ports; Event 7 excludes Microsoft-signed System32 DLLs; Event 11 include-filters executable extensions + suspicious paths; Events 12/13 include-filter persistence/tampering keys; Event 22 logs all DNS. User-configurable via .eforge/config/ overlay with per-event enabled toggle.
- [x] ✓³ ParentCommandLine always "-" — added parent_command_line to ProcessContext; populated via _lookup_parent_command_line() from StateManager
- [x] Event 7 DLL load profiles per process — `loaded_modules` field on application_catalog.yaml (user apps) and system_processes.yaml (OS processes), using same schema. Unified loader in dll_load_profiles.py collects from both. Common OS loader chain applied to all processes; unprofilesd processes fall back to common-only.
- [x] GrantedAccess diversity limited to 3-4 values (0x1000/0x1010/0x1410/0x1FFFFF) — fixed with data-driven `process_access_patterns.yaml` baseline pairs and weighted mask alternatives. Verification audit output: 949 Event 10 records used 6 distinct masks (`0x1000`, `0x1400`, `0x1010`, `0x0400`, `0x1410`, `0x0410`).
- [x] CallTrace offsets limited to 2 patterns — moved to calltrace_patterns.yaml with 8 distinct call chains (ntdll, KERNELBASE, kernel32, RPCRT4, wbemcomn, combase, advapi32, sechost)
- [x] Sysmon EventRecordIDs perfectly sequential (no gaps) — gaps widened to 1-7 with 15% chance of 8-50
- [x] Event 8 StartModule/StartFunction always empty for benign pairs — fixed by deterministic source-aware StartModule/StartFunction selection in the Sysmon emitter. Verification audit output: 325/325 Event 8 records had populated StartModule/StartFunction values.
- [x] **P1** Event 3 process-to-destination mismatch — fixed with data-driven `process_network_map.yaml` `dns_tags`, app-specific DNS registry tags, and hostname-aware process attribution in `WorldPlanner.ensure_connection_process()`. Audit regeneration showed 0 bad Teams/Outlook/OneDrive Event 3 pairs; office app records only used allowed endpoint families.
- [x] **P1** Event 3 sampling uses non-deterministic `random.random()` — fixed with per-connection stable sampling using Zeek UID/connection ID/time fallback, plus low-rate browser/app sampling. Verification audit output: 3,478 Event 3 records with 46 unique destination hostnames.
- [x] **P1** Event 7 (ImageLoaded) volume too thin — fixed with process-aware application DLL pool materialization and higher standalone baseline sampling. Verification audit output: 459 Event 7 records across 14 Windows hosts over 12h with 46 unique ImageLoaded paths and no template-wide app DLL assignment to unrelated system processes.
- [x] **P2** Registry TargetObject path diversity — fixed with registry template materialization in `edr_pools.yaml` (`{guid}`, `{hex}`, `{doc}`, `{mru}`, etc.) and process-create registry events. Verification audit output: Event 12/13 records used 1,048 unique TargetObject paths with 0 double-braced TargetObject artifacts.
- [x] Sysmon actor-diversity review follow-up — independent reviewer score improved from 82% synthetic (initial) to 78% synthetic after Sysmon cleanup; completed an actor-diversity pass targeting the remaining process fingerprints.
- [x] Event 8 source/target pairs too narrow — fixed by moving benign CreateRemoteThread pairs into `create_remote_thread_patterns.yaml` and widening seeded actors. Verification audit output: 326 Event 8 records used 10 source/target pairs.
- [x] Event 10 source/target pairs too narrow — fixed by widening `process_access_patterns.yaml` and seeded long-lived process actors. Verification audit output: 950 Event 10 records used 16 source/target pairs.
- [x] Registry writer processes too narrow — fixed with key-family-aware writer selection. Verification audit output: Event 12/13 records used 12 writer process images and 1,968 unique TargetObject paths with 0 template artifacts.
- [x] Event 7 residual attribution issues — tightened generic module/process matching and retained process-aware DLL materialization. Verification audit output: 380 Event 7 records used 42 unique ImageLoaded paths.
- [x] Cross-source distribution realism layer — implemented a deterministic, overlay-capable host/activity profile layer derived from host family, roles, persona/risk, services, and stable per-host variance. Baseline generation now uses these profiles to scale Windows Security/Sysmon/eCAR, Zeek/network/web/proxy, Linux syslog/bash, firewall/ASA, IDS, auth, endpoint registry, scheduled process, and service-noise volumes without requiring scenario YAML changes.

**Zeek:**
- [x] Zeek DNS / network support log review — fixed DNS/TLS PTR coherence, added realistic TXT lookup variety, prevented CDN-hostname MX artifacts, increased file-server SMB target coverage, and made SSH pivot UIDs respect sensor visibility. Tests, docs, skills, and skill references updated where needed.
- [x] Blind Zeek/network eval high+medium follow-up — fixed high-impact DNS/TLS/IP coherence, unresolved HTTP URI placeholders, Linux SSH UID metadata, SMB session/operation shape, weird.log conditionality, and NTP syslog semantics. TLS volume clustering remains deferred for a separate design discussion.
- [x] Blind Zeek/network non-TLS follow-up — fixed duplicate exact non-ICMP flows, weird.log overproduction on clean TLS, syslog/eCAR chronological ordering, direct internal DNS cache behavior, DHCP↔conn UID coherence, and ground-truth sensor UID coherence. Blind score improved to 85% synthetic; remaining high-impact findings are TLS/x509/OCSP modeling and SMB Zeek file visibility design items.
- [x] ✓ Cross-sensor UIDs byte-identical — deterministic per-sensor UID derivation (SHA-256 of uid+sensor) preserving intra-sensor cross-log correlation
- [x] ✓ x509 certificate serial numbers all 5 bytes — generate 128-bit (16-byte) serials matching real CA practice
- [x] ✓ NTP Zeek ref_time/org_time/rec_time/xmt_time all 0.0 — populate with realistic values relative to event timestamp
- [x] OTH/"Cc" conn_state over-represented; SF at 88% (real: 55-75%); missing SH/S2/S3 states — rebalanced TCP distribution: SF 82%→62%, added S2/S3 half-closed states, increased S0/REJ/RSTO/RSTR
- [x] SSL ssl_history limited to 2 values (CsiI, CsijI) — stale audit finding: generator now has 5 success patterns + 2 failure patterns, and `tests/unit/test_network_realism.py` verifies diversity.
- [x] Zeek conn history too uniform (ShADadfF dominant) — 26 distinct history patterns in TCP_CONN_STATE_DISTRIBUTION including RST-based terminations, retransmissions, partial closes
- [x] Zeek files not chronologically ordered after multi-source generation — Zeek sensor writers now preserve normal flush behavior and sort the complete NDJSON file by `ts` on close. Focused regression coverage verifies cross-flush ordering for direct and per-sensor outputs.
- [x] SMB volume too low for Windows file server environments — Windows file servers now independently drive SMB baseline targets even when no DC is present, and file-server targets are weighted above DC SYSVOL/GPO traffic.
- [x] SMB session shape and operation mix — file-server SMB baseline traffic now uses larger read/write/metadata-sized connection profiles and emits eCAR FILE READ/WRITE plus create/delete operations for the authenticated user.
- [x] ~~DNS UIDs missing from conn.log (~7%)~~ — no longer reproduces (0/6487 orphans on apt-healthcare-breach); prior visibility fixes resolved this
- [x] UFW BLOCK entries don't appear in conn.log — UFW BLOCK dispatches via SecurityEvent, emits Zeek conn with conn_state='REJ'
- [x] weird.json TCP-specific types attributed to UDP sources — split into protocol-specific pools; UDP gets DNS/checksum/length anomalies at 0.5% rate vs TCP's 3%
- [x] weird.log condition-driven anomaly distribution — weird events now concentrate around partial/reset TCP flows, missed bytes, long bulk sessions, and DNS/UDP-specific oddities instead of uniform random sprinkling.
- [x] Exfiltration connections show 0 bytes transferred — auto-size by technique/description heuristic; added orig_bytes/resp_bytes/conn_state to ConnectionEventSpec; storyline defaults to SF
- [x] No port 135 (RPC/EPMAP) traffic — stale audit finding: baseline legitimate lateral movement, scan ports, blocked ports, RSAT tooling, and Sysmon port-name mapping all include 135/RPC.
- [x] Inconsistent sensor coverage for SSH pivot — SSH session generation now returns an empty network UID when topology says no sensor can observe the SSH leg, allowing storyline ground truth to mark the network evidence as filtered while preserving host-side syslog/eCAR evidence.

**DNS:**
- [x] DNS IP pool reuse: 15+ unrelated SaaS domains resolve to same IP — switched to domain-first selection for baseline web/SaaS; FORWARD_DNS maps domain→IP; fixed 93.184.216.34 mapping (was Reuters, now example.com)
- [x] DNS AAAA records: unrelated services share IPv6 prefix (cross-provider) — stale audit finding: `dns_registry.yaml` now has explicit IPv6 mappings and provider-prefix fallback ranges keyed by IPv4 allocation.
- [x] CloudFront distributions resolve to Microsoft IP ranges (cross-provider) — stale audit finding: CloudFront/AWS registry entries now resolve to AWS-style 52/54 ranges, not Microsoft-owned ranges.
- [x] No TXT queries (SPF/DKIM/DMARC checks) — baseline DNS now includes low-rate TXT companion lookups for SPF/DKIM/DMARC-style mail/authentication noise.
- [x] No Windows telemetry noise in query set — stale audit finding: registry includes Windows/background domains such as `settings-win.data.microsoft.com`, `ctldl.windowsupdate.com`, `crl.microsoft.com`, and `arc.msn.com`.
- [x] TTL distribution too uniform — Phase 6.0: varied TTLs with cache-aging jitter
- [x] Queries default to corp.local instead of scenario domain — stale audit finding: generated internal DNS used `vandynefoundation.org` (e.g., `dc01.vandynefoundation.org`, `_kerberos._tcp.vandynefoundation.org`, `wpad.vandynefoundation.org`), not `corp.local`.
- [x] MX records for CDN domains that shouldn't have mail exchangers — MX queries now use registrable domains only when the hostname is plausible for mail ownership; CDN/static hostnames fall back to TXT support lookups instead.
- [x] DNS/TLS/IP coherence at scale — hostname-aware connection generation now rewrites mismatched destination IPs to the hostname's registered IP pool, and browsing subresources resolve CDN/resource hostnames through the same DNS registry.

**TLS/SSL:**
- [x] TLS/x509 correlation gaps — baseline audit found SSL records without `cert_chain_fuids` and x509 issuer/subject pairings that looked implausible. Added deterministic certificate file UIDs, linked ssl.log to x509.log, and tightened domain-to-CA overrides for common CA-owned/Microsoft domains.
- [x] TLSv13 ratio too low for 2024 timeframe — audit output showed TLSv13 at 19,669/56,372 SSL records (~35%). TLS version selection now uses explicit weighted constants with TLSv13 as the modern majority default.
- [x] TLS version/cipher suite mismatches — resolved by TLS-version-aware cipher selection and certificate key-type coherence tests.
- [ ] Non-intercepting proxy mode — current proxy behavior assumes TLS interception, so HTTPS proxy logs can include CONNECT plus inspected request rows and downstream visibility should follow the inspected transaction. Future config can add tunnel-only/non-intercepting behavior separately because it changes proxy URL visibility, Zeek SSL/x509 certificate chains, HTTP visibility inside CONNECT tunnels, and IDS content inspection semantics.
- [x] x509 Let's Encrypt certs show 280+ day validity (should be 90) — tls_issuers.yaml with per-issuer validity (LE=90d, DigiCert=397d, etc.); issuer-aware key type selection
- [x] No SSL certificate subject/issuer data in ssl.log — zeek_x509.yaml includes subject/issuer fields; generation uses tls_issuers.yaml

**Syslog:**
- [x] ✓ DHCP messages contain integers instead of IP addresses — use system.ip
- [x] ✓ Persistent daemon PIDs randomized per message — map to sys_pids for known daemons; hash-derived stable PIDs for others
- [x] ✓ CentOS hosts run Ubuntu daemons (snapd, systemd-timesyncd, debian-sa1, user ubuntu, APT) — filter by is_rhel_like
- [x] ✓ dhclient shares PID with NetworkManager — isolated PID derivation per daemon
- [x] ✓ NetworkManager internal timestamps non-monotonic — use kernel uptime counter
- [x] ✓ Googlebot user-agent on internal hosts — split UA pool; bots only from external IPs
- [x] ✓ AppArmor mysqld audit on all hosts — only on DB-role hosts, skip RHEL
- [x] ✓ phpsessionclean on non-PHP hosts — only on web_server/forward_proxy role
- [x] ✓ Transient process (sudo) gets stable PID — sudo/cron children now get random PIDs
- [x] ✓ systemd-logind session IDs random — sequential per-host counter from boot
- [x] Session IDs appear out-of-order — resolved by later host-local LUID/session ordering fixes for Windows auth and Linux logind session generation.
- [x] NTP server mismatch — resolved: Zeek NTP and systemd-timesyncd syslog both choose from the same scenario infrastructure NTP pool with the same per-host deterministic source selection.
- [x] NTP syslog lifecycle semantics — periodic systemd-timesyncd messages now mix source selection, clock sync, offset adjustments, and timeout messages without repeating initial synchronization after the first host sync.
- [ ] No SSH protocol negotiation messages
- [x] Logrotate/cron.daily fire too frequently (should be daily, not multiple times per hour) — stale audit finding: `systemd_schedules.yaml` defines logrotate and cron-daily as daily scheduled jobs with per-host jitter, outside the per-hour probability loop.
- [x] Centralized syslog timestamps not chronologically sorted — _sort_flat_file = True in syslog.py; sorting in host_base.py
- [x] Dual SSH syslog entries with mismatched PIDs/ports — resolved by later SSH syslog lifecycle/source-port correlation fixes. Keep any future SSH duplication finding as a fresh concrete regression.

**Windows Events:**
- [x] ✓ IpAddress "::ffff:-" malformed — handle "-" string in _ipv6_mapped()
- [ ] DLL file as NewProcessName in 4688
- [x] Low 4689:4688 process termination ratio (57% vs 80-90%) — raised termination probability from 0.5 to 0.85
- [x] EventRecordID gaps too regular — stale audit finding: audit output had 189 distinct Windows Security EventRecordID gaps (max 200) and 50 distinct Sysmon gaps (max 50).
- [x] 4769 TargetUserName double-realm format — stale audit finding: audit output had 3,495 Event 4769 records and 0 double-realm TargetUserName values.
- [x] KeyLength always 0 for NTLM logons — stale audit finding: audit output included both `KeyLength=0` and `KeyLength=128` values.
- [ ] 4648 targets localhost instead of DC for domain commands
- [ ] 4728 MemberName is "-" (should be DN of added member)
- [ ] No 4778/4779 (RDP reconnect/disconnect)
- [x] Process creation timestamp can precede its authorizing logon
- [x] Missing 4634 logoff events for network logon sessions — paired logoffs for type 3 machine account logons on DCs (1-30s delay); baseline type 3/5 already had logoff pairing
- [x] Only AES-256 Kerberos encryption; no RC4/AES-128 mix — stale audit finding: Kerberos TGT/TGS/renewal generation samples `0x12`, `0x11`, and `0x17` with weighted probabilities.
- [x] Only 2 unique TicketOptions values; zero 4771 pre-auth failures — randomized TicketOptions per event type; boosted stale 4771 probability to 15%; added active-user typo 4771 at 2%/hour
- [x] File server has no domain user logon events — type 3 logon+logoff pairs for SMB access in baseline traffic profiles and storyline causal expansion
- [x] NETWORK SERVICE TargetDomainName shows domain instead of "NT AUTHORITY" — _subject_domain() helper in windows.py returns "NT AUTHORITY" for SYSTEM/NETWORK SERVICE/LOCAL SERVICE
- [x] Event 4672 LogonId 0x3e7 for domain users — target-host 4672 now shares the 4624 LogonID, and Kerberos causal expansion no longer emits duplicate standalone DC 4672 records.

**Process Trees:**
- [x] ✓³ explorer.exe parent for everything — spawn_rules.yaml now defines valid parent-child relationships; _resolve_parent() auto-creates intermediate chains (shells for CLI tools, services.exe for system processes, sshd→bash for Linux)
- [x] ✓³ PID allocation monotonic with uniform stride (~4) — replaced choice list with lognormal distribution (Windows mu=1.2 sigma=0.8; Linux mu=0.5 sigma=0.6); PID wraparound skips allocated PIDs
- [x] explorer.exe parent for RDP sessions (should be per-session userinit→explorer) — per-session smss→winlogon→userinit→explorer chain for type 10 logons
- [x] All Linux user processes share same ppid — per-SSH-session sshd fork + bash login shell; session_shell_pid on ActiveSession
- [x] Human Burstiness at 56/100 — retuned Hawkes params (alpha_beta_ratio 0.80→0.60, beta 0.04→0.06), tightened bias clamps (0.95→0.75), narrowed Gaussians
- [ ] Mimikatz at Medium integrity would succeed in scenario but fail in reality — generator doesn't model integrity levels

**HTTP/Proxy:**
- [x] Web/proxy format correctness pass — kept `web_access` aligned to Apache/Nginx combined log format and made `proxy_access` a consistent W3C Extended enterprise proxy log. Current proxy behavior assumes TLS interception, so HTTPS emits CONNECT plus inspected request rows; tunnel-only non-intercepting proxy behavior is deferred. Updated emitted fields, parser coverage, format docs, skill references, and regression tests.
- [x] Explicit proxy blind eval — agent-based review of a fresh explicit-proxy dataset rated synthetic likelihood at 65%. Strengths: client-side sensors see client→proxy, egress sensors see proxy→origin, core sensors see both, proxy access mostly aligns with Zeek HTTP, and IDS alerts have matching visible Zeek connection evidence. Remaining tells are now tracked below.
- [x] Validate requested proxy output requires proxy topology — when `proxy_access` is requested but no scenario system has `roles: [forward_proxy]`, `eforge validate` now warns with actionable topology guidance. Handled in validation, not generation; tests, docs, scenario skill, and skill reference updated.
- [x] Proxy logs omitted/mis-scored in evaluation — proxy parser existed but was not imported into the evaluation parser registry, and optional dash fields were parsed as invalid nulls. Registered `ProxyAccessParser`, added discovery coverage for host-directory `proxy_access.log`, and aligned optional field parsing/format validation.
- [x] Web/proxy access logs not chronologically sorted — baseline audit found per-web-server timestamp inversions. Host-multiplexed web/proxy access writers now sort by rendered request timestamp before flush; focused emitter tests added.
- [x] Web scan request counts too identical across campaigns — duration/end-time web_scan events treated `rate` as exact throughput. Explicit `count` remains exact, while duration/end-time scans now apply deterministic per-campaign rate drift so repeated scanner runs do not produce identical request totals.
- [x] Proxy access logs lack coherent Zeek-observed proxy path — added `environment.proxy.mode` (`transparent` default, `explicit` for PAC/browser-configured proxy) and `listener_port` (explicit default 8080). Explicit proxy generation now emits client→proxy and proxy→origin legs through normal sensor visibility instead of the original direct client→origin network event; validator, signal-integrity eval, docs, skills, and regression tests were updated. The current proxy model assumes TLS interception; non-intercepting tunnel-only behavior remains deferred.
- [x] Explicit proxy DENIED requests still produce upstream origin evidence — denied explicit-proxy transactions now return after the client→proxy/proxy_access event and do not dispatch proxy→origin Zeek HTTP/conn/SSL evidence, firewall/ASA built/deny/teardown transactions, or IDS alerts. Regression coverage includes egress Zeek, Snort, and Cisco ASA emitters.
- [x] Explicit proxy client-side origin DNS too common — fixed two leakage paths: port-only HTTP/S connections now infer service before DNS/proxy routing, and external web hostnames paired with the DNS-server fallback IP are resolved to deterministic external IPs before explicit proxy handling. Private destinations without hostnames now get internal DNS names instead of public SaaS/analytics names. Smoke output reduced client-side DNS from 474 to 171 records and removed tracked analytics/web-origin domains from client DNS while increasing proxy-side origin DNS as expected.
- [x] Explicit proxy TLS completeness gaps — established proxy→origin `service=ssl` connections now get a final SSL/x509 context repair before dispatch if earlier state normalization left them as `conn_state=SF` without SSL context. Normal TLS handshake failures still mutate away from SF. Smoke output showed 0 missing SSL rows by UID for established egress TLS on both core and egress sensors.
- [x] Explicit proxy CONNECT reuse/pooling realism — explicit HTTPS traffic now tracks active `(client, proxy, host, origin, port)` tunnels for 5 minutes and reuses them for ordinary HTTP-context requests without IDS/firewall/custom DNS evidence. This suppresses extra client→proxy CONNECT and proxy→origin TLS legs while preserving high-signal events. Smoke output dropped proxy_access rows 13,406→6,890, CONNECT rows 13,385→6,855, and core Zeek 8080/443 connection rows 13,406/13,687→6,892/7,281.
- [x] Post-CONNECT blind eval — fresh 50/50 agent review of `/tmp/network-observations-set-a/data` rated the HTTP/proxy/Zeek-focused dataset 96% synthetic. Improvements noted: plausible multi-vantage proxy topology, proxy CONNECT to internal proxy followed by proxy-origin TLS, IDS alerts correlated with visible Zeek tuples, and SSL/x509 FUID reference integrity. Remaining findings are tracked below.
- [x] Blind-eval remediation loop — stopped after exceeding the 10-iteration cap for this remediation run. Fixed the highest-impact contained findings: CONNECT and TLS byte/packet invariants, proxy byte/status semantics, internal CA issuer selection, stateful TLS resumption/cert-chain behavior, TLS cipher/key coherence, proxy failure diversity, per-sensor Zeek timing jitter/UID/FUID spaces, x509 SAN rendering and local SSL/x509 reference integrity, CONNECT zero-body Zeek HTTP semantics, HTTP files FUID correlation, and proxied storyline beacon denial behavior. Last blind score before the final structural fixes was still 97% synthetic, so remaining medium/high-impact work is tracked below rather than continuing this loop indefinitely.
- [x] Proxy CONNECT flow accounting invariants — successful explicit-proxy CONNECT rows now carry plausible client/server bytes and packets in Zeek conn while Zeek HTTP CONNECT request/response body lengths remain zero.
- [x] TLS/SNI-bearing flow accounting invariants — established SSL connections now enforce plausible client/server payload and packet floors before SSL/x509 fan-out.
- [x] Proxy byte semantics alignment — proxy access retains proxy request/response byte accounting while Zeek HTTP CONNECT body lengths are zero; HTTP response file FUIDs now stay coherent with files.log after per-sensor FUID derivation.
- [x] Internal certificate issuer realism — internal `.test`/`.local`/`.internal` host certificates now use an enterprise/private CA profile instead of public CA issuers.
- [x] TLS resumption/certificate-chain realism — first-observed SNI handshakes are non-resumed, most resumed sessions omit fresh cert chains, TLS cipher choice is bound to certificate key type and modern destination profiles, and x509 SANs render correctly.
- [x] Proxy edge-case diversity — explicit CONNECT now includes lower-rate denied, auth-required, and gateway error outcomes, and non-2xx CONNECTs stop at the proxy.
- [x] Storyline beacon proxy routing — HTTP/S beacon events from hosts with explicit proxy routes now traverse the proxy instead of raw direct client-origin network connections, including denied proxy evidence and allowed beacons that use documentation-range IPs with explicit external hostnames.
- [x] Three-loop HTTP/proxy blind-review follow-up — completed one root-cause loop after corrected full-output regeneration. Loop31 blind review rated synthetic likelihood at 68%, meeting the configured exit threshold (<=70%); structural probes passed for DMZ proxy browser UAs, GET request bodies, and ASA inbound static NAT. Deterministic eval was attempted on loop31 but stopped after running unusually long without output.
- [x] HTTP/proxy eval fixture coverage — added an explicit-proxy integration fixture that generates `proxy_access`, Zeek, and Cisco ASA together, with allowed HTTPS producing client→proxy plus proxy→origin evidence and denied HTTPS stopping at the proxy before origin-side Zeek/ASA evidence.
- [x] Proxy/Zeek consistency tests — added file-level regression coverage for proxy_access status/host alignment with visible Zeek proxy legs, denied suppression, explicit proxy sensor placement, and TLS-intercepted HTTPS rows on listener-port proxy traffic.
- [x] Regenerate HTTP/proxy blind-review samples with explicit `eforge generate --output ...` — generated `/private/tmp/eforge-http-proxy-review-output/data` from `/private/tmp/eforge-http-proxy-review.yaml` using `--output /private/tmp/eforge-http-proxy-review-output --force`. Structural probes confirmed proxy_access rows, client→proxy Zeek on listener port 8080, allowed proxy→origin Zeek/ASA, denied HTTPS stopping at proxy, and no Squid-specific assumptions.
- [x] Shared HTTP MIME/path coherence helper — centralized extension-to-MIME and MIME-to-size behavior in `generation/activity/http_content.py`, wired it into proxy URI selection, browsing sessions, baseline web requests, and storyline HTTP/beacon/web_scan contexts. Overlay regression coverage verifies an overlay-defined `/status.gif` with `text/html` is normalized to `image/gif`, and `eforge validate-config` passed.
- [x] TLS-intercepted proxy CONNECT/request row separation — inspected HTTPS request rows no longer share exact timestamps, byte counts, and durations with synthetic CONNECT rows. CONNECT setup rows now get deterministic earlier timestamps plus small tunnel-setup byte/time fields, while inspected request rows keep their own accounting. Regression coverage added for listener-port HTTPS proxy traffic.
- [x] DMZ web servers generate desktop-like proxy traffic — blind review found public web servers using the explicit proxy with browser-looking Windows/Linux UAs for SaaS/CDN requests. Fixed by selecting role-aware proxy User-Agents, avoiding browser-session expansion for server-role HTTP(S) traffic, and passing source host identity through outbound IDS baseline events; proxy+IDS regeneration had 0 Mozilla-style DMZ proxy rows across 1,517 DMZ proxy entries.
- [x] ASA inbound static NAT representation still needs a focused probe after correct-output regeneration — loop 17 fixed reversed translation records, but blind review flagged allowed inbound connection builds that exposed private DMZ destinations where public VIP context was expected. Fixed inbound IDS false-positive baseline events plus external storyline web-scan/connection events to enter through static-NAT VIPs; targeted ASA+IDS regeneration had 0 private-private inbound built records and confirmed VIP-in-parentheses plus 305011 static translation records.
- [x] Server-originated proxy User-Agent destination/OS refinement — loop31 blind review found DMZ server proxy traffic no longer uses desktop browser UAs, but package-manager UAs could appear against unrelated SaaS/CDN domains and Fedora `libdnf` could appear from Ubuntu hosts. Added overlay-aware `proxy_user_agents.yaml`, moved proxy UA pools out of Python, and limited package-manager UAs to matching OS families and package/update repository hosts. Targeted loop33 proxy+IDS regeneration had 1,472 DMZ proxy rows, 0 DMZ Mozilla rows, 0 Fedora UA rows, and 0 package-manager UA rows to non-package destinations.
- [x] HTTPS beacon proxy User-Agent passthrough — `service: ssl` storyline beacons that specify `user_agent` but omit HTTP method/URI now write that UA verbatim to proxy CONNECT entries instead of falling back to the generic proxy browser UA pool. Added explicit-proxy storyline regression coverage for repeated HTTPS beacon CONNECT rows.
- [x] Storyline/time-window mismatch validation — blind review caught a stale sample with `time_window.duration: "24h"` but storyline activity at `+36h`, causing source horizon mismatches. `eforge validate` now warns when storyline events fall outside the generation window, and docs/skills note that all storyline/red-herring times should fit inside `time_window`.
- [x] Public web HTTPS and HTTP body semantics follow-up — external public-web baseline traffic now strongly prefers HTTPS, and generated proxy GET/HEAD/CONNECT/OPTIONS Zeek HTTP records keep `request_body_len=0` instead of copying proxy byte accounting into request bodies. Focused regression coverage added for proxy GET body semantics.
- [x] DNS analyzer parity for direct external DNS — DNS-service connections now attach a DNS context when the caller does not provide one, and explicit proxy-origin connections emit resolver evidence from the proxy host. This prevents Zeek `service:"dns"` conn rows without matching dns.log evidence in the normal generator path.
- [x] ASA teardown reason and connection ID realism — ASA connection IDs now use non-wrapping per-sensor counters, and `SYN Timeout` teardown reasons are limited to handshake-only/no-payload TCP connections rather than sampled independently from byte counts.
- [x] Scenario public IP hygiene — bundled/review scenarios that are meant to look like real collected data should avoid RFC 5737 TEST-NET ranges (`192.0.2.0/24`, `198.51.100.0/24`, `203.0.113.0/24`) for public NATs, external scanners, and attacker infrastructure. Updated `/eforge scenario`, reference docs, VDF realism fixture, coverage prompt examples, and the suspicious-DNS generator path. External client generation now rejects all non-global special-use ranges, including benchmark space such as `198.18.0.0/15`. Regenerated multi-source VDF output had 0 TEST-NET or benchmark-space hits and deterministic eval scored 97/100.
- [ ] Add RFC 5737 validation warnings for realism-bound scenario fields — warn when `public_cidrs`, NAT `mapped_ip`, storyline `source_ip`/`dst_ip`, DNS `answer_ip`, or similar external-facing fields use `192.0.2.0/24`, `198.51.100.0/24`, or `203.0.113.0/24`. Allow the scenario, but make the warning explicit so documentation-safe examples do not accidentally become realism-eval fixtures.
- [x] Blind HTTP/proxy loop 17 follow-up — fixed high-impact reviewer findings from the corrected multi-source dataset: ASA static NAT interface direction reversal, missing proxy DNS for high-volume explicit proxy hostnames, and public web HTTP Host headers collapsing to bare internal hostnames. Also fixed medium-impact SYN timeout duration tells and excluded non-global generated external client IPs. Focused regression tests passed; regenerated output had no targeted structural hits and deterministic eval passed at 97/100.
- [x] ✓² Proxy user-agent pool limited to 2 agents — expanded to 8 diverse agents (Chrome/Firefox/Edge/Opera/IE11)
- [x] ✓² Proxy/SSL hostname uses CDN reverse-DNS PTR records instead of domain names — now prefers dns.query from DnsContext; partial fix (first connections per host still use PTR when no DNS context exists)
- [x] ✓² Proxy URL paths all root "/" only — added pool of 18 realistic URI paths
- [x] User-Agent OS mismatch with source hosts — os field on proxy_uri_templates.yaml; OS-aware filtering in pick_proxy_uri(); OS-aware baseline web UA pool
- [x] 100% HTTP 200 status codes — _get_http_status() in network.py returns 200 (70%), 304 (8%), 301 (10%), 302 (5%), 404 (4%), 403 (2%), 500 (1%)
- [x] HTTP MIME type mismatches with URI — _URI_MIME_MAP in baseline.py and generator.py pairs URIs to correct MIME types
- [ ] Proxy format doesn't match standard Squid or Bluecoat output
- [ ] Proxy lacks authenticated usernames (all "-") — healthcare proxies typically show NTLM/Kerberos auth
- [x] Proxy URL paths randomly paired with hostnames (e.g., download.windowsupdate.com/search?q=...) — site map data layer with 12 curated domains + 8 tag-based synthesis templates; browsing session generator selects paths from site-specific page definitions
- [x] Proxy lacks session depth — browsing session model generates landing page + subresource cascade (CSS/JS/images/fonts/favicon/API) + navigation to additional pages with referrer chains; persona-driven intensity (light/normal/heavy); cross-domain CDN fan-out; CONNECT tunnel deduplication with 5-min timeout
- [x] Proxy user-agent mismatch — removed system UAs (Windows-Update-Agent, Microsoft-CryptoAPI) from general _PROXY_UAS_WINDOWS pool; restricted workstation role traffic dns_tags to [background, windows]; added dns_tags to all persona profiles; retagged CDN/API domains in dns_registry
- [x] Web access log referrer headers — tightened web_access emitter can_handle() to require dst_host (destination is a scenario system); prevents outbound HTTPS connections from creating entries on source workstation
- [x] Referer header blank across all non-browsing HTTP paths — centralized `pick_referrer()`/`pick_scan_referrer()` helpers; baseline web-server traffic, auto-gen HTTP, storyline HTTP events, and proxy single-connection paths all now emit realistic distributions; scanner Referer behavior declarative via `send_referrer` preset field (Nikto: 30% same-origin; gobuster/sqlmap/dirb/nmap_http: none)
- [x] Nikto UA static test ID `(Test:map_codes)` — `render_ua()` token substitution engine; Nikto UA template uses `@NIKTO_TESTID@` generating unique 6-digit test IDs per request
- [x] Uniform jitter default (0.2) across all periodic event types — per-type defaults: BeaconEventSpec 0.15, WebScanEventSpec 0.4, CredentialSprayEventSpec 0.5, DgaQueriesEventSpec 0.3, DnsTunnelEventSpec 0.25
- [x] DHCP shows full discovery instead of renewals in mid-scenario windows — initial leases emitted during warm-up (suppressed); periodic REQUEST/ACK renewals at T/2 in _generate_system_traffic()

**Cisco ASA:**
- [x] Security: bound threat-detection deny timestamp tracking window to prevent unbounded memory/CPU growth
- [x] ASA imperfect-observation realism — addressed by the general observation profile layer. `complete` preserves paired training-friendly firewall evidence, while non-default profiles can apply deterministic ASA source-family gaps that create realistic missing/partial firewall evidence without rewriting canonical truth.
- [ ] ASA message type diversity limited to 106023/302013-16/305011-12 — missing 111008, 113004, 733100, 106001, 725001, 304001
- [x] ASA deny baseline burstiness/profile variance — fixed through host activity profiles and firewall-deny burst scheduling. Baseline denies now use deterministic burst/quiet periods and host/profile variance instead of uniform 3-7 second spacing.
- [x] ASA deny metadata diversity — fixed by carrying deny hash metadata on canonical firewall context and rendering stable varied ASA hash values where appropriate instead of hardcoded `[0x0, 0x0]`.
- [ ] Recognizable 45.33.32.x public IPs remain in built-in scan/attacker pools — the original `45.33.32.1` NAT PAT finding is stale, but code still uses `45.33.32.156` in scan/attacker pools. Move these values into data/config or replace them with less recognizable public-looking lab addresses during the broader public-IP/profile cleanup.

**eCAR:**
- [x] Limited object diversity on Linux — expanded _EDR_FILE_PATHS_LINUX from 5 to 20 entries (logs, caches, config files, /proc, package manager)
- [x] No FILE events on attack hosts — storyline processes now pass ensure_file_event=True, guaranteeing a FILE/CREATE for the process image
- [x] ensure_file_event PID/image mismatch — Event 11 file_create used child PID with parent image, breaking PID-based joins; fixed to use child's process_name for consistent attribution
- [x] No USER_SESSION events for server-side RDP lateral movement — generate_rdp_session() calls generate_logon() on target, which dispatches USER_SESSION/LOGIN to eCAR with EdrContext
- [x] Vary filenames in file operations — expanded _EDR_FILE_PATHS_WIN from 7 to 21 entries, _EDR_FILE_PATHS_LINUX from 5 to 20 entries
- [x] Template variable leak — literal `{psql_db}` appearing in eCAR output; stale audit finding: Linux query placeholders are handled by `_parameterize_command()`, with `tests/unit/test_activity_helpers.py` covering `{psql_db}` replacement.

**Cross-Source / General:**
- [x] Configurable cross-source evidence disagreement — implemented as named observation profiles with `complete` as the default. Non-default profiles can introduce deterministic dropped/delayed/filtered/out-of-window evidence across Zeek, web, proxy, firewall, IDS, Windows, Sysmon, syslog, bash history, and eCAR without contradictions or ambiguous rewrites; ground truth retains source evidence status for traceability.
- [x] Cross-sensor timestamp precision identical to 15+ decimal places — microsecond jitter added in snort.py, windows.py, and storyline.py
- [x] **P2** Per-host-type event rate multiplier — implemented as implicit host/activity profile defaults rather than scenario YAML fields. Domain controllers, file servers, web servers, proxies, Linux servers, and workstations now receive role/family/persona-specific multipliers across baseline activity, auth, endpoint, network, and source-specific noise.
- [x] Configurable per-entity artifact variation — implemented in the host/activity profile layer for baseline artifact texture, including stable per-host encoded PowerShell variants and profile-owned endpoint activity scaling.
- [x] Configurable per-host volume variance — implemented via stable host/persona/role multipliers applied across major activity families so hosts no longer share narrow uniform volume bands by construction.
- [ ] Configurable per-host/source log deployment coverage — observation profiles now support source-family gaps and host-scoped missingness multipliers, but explicit per-host source enablement/disablement remains future work. A later setting should model named host groups, disabled sensors, partial deployments, and collection windows when users need topology-level telemetry coverage differences rather than event-level missingness or host/activity volume variance.
- [ ] **P2** Generation speed and efficiency follow-up — Sprint 4 host/activity realism is functionally verified, but the slow-inclusive suite exposed that `pytest-cov` plus `tracemalloc` can make the medium dataset memory test pathological. A future sprint should profile generation without instrumentation noise, identify hot paths introduced by richer host activity/web fanout/firewall texture, and decide whether to optimize generation, mark the memory test `--no-cov`, or relax/update stale performance assertions.
- [x] DNS IP pool reuse causes cross-provider resolution (CloudFront→Microsoft IPs, etc.) — domain-first selection ensures consistent domain→IP mapping via FORWARD_DNS
- [x] AWS region mismatch between DNS PTR and SSL SNI for same IP — AWS hostname/PTR generation now derives a stable per-IP region/edge identity and PTR generation respects known forward hostname context.
- [x] TLS volume clustering design — added data-driven TLS destination profiles with overlay support and `eforge validate-config` schema/tag checks. Auto-generated external TLS now uses weighted enterprise, certificate-infra, package-update, developer-tool, and long-tail browsing profiles with stable per-host preferences. Smoke output had 28,544 TLS SNI rows, 116 distinct names, top SNI share 5.5%, and top-5 share 18.0%.
- [x] TLS/x509/OCSP contained realism pass — fixed IP/PTR-derived SNI without DNS evidence, omitted fresh x509 chains on resumed TLS sessions, staggered conn/ssl/x509/OCSP analyzer timestamps, prevented public-suffix wildcard SANs, and added varied OCSP status/timing behavior. Broader TLS volume clustering remains deferred for user discussion.
- [x] OCSP response cache realism — fixed expired `nextUpdate` values, made OCSP status stable per certificate identity, cached `thisUpdate`/`nextUpdate` windows by certificate/time bucket, and stopped using EC2 PTR-shaped hostnames as random AWS forward DNS/SNI defaults.
- [x] TLS certificate chain depth realism — added data-driven TLS realism config with overlay support for SAN suffixes, OCSP behavior, and chain templates; ssl.log now references leaf/intermediate FUID chains, x509.log emits matching CA/intermediate rows, and per-sensor FUID derivation preserves references.
- [x] TLS realism validate-config coverage — added `tls_realism.yaml` overlay/schema validation to `eforge validate-config` and removed stale hardcoded validation-count docs.
- [x] Scenario hostname authoring guidance — clarified that storyline connection/beacon `hostname` values should be the client-facing DNS name actually used for DNS/SNI/proxy evidence, not reverse-DNS/PTR artifacts.
- [x] Zeek SMB file visibility design — successful SMB data transfers now emit data-driven Zeek files.log observations with SMB source, MIME/analyzer mix, threshold, missing-byte, and timeout behavior from `smb_file_transfers.yaml`. Dedicated Zeek SMB logs remain out of scope; docs and skill references clarify conn.log/files.log/eCAR coverage.
- [x] Blind eval non-clustering follow-up — fixed concrete OCSP joinability, SMB session attribution, and DNS RTT realism findings while leaving cross-source “too perfect” and TLS clustering design deferred. Initial OCSP joinability used conn UID/tuple fields; the follow-up below replaced that with Zeek-native file-analysis linkage.
- [x] Blind eval protocol semantics follow-up — made OCSP Zeek-native via `ocsp.id` → `files.fuid` linkage, prevented public resolvers from answering internal AD/SRV split-horizon names, and fixed NTP client/server mode semantics. Generated smoke output showed 3,340 OCSP rows with 0 missing files.log FUID links, 96 NTP rows with 0 mode-3 records, and 0 internal AD-style DNS queries answered by public resolvers.
- [x] Post-TLS-profile blind review — fresh 50/50 blind review of `/tmp/eforge-blind-tls-review-output/data` rated synthetic likelihood at 90%. TLS clustering was reduced but not eliminated; top score-impacting findings shifted to provider/IP ownership incoherence (e.g., Google/Facebook/AWS names on wrong provider-looking IP ranges), revoked OCSP on successful mainstream browsing certs, templated NTP stratum/ref_id behavior, over-broad DMZ Zeek visibility, and proxy UA/domain mismatches for package/SaaS destinations.
- [x] Post-TLS blind review remediation — fixed provider/IP ownership coherence for curated TLS and site-map hosts, constrained revoked OCSP away from mainstream browsing domains, diversified internal/public NTP stratum/ref_id semantics, tightened multi-segment TAP visibility for internal east-west traffic, and cleaned proxy/TLS destination selection so servers avoid human SaaS pools and OS-specific package traffic uses OS-specific domains. Added focused tests plus `validate-config` coverage for site-map subresource hosts and TLS override tags.
- [x] Post-remediation blind review — generated `/tmp/eforge-blind-post-remediation-output/data` from the current branch and ran a 50/50 blind agent review. Score improved from 90% synthetic to 82% synthetic. Previous high-impact provider/IP/SNI/cert ownership and mainstream revoked-OCSP findings were not repeated for external TLS; remaining top findings are internal/private-IP TLS certificates using public CAs with empty SANs, Zeek files.log schema realism, NTP ref_id semantics, proxy UA/domain coherence for update/certificate endpoints, and a small DHCP visibility leak into the DMZ sensor.
- [x] Internal TLS certificate realism — internal/private-IP TLS sessions now use known internal hostnames, enterprise/private CA issuer, and hostname SANs instead of public CAs on private-IP CNs. Smoke output had 0 private-IP host-certificate CNs and enterprise examples such as `CN=web01.example.com`.
- [x] Zeek files.log schema realism — files.log now emits Zeek-native `tx_hosts`, `rx_hosts`, and `conn_uids` arrays and no longer renders connection-style `uid`/`id.*` fields. Smoke output had 0 schema-mismatch rows.
- [x] NTP ref_id semantics refinement — stratum >1 internal NTP responses now use stable IP-like upstream refids instead of full DNS hostnames. Smoke output had 0 internal NTP rows with hostname/primary-clock-style refs.
- [x] Post-top-three blind review — generated `/tmp/eforge-blind-top3-review-output/data` after internal TLS, Zeek files.log, and NTP ref_id fixes. Blind score regressed to 90% synthetic because the reviewer found deeper x509 identity bugs: duplicate certificate fingerprints with conflicting metadata, provider/CA mismatches for Google PKI and GitHub assets, internal SNI/SAN mismatches, files.log `local_orig` semantics for external response files, and remaining proxy UA/domain coherence issues. Strengths: DMZ visibility coherent, OCSP no mainstream revoked, NTP semantically plausible.
- [x] X509 certificate identity consistency — certificate fingerprints now include certificate-defining metadata for leaf and intermediate certificates, preventing one fingerprint from appearing with conflicting subject/issuer/key fields. Smoke output had 0 duplicate-fingerprint metadata conflicts.
- [x] Provider/CA overrides for PKI and asset hostnames — added CA overrides for `*.pki.goog`, `*.githubassets.com`, and `*.githubusercontent.com` so provider-owned certificate/asset hostnames use coherent issuers. Smoke output had 0 Google PKI/GitHub asset CA mismatches.
- [x] Internal SNI/SAN coherence — explicit internal SNI now controls enterprise certificate subject/SANs for private-IP TLS, rather than being overwritten by the destination system's canonical hostname. Smoke output had 0 internal SNI/SAN mismatches.
- [x] Zeek files.log `local_orig` semantics — OCSP response files now mark `local_orig` according to the transmitting response host, preventing external response files from appearing locally originated. Smoke output had 0 external-transmitter rows with `local_orig=true`.
- [x] Post-x509 blind review — generated `/tmp/eforge-blind-x509-review-output/data` after x509 identity/provider/SNI fixes. Blind score improved from 90% back to 82% synthetic. Previous duplicate-fingerprint/provider/SNI issues were mostly cleared, but the reviewer found the sibling x509 invariant: duplicate `certificate.issuer` + `certificate.serial` pairs with different fingerprints/key metadata. New medium findings: proxy UA/domain coherence, Zeek files.log hash analyzer fields missing, limited DHCP leakage into DMZ sensor, and low-impact NTP uniformity.
- [x] X509 issuer+serial uniqueness — leaf and intermediate serials now include certificate-defining metadata in the deterministic seed, so `certificate.issuer` + `certificate.serial` identifies one certificate profile instead of colliding across different keys/validity windows.
- [x] Zeek files.log hash fields — files.log now emits `md5`, `sha1`, and/or `sha256` when the configured analyzer list includes `MD5`, `SHA1`, or `SHA256`; SMB transfer hashes are deterministic from the transfer identity.
- [x] Proxy UA/domain refinement — `proxy_user_agents.yaml` now has data-driven domain overrides for Windows update, certificate, and telemetry endpoints; CONNECT proxy entries use those overrides instead of generic browser/API UAs.
- [x] DHCP sensor visibility refinement — DHCP lease events are marked link-local and route only to SPAN-style Zeek sensors on the client segment, preventing broadcast DHCP from leaking across unrelated TAP/firewall boundaries. `eforge validate` now warns when a segment has no SPAN Zeek sensor for link-local evidence.
- [x] NTP public server defaults — public NTP fallback choices moved into overlay-capable `network_params.yaml`; scenario-defined internal/domain NTP servers still take precedence, and internal NTP upstream refids draw from the configured public server pool.
- [x] Post-link-local blind review — generated `/tmp/eforge-blind-after-linklocal-output/data` after x509 serial, files hash, proxy UA, DHCP visibility, and NTP config fixes. Blind score improved from 82% to 78% synthetic. Resolved findings: x509 issuer+serial collisions, files hash/analyzer mismatches, DHCP UID linkage, DHCP/link-local leakage, CONNECT raw-IP targets, conn sorting/duplicate flow tells. Remaining high/medium findings are tracked below.
- [x] Proxy UA/domain coherence follow-up — domain-specific proxy User-Agent overrides now run as a final pass even when the proxy context inherited a prebuilt HTTP/browser session UA, so Windows Update, telemetry, CRL, and OCSP proxy rows keep service-specific UAs.
- [x] TLS intermediate CA profile stability — intermediate certificate profiles are cached by subject/issuer and reused across leaf certificates, preserving per-connection FUIDs while keeping serial/fingerprint/key/validity stable for the same CA certificate.
- [x] SMB files.log filename realism — SMB file-transfer rows can now include Zeek-native `filename` values generated from data-driven `smb_file_transfers.yaml` templates with overlay support, schema validation, tests, and docs.
- [x] NTP per-client association stability — NTP version, poll, precision, root delay, and root dispersion are now stable per client/server association while per-event timestamps continue to vary.
- [x] Post-coherence blind review — generated a fresh HTTP/proxy-focused dataset and ran blind 50/50 reviews. Score moved 92% → 86% → 92% synthetic. Completed fixes: reserved fixture domains, proxy cache HIT origin suppression, Let's Encrypt R3 key metadata, proxy W3C `sc-bytes` semantics, HTTPS proxy MISS response-size propagation into proxy→origin TLS/ASA byte accounting, disabled automatic Zeek weird generation pending a future compatibility matrix, removed OCSP files.log records on encrypted SSL UIDs, timestamp-ordered but non-epoch-shaped ASA IDs, ASA teardown timestamps that match rendered durations, Zeek responder packet counts for responder-history markers, failed/rejected Zeek conn service cleanup, and DNS response completion before dependent connection starts. Deferred: proxy resolver mix until a larger sample shows it is still a problem; TLS-intercepting proxy CONNECT+GET behavior is expected under the current project assumption but should be documented in the review fixture if blind reviewers lack scenario context.
- [x] HTTP/proxy blind review critical/high follow-up — fixed denied/inspected HTTPS proxy semantics and local proxy CONNECT timestamp ordering; implemented OCSP option 1 with issuer-aware OCSP responder config plus supporting HTTP/files evidence so `ocsp.id` joins to `files.fuid` and the file row has a visible `conn_uids` link. Broader cross-emitter timing should use a shared event-timing helper/context in a later pass if additional concrete ordering bugs appear.
- [x] HTTP/proxy blind review second critical/high follow-up — denied DNS now renders as source-side query/no-response evidence, proxy tunnel setup status is distinct from inspected request status at the canonical proxy context level, ASA connection ID low-order digits are diversified, and known internal DNS names no longer get random SERVFAILs. Regenerated HTTP/proxy fixture smoke checks showed matching proxy/Zeek CONNECT status counters, no internal `example.com` SERVFAIL rows, varied ASA terminal digits, and intact OCSP files linkage; the denied-DNS case is covered by focused unit tests.
- [x] HTTP/proxy blind review third critical/high follow-up — plain HTTP explicit-proxy client-side Zeek now sees the proxy decision/status/body for denied requests, enterprise-issued certificates use the internal `ocsp.example.com` responder from data-driven TLS realism config, and Zeek UID/FUID derivation skips obvious synthetic marker substrings. Regenerated fixture smoke checks showed the denied proxy row matched a client-side 403/body-size row, enterprise OCSP used the internal responder, and no `FAKE` marker IDs remained.
- [x] HTTP/proxy docs/tests/skills/reference audit — verified coverage for proxy, DNS, ASA, OCSP, and Zeek UID realism changes; added validate-config coverage for OCSP responder hostnames in `dns_registry.yaml`; updated evidence-format docs, config references, skill-local scenario/evidence references, and customization guidance.

**Other:**
- [x] ✓³ Bash history only for root on compromised hosts — baseline SSH sessions now generate per-user bash history for admins on all Linux servers (34 files vs 3); organic noise commands interleaved via generate_bash_command_with_noise()
- [x] Bash history still lacks typos, repeated commands, tab-completion artifacts — bash_commands.yaml with per-role command vocabularies (sysadmin/dba/webadmin/developer/security), template parameterization, 5% typo rate; per-server RBAC user rosters via _get_server_ssh_users()
- [x] Baseline generates IPs outside defined network segments — external IP generator excludes org CIDRs; diagnostic validator warns on out-of-segment internal IPs
- [x] Parsability at ~95% (5% records fail structure validation) — stale audit finding: evaluator parsed 1,056,984/1,057,006 records successfully (99.998% parsability).
- [x] Evaluation schema missing Windows Security EventIDs 4800/4801 — audit evaluator failures were the 22 generated workstation lock/unlock events rejected by `windows_event_security` allowed_values, despite the template task map already including 4800/4801. Added the IDs to the base allowed-values list and covered the regression in format-definition tests.

### Tier 4: Eval Fixes

- [x] Harden temporal causal-account exclusion against non-string SubjectUserName/principal values to prevent evaluator exceptions on malformed logs
- [x] Signal integrity misses web_scan traces in host-scoped web logs and responder-side Zeek HTTP records — generated evidence exists, but evaluator indexing could not find `web_access.log` records by host directory or inbound Zeek HTTP by destination IP. Parser records now carry source-host metadata, and signal-integrity indexing includes responder IPs. Event Presence improved from 1/9 to 9/9 on the HTTP/proxy eval sample.
- [x] Causal Ordering hard failure on generated audit sample — root cause was future same-hour session reuse during non-chronological baseline generation. Session lookup now only reuses sessions whose start time is at or before the activity timestamp. Fresh HTTP/proxy sample eval improved Causal Ordering from 95.53% to 99.94%, and all hard acceptance criteria pass.
- [x] Storyline Trace Coverage hostname normalization bug — resolved by later FQDN/bare hostname indexing and storyline trace normalization fixes.
- [ ] Ground truth File IOCs section truncated in GROUND_TRUTH.md output

### Cross-Source Correlation (depends on Tier 1 baseline migration)

Once baseline activity uses SecurityEvent dispatch, these become straightforward:

- [x] Migrate eCAR FILE/REGISTRY/MODULE to SecurityEvent dispatch (enables 4663 + Sysmon 11/12/13 correlation) — completed in Phase 8.2; probabilistic EDR events dispatch via SecurityEvent with EdrContext
- [x] Migrate syslog system messages: CRON↔eCAR PROCESS, UFW BLOCK↔Zeek conn, systemd↔eCAR PROCESS — CRON and UFW were already working; systemd now uses paired generate_system_process/generate_system_process_termination lifecycle
- [x] Sysmon Event 3 (Network), 11 (FileCreate), 13 (Registry) emission — implemented alongside Sysmon P1

---

## Data Realism — COMPLETE

**Goal:** Address structural realism gaps identified by adversarial review. These are issues where the generated data is technically correct but experienced analysts or ML models would identify it as synthetic due to missing real-world patterns. Prioritized by impact on analyst training, then implementation complexity.

**Completed:** All items except #13 (Cloud/SaaS formats, deferred to post-MVP). Sensor timestamp skew (#10) dropped — tight NTP is best practice.

### Temporal Realism

- [x] **Causal event ordering** — CausalExpansionEngine with 4 composable rules (DnsBeforeConnection, KerberosBeforeLogon, ProcessAccessAfterRemoteThread, SupplementaryAuditEvents). Validator warns on redundant manual prerequisites. Evaluator scores DNS→connection and Kerberos→logon causal pairs.
- [x] **Hawkes/bursty temporal model** — Replaced cluster model with Hawkes self-exciting process (Lewis-Shedler thinning). Parameters derived from persona risk_profile. Cross-hour state continuity. Storyline multi-event steps use typing cadence. System traffic uses periodic+jitter. Lateral movement uses hash-based periodic offsets.
- [x] **Day-of-week variation** — Monday 1.15x login storms, Friday 0.85x early departures, Saturday/Sunday 0.05-0.08x near-zero. Non-IT personas skipped on weekends.
- ~~**Sensor timestamp skew**~~ — Dropped: tight NTP is best practice in production environments.

### Baseline Depth

- [x] **Process → network correlation** — Baseline processes now emit correlated connections via _PROCESS_NETWORK_MAP (browsers→HTTPS, Office→cloud, DB clients→SQL, dev tools→registries). 60% emission probability with process PID for eCAR FLOW correlation.
- [x] **Linux baseline activity** — SSH login/key exchange messages (70% key / 30% password), package management (apt-daily/dnf-automatic), systemd timer execution (fstrim/logrotate/tmpfiles), logrotate file detail, journald runtime statistics. 18 syslog categories total.
- [x] **Legitimate lateral movement** — 26 patterns: backup agents, monitoring, AD replication, app→DB, config management, DNS zone transfers, NFS, Docker registry, syslog relay, etc. Conditional on environment topology and system roles.
- [x] **Stale account enrichment** — Kerberos pre-auth failures (4771, 0x12), scheduled task failures (batch logon type 4), service startup failures (type 5, first hour), plus existing failed network logons.

### Red Herring Sophistication

- [x] **Network-level red herrings** — 3 new patterns: suspicious DNS (high-entropy CDN subdomains, DoH providers), unusual outbound (cloud regions, dev tools, large backup sync), scheduled vulnerability scan overlap. 7 total patterns now.
- [x] **Expand suspicious ambient noise types** — Covered by network-level red herrings above (large outbound transfers, scan overlap).

### Entity Consistency

- [x] **Entity lifecycle validation** — StateManager tracks per-system boot times (register_boot_time at process tree seeding). validate_target_pid() checks PID existence for Sysmon 8/10 events. Warnings logged for impossible sequences.

### Format Expansion

- [x] **Static command pool diversification** — All process template categories parameterized with {placeholder} syntax. New _GENERAL_PARAMS pool (project paths, doc names, build configs, git branches, internal URLs). Per-user affinity via {username} substitution.

### State Pre-Population

- [x] **Warm-up period** — Configurable `warmup` field on `time_window` (default `"8h"`). Runs baseline generation before `start` to pre-populate DNS cache, process trees, active sessions, Kerberos tickets, Hawkes timing kernels, and event counters. Events during warm-up update internal state but are not written to output files, eliminating cold-start artifacts (logon bursts, universal DNS cache misses, orphaned process parents).

---

## Post-MVP Enhancements (Future)

### Short-term
- [ ] **Configurable work-week schedules** — Allow scenario authors to shift the typical workday (e.g., Tues–Sunday for retail/healthcare), define shift workers with non-standard hours, or specify per-persona day-of-week overrides
- [ ] **Storyline cadence field** — `cadence: human|automated|periodic(interval, jitter)` on storyline steps for malware beacons, AI-driven attacks, and automated exfiltration with appropriate timing (currently all steps use human typing cadence by default)
- [ ] **Cloud/SaaS log formats** — Azure AD sign-in logs, AWS CloudTrail, GCP audit logs, M365 audit logs. Most modern SOCs are hybrid; on-prem-only formats limit training relevance
- [ ] `snort_alert` typed event spec for IDS signature declarations
- [ ] HTTP proxy server support (Squid, Blue Coat, Zscaler)
- [ ] Checkpointing and resume for long-running generation
- [ ] Additional skills: create-persona, create-log-format, create-network, analyze-output
- [ ] Example scenario collection (ransomware, credential stuffing, insider threat)
- [ ] Config file inheritance/templating
- [ ] Overlay `_replace: true` recursive propagation — currently `_replace` only affects top-level list fields within a keyed entry; nested lists (e.g., `platforms.windows.command_templates`) still extend. Low impact: replacing entire app definitions with nested platform configs is rare.
- [ ] Overlay `_delete: true` for removing built-in entries — users cannot suppress stock domains/apps/personas from generation. Deferred until a real use case surfaces.
- [ ] Subset sensor format support (e.g., `log_formats: [zeek, -zeek_dns]`)
- [ ] PyPI package distribution
- [ ] Network diagram ingestion for auto-inferred sensor placement
- [ ] Performance optimizations (Rust extensions, better parallelization)
- [ ] Full user directory export as separate CSV
- [ ] Separate student/instructor output packages

### Medium-term
- [ ] Web UI for scenario creation
- [ ] Streaming output to SIEM/data lakes
- [ ] Log format auto-detection from samples
- [ ] **D3FEND Defensive Response Modeling** — Extend storyline events with environmental response: when an attack step fires, security controls react (account lockout on brute force, EDR quarantine on suspicious process, firewall deny on lateral movement). Driven by a defense profile in scenario YAML defining deployed controls (EDR, DLP, lockout policy, firewall rules). Some attacks succeed, some get partially blocked, some get detected but not prevented. Creates more realistic training (analysts see attacks that don't go unopposed) and more correlation opportunities ("trace from EDR alert back to initial access").
- [ ] **ML-Informed Baseline Profiles** — Optional offline pipeline: feed in sanitized real logs, extract statistical profiles, output persona definitions matching actual observed patterns. Covers temporal distributions (hourly activity curves, night shifts), process/application mix (top 50 processes, PowerShell:cmd ratio), network patterns (DNS volume, port distributions, internal/external ratios), and authentication patterns (failed login frequency, Kerberos:NTLM ratios). Ships as `eforge learn` CLI command producing persona YAML. Closes the gap between structurally correct synthetic data and data that "feels" real to experienced analysts.

### Long-term
- [ ] OT/ICS environment simulation
- [ ] Real-time log streaming mode
- [ ] Collaborative scenario editing
- [ ] Scenario marketplace
- [ ] Integration with attack frameworks (CALDERA, Atomic Red Team)
- [ ] **High-Performance Generation Mode** — Parallelize generation for enterprise-scale scenarios (200+ users, 7+ days, CI pipelines). Two approaches: (1) parallelize across emitters — EventDispatcher fans out to 20+ emitters concurrently (lower risk, emitters don't share state); (2) parallelize across time windows — process hours in parallel batches with StateManager coordination (higher complexity, bigger payoff). Even approach #1 removes the proportional scaling ceiling for large scenarios.

---

## Field Test Gaps (FOR668/FOR669 Exercise Data)

Gaps identified by comparing exercise data requirements against current engine capabilities. Full per-exercise analysis and recommendations in [scenarios/EXERCISE_DATA_REQUIREMENTS.md](scenarios/EXERCISE_DATA_REQUIREMENTS.md).

### Cluster 1: Configurable Bulk Event Framework + DNS Independence

Highest impact — unblocks or improves 10 exercises across all 5 days. These are all variations of "generate N events matching a pattern over a time window." A single YAML-configurable bulk event primitive with type-specific parameter sets covers all of them. DNS independence is part of this because DNS beaconing and DGA are primary use cases driving the framework.

- [x] General repeating/bulk event primitive (`_PeriodicEventBase` + `_iter_periodic_ticks()` shared engine)
- [x] Built-in type: beacon — any protocol (HTTP/S, SSH, DNS, NTP, arbitrary), permitted or blocked
- [x] Built-in type: web_scan — directory enumeration, vuln probing, URI lists, status code distribution (5 presets with overlay support)
- [x] Built-in type: credential_spray — bulk failed_logon with spray/brute_force/stuffing patterns, optional success
- [x] Built-in type: dga_queries — domain generation parameters (length, TLD, charset, count, rcode distribution, deterministic seed)
- [x] Standalone dns_query event type (query, qtype, rcode, ttl) — DNS records independent of TCP connections
- [x] DNS TTL control field on dns_query events
- [x] Replaced `blocked_c2` with beacon `action: deny` (blocked_c2 removed)
- [x] Built-in type: dns_tunnel — encoded subdomain exfiltration (base32/base64/hex, TXT/NULL/CNAME, payload chunking)
- [ ] DGA algorithm presets (known malware families — Conficker, Suppobox, etc.)
- [ ] Dictionary-based DGA (word combination domains)
- [ ] active_hours / active_days on periodic types
- [ ] Connection to non-listening host (conn_state=REJ/S0 without firewall deny)

**Exercises:** 1.1 (web_scan), 1.1b (beacon), 1.3 (injection payload volume), 3.3 (beacon), 4.1 (dns_query, dga), 4.2 (dns_query, dga), 5.1 (credential_spray)

### Cluster 2: Format Filtering

High breadth, low cost — makes multi-week generation practical for 5 exercises without deep optimization.

- [x] `--formats` CLI filter with intersection semantics and group name support
- [x] `format_groups` inventory in `eforge info` output

**Exercises:** 3.1, 3.2, 3.3, 5.1, 5.2 (all need 2-4 week windows)

### Cluster 3: Temporal Baseline Phases — Resolved by Design

Achievable by composing bulk event primitives (beacon, connection, dns_query) over a stable baseline. Students detect injected activity as statistical outliers. No engine changes needed — documented as a scenario authoring pattern.

**Exercises:** 3.2 (gradual behavioral shifts — use beacons with start_time offsets and orig_bytes overrides)

### Cluster 4: Windows Auth Enrichment

Same area of codebase — baseline engine Windows auth generation, persona work schedules.

- [x] Broader baseline 4648 generation (service account delegation, sysadmin RunAs, SCCM/GPO, helpdesk remote)
- [x] Event IDs 4800/4801 (workstation lock/unlock with persona variance, paired 4624 type 7, failed unlock)
- [x] Storyline EventSpecs: explicit_credentials, workstation_lock, workstation_unlock

**Exercises:** 5.1 (4800/4801), 5.2 (4648 breadth)

### Cluster 5: Labeled Data Export

Out of scope — Ex 4.2 MLTK classifier needs real-world labeled domains (threat intel + benign lists), not synthetic EvidenceForge labels. Dataset curation task, not an engine feature.

---

## Notes

- **Testing:** Write tests alongside implementation, not after
- **Documentation:** Update docs incrementally, not all at end
- **Dependencies:** Add via `uv add`, never use `pip` directly
- **Changelog:** When completing a phase, move detailed task history to [CHANGELOG.md](CHANGELOG.md)
