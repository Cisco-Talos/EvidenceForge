# v2.1.2 Release Preparation

**Date:** 2026-09-24
**Branch:** `dev`
**Release target:** `main`

## Scope

Prepare the patch release following `v2.1.1`. The 89 non-merge commits from `v2.1.1` through
`bf328206` are fixes, documentation, or maintenance; no feature or breaking-change commit was
found. The version is therefore `2.1.2`. The changelog groups those commits by behavior area and
includes their short SHAs.

## Validation

- Slow gate: the immediately preceding full run on the same source HEAD (`bf328206`) passed with
  1,794 passed, 5 skipped, in 26m39s. Release preparation changes only version, changelog,
  roadmap, worklog, and lock metadata; no slow tests were rerun.
- Ruff lint: passed (`uv run --extra dev ruff check .`).
- Ruff format: passed, 896 files already formatted.
- Routine suite with the 70% coverage gate: passed, 11,709 passed, 48 skipped, 2,030 deselected
  in 696.59s; total coverage 75.57%.
- Version declaration/lock consistency: `pyproject.toml`, `src/evidenceforge/__init__.py`, and
  `uv.lock` all declare `2.1.2`; `uv sync` regenerated only the package version in the lockfile.
- Generation behavior declaration: passed, revision 153, digest
  `d88b27d5f6236de3ffff07e4ec2d7419e3543be12ef0005121ae4e7d1afa2046`.
- Python 3.12 to 3.13 checkpoint verify/resume portability: passed.
- Source distribution and wheel: built successfully under `/private/tmp/eforge-2.1.2-dist/`.
- Release PR and CI: pending.

## Release flow

After local checks, commit the release bump on `dev` and open a PR from `dev` to `main`. CI owns
release tag and GitHub Release creation after merge. Merge/publication remain maintainer actions.
