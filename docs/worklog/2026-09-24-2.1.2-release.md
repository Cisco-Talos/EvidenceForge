# v2.1.2 Release Preparation

**Date:** 2026-09-24
**Branch:** `dev`
**Release target:** `main`

## Scope

Prepare the patch release following `v2.1.1`. The 89 non-merge commits from `v2.1.1` through
`bf328206` are fixes, documentation, or maintenance; no feature or breaking-change commit was
found. The version is therefore `2.1.2`. The changelog groups those commits by behavior area and
includes their short SHAs.

## Validation

- Slow gate: the immediately preceding full run on the same source HEAD (`bf328206`) passed with
  1,794 passed, 5 skipped, in 26m39s. Release preparation changes only version, changelog,
  roadmap, worklog, and lock metadata, so the full suite was not repeated locally for the bump.
- Ruff lint: passed (`uv run --extra dev ruff check .`).
- Ruff format: passed, 896 files already formatted.
- Routine suite with the 70% coverage gate: passed, 11,709 passed, 48 skipped, 2,030 deselected
  in 696.59s; total coverage 75.57%.
- Version declaration/lock consistency: `pyproject.toml`, `src/evidenceforge/__init__.py`, and
  `uv.lock` all declare `2.1.2`; `uv sync` regenerated only the package version in the lockfile.
- Generation behavior declaration: passed, revision 153, digest
  `d88b27d5f6236de3ffff07e4ec2d7419e3543be12ef0005121ae4e7d1afa2046`.
- Python 3.12 to 3.13 checkpoint verify/resume portability: passed.
- Source distribution and wheel: built successfully under `/private/tmp/eforge-2.1.2-dist/`.
- Release PR: [#429](https://github.com/Cisco-Talos/EvidenceForge/pull/429), open.

## Windows CI failure and repair

The [first PR CI run](https://github.com/Cisco-Talos/EvidenceForge/actions/runs/36046830702)
finished the Windows routine suite in 26m28s, below its 45-minute job limit.
One of 11,757 selected tests failed: the native Windows Bash history journal case raised
`OverflowError: Python int too large to convert to SQLite INTEGER` while storing an export plan.
The Windows test module was skipped in the earlier local macOS run. Linux routine, all four slow
shards, Windows checkpoint durability, Python portability, lint, and release version guard passed.

Commit `938f7da5` stores native file identities as exact SQLite BLOB values and accepts older
integer rows when reading a plan. A cross-platform regression injects values above the SQLite
signed-integer limit. Local validation: 1 regression test, 197 Bash/Snort exact-publication tests,
and 23 behavior-manifest tests passed; repository-wide Ruff lint and formatting passed. The
generation behavior manifest advanced to revision 154 with `none` evidence impact. PR CI must
confirm the fix on a Windows runner.

## Release flow

The release bump was committed on `dev` and PR #429 targets `main`. CI owns release tag and GitHub
Release creation after merge. Merge/publication remain maintainer actions.
