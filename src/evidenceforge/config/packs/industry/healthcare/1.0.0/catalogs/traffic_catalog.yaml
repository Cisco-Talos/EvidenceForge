traffic_catalog:
  clinical-shift:
    description: "Shift-oriented clinical application traffic shape."
    data:
      audience: [clinical_coordinator]
      cadence: shift-change
      outbound:
        - role: _external
          port: 443
          service: ssl
          weight: 20
          emit_dns: true
          dns_tags: [healthcare]
