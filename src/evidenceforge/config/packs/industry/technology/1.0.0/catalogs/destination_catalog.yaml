destination_catalog:
  artifact-registry:
    description: "Synthetic package and container registry destination family."
    data:
      tags: [dev, storage]
      service: ssl
      endpoints:
        - domain: registry.technology.example
          ips: [203.0.113.80]
